---
phase: 06-combat-detail-panel
plan: 04
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - src/views/CompendiumView.vue
  - src/components/AppLayout.vue
  - src/views/__tests__/CompendiumView.test.ts
  - src/components/__tests__/AppLayout.test.ts
autonomous: true
requirements: [WORK-06]
user_setup: []
must_haves:
  truths:
    - "CompendiumView renders a 2-column layout — 30% left (filters + entity list) and 70% right (StatBlock or placeholder)"
    - "Compendium right column shows 'Select an entity to view its stat block.' when nothing is selected"
    - "Clicking an entity in the Compendium list renders StatBlock in the right column with that entity's rawData"
    - "AppLayout no longer renders CreatureDetailPanel component"
    - "CreatureDetailPanel.vue and creatureDetail.ts store files are deleted"
  artifacts:
    - path: "src/views/CompendiumView.vue"
      provides: "2-column layout with session config header, inline StatBlock"
      min_lines: 50
    - path: "src/components/AppLayout.vue"
      provides: "Clean layout without CreatureDetailPanel"
  key_links:
    - from: "src/views/CompendiumView.vue"
      to: "src/components/StatBlock.vue"
      via: "<StatBlock :raw-data> in right column"
      pattern: "StatBlock.*raw-data"
    - from: "src/views/CompendiumView.vue"
      to: "src/components/CreatureBrowser.vue"
      via: "@select handler setting selectedEntity"
      pattern: "@select.*selectedEntity"
---

<objective>
Replace CompendiumView's single-column layout with a 2-column inline layout (filters+list left, StatBlock right), add session config header (party level/size), delete CreatureDetailPanel.vue and creatureDetail.ts store, and clean AppLayout.

Purpose: Completes the slide-over removal and delivers the inline stat block experience for the Compendium page per locked decisions. Also removes dead code (CreatureDetailPanel + store).

Output: Updated `CompendiumView.vue`, updated `AppLayout.vue`, deleted `CreatureDetailPanel.vue` and `creatureDetail.ts`, updated test files.
</objective>

<execution_context>
@C:/Users/kiryl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kiryl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-combat-detail-panel/06-CONTEXT.md
@.planning/phases/06-combat-detail-panel/06-RESEARCH.md
@.planning/phases/06-combat-detail-panel/06-UI-SPEC.md
@.planning/phases/06-combat-detail-panel/06-01-SUMMARY.md
@.planning/phases/06-combat-detail-panel/06-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/components/StatBlock.vue (created in Plan 01):
```typescript
defineProps<{ rawData: object | null; isLoading?: boolean }>()
```

From src/components/CreatureBrowser.vue (updated in Plan 02):
```typescript
defineProps<{
  defaultEntityType?: string; mode?: 'compendium' | 'combat';
  selectedId?: number | null; partyLevel?: number;
}>()
// Emits 'select' with EntityResult (both modes after Plan 02)
```

From src/lib/entity-query.ts:
```typescript
export interface EntityResult {
  id: number; name: string; entityType: string; pack: string; slug: string;
  level: number | null; rarity: string | null; family: string | null; rawData: string;
}
```

CompendiumView current structure:
```vue
<div class="flex flex-col h-full overflow-hidden bg-charcoal-800">
  <div class="px-6 py-4 border-b border-charcoal-600 flex-shrink-0">
    <h1 class="text-2xl font-display font-bold text-gold">Compendium</h1>
  </div>
  <!-- loading / empty / browser states -->
</div>
```

AppLayout current structure:
```vue
<div class="flex h-screen bg-charcoal-800 overflow-hidden">
  <AppSidebar />
  <main class="flex-1 overflow-y-auto"><RouterView /></main>
  <CreatureDetailPanel />  <!-- TO BE REMOVED -->
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CompendiumView 2-column layout with inline StatBlock + session config header</name>
  <files>src/views/CompendiumView.vue, src/views/__tests__/CompendiumView.test.ts</files>
  <read_first>
    - src/views/CompendiumView.vue
    - src/views/__tests__/CompendiumView.test.ts
    - src/components/StatBlock.vue
    - .planning/phases/06-combat-detail-panel/06-UI-SPEC.md
    - .planning/phases/06-combat-detail-panel/06-CONTEXT.md
  </read_first>
  <action>
    **Rewrite `src/views/CompendiumView.vue`:**

    **Script setup:**
    ```typescript
    import { ref, onMounted } from 'vue'
    import CreatureBrowser from '@/components/CreatureBrowser.vue'
    import StatBlock from '@/components/StatBlock.vue'
    import { db } from '@/lib/database'
    import { syncState } from '@/lib/schema'
    import type { EntityResult } from '@/lib/entity-query'

    const hasSyncedData = ref<boolean | null>(null)
    const selectedEntity = ref<EntityResult | null>(null)
    const parsedRawData = ref<any>(null)
    const partyLevel = ref<number>(1)
    const partySize = ref<number>(4)
    const statBlockPanel = ref<HTMLDivElement | null>(null)

    onMounted(async () => {
      try {
        const rows = await db.select().from(syncState).limit(1)
        hasSyncedData.value = (rows[0]?.totalEntities ?? 0) > 0
      } catch {
        hasSyncedData.value = true
      }
    })

    function handleEntitySelect(entity: EntityResult) {
      selectedEntity.value = entity
      try {
        parsedRawData.value = JSON.parse(entity.rawData)
      } catch {
        parsedRawData.value = null
      }
      // Scroll right column to top on entity change
      if (statBlockPanel.value && typeof statBlockPanel.value.scrollTo === 'function') {
        statBlockPanel.value.scrollTo(0, 0)
      }
    }
    ```

    **Template — full rewrite:**
    ```html
    <div class="flex flex-col h-full overflow-hidden bg-charcoal-800">
      <!-- Loading skeleton (DB check pending) -->
      <div v-if="hasSyncedData === null" class="flex-1 flex items-center justify-center">
        <div class="animate-pulse bg-charcoal-700 h-4 w-48 rounded" />
      </div>

      <!-- Empty state (no synced data) -->
      <div v-else-if="hasSyncedData === false"
        class="flex-1 flex flex-col items-center justify-center gap-3">
        <p class="text-sm font-bold text-stone-300">No data synced yet</p>
        <p class="text-xs text-stone-400">Import PF2e content to browse the Compendium.</p>
        <RouterLink to="/sync" class="text-gold hover:text-gold-light text-sm font-bold underline">
          Go to Sync Data
        </RouterLink>
      </div>

      <!-- Main 2-column layout -->
      <template v-else>
        <div class="flex flex-1 min-h-0">
          <!-- Left column (30%): session config + filters + entity list -->
          <div class="w-[30%] min-w-[240px] flex flex-col border-r border-charcoal-600 overflow-hidden">
            <!-- Session config header: party level + party size -->
            <div class="bg-charcoal-900 border-b border-charcoal-600 px-3 py-2 flex items-center gap-3 flex-shrink-0">
              <div>
                <label class="text-xs text-stone-400 mb-1 block">Party Level</label>
                <input type="number" v-model.number="partyLevel" min="1" max="20"
                  class="w-14 bg-charcoal-700 border border-charcoal-600 rounded px-2 py-1 text-sm text-stone-200 focus:border-gold focus:outline-none" />
              </div>
              <div>
                <label class="text-xs text-stone-400 mb-1 block">Party Size</label>
                <input type="number" v-model.number="partySize" min="1" max="8"
                  class="w-14 bg-charcoal-700 border border-charcoal-600 rounded px-2 py-1 text-sm text-stone-200 focus:border-gold focus:outline-none" />
              </div>
            </div>

            <!-- CreatureBrowser with filters + entity list -->
            <CreatureBrowser
              :selected-id="selectedEntity?.id ?? null"
              :party-level="partyLevel"
              @select="handleEntitySelect"
              class="flex-1 min-h-0"
            />
          </div>

          <!-- Right column (70%): StatBlock or placeholder -->
          <div ref="statBlockPanel" class="flex-1 overflow-y-auto">
            <div v-if="!selectedEntity"
              class="flex items-center justify-center h-full text-stone-500 text-sm">
              Select an entity to view its stat block.
            </div>
            <StatBlock v-else :raw-data="parsedRawData" />
          </div>
        </div>
      </template>
    </div>
    ```

    Note: No `<h1>Compendium</h1>` header — the page title is removed in favor of maximizing vertical space for the 2-column layout. The sidebar nav already indicates the user is on the Compendium page. If the executor thinks the heading should stay, it can be added as a slim bar above the 2-column flex — but the CONTEXT.md does not mention keeping it.

    Actually, re-reading CONTEXT.md: no mention of removing the heading. Keep it slim:
    ```html
    <!-- Page heading (slim) -->
    <div class="px-4 py-2 border-b border-charcoal-600 flex-shrink-0">
      <h1 class="text-lg font-display font-bold text-gold">Compendium</h1>
    </div>
    ```
    Place this above the 2-column flex.

    **Note on CreatureBrowser props in CompendiumView:**
    - `show-party-controls` is NOT passed (or set to false) because party controls are in the session config header above, not in EntityFilterBar for Compendium mode
    - Actually, if EntityFilterBar defaults `showPartyControls` to true, we need to explicitly pass `:show-party-controls="false"` here. But CreatureBrowser doesn't have that prop — EntityFilterBar does. So CreatureBrowser needs to forward it. However, a simpler approach: in CompendiumView, DON'T pass partyLevel through CreatureBrowser to EntityFilterBar's party controls. Instead, EntityFilterBar's `showPartyControls` defaults to `true`, and CreatureBrowser can pass it through.

    Simplest approach: CreatureBrowser gets a `showPartyControls` prop (default true in combat, false in compendium). CompendiumView passes `:show-party-controls="false"` since party controls live in CompendiumView's own session config header.

    Wait — Plan 02 already established `showPartyControls` on EntityFilterBar. CreatureBrowser needs to pass it through. Add to CreatureBrowser's props in Plan 02 (or the executor handles it here since it's a simple passthrough).

    For this plan: pass `:show-party-controls="false"` to CreatureBrowser which forwards to EntityFilterBar. If CreatureBrowser doesn't have this prop yet, add it as a simple passthrough.

    **Update `src/views/__tests__/CompendiumView.test.ts`:**
    - Stub StatBlock and CreatureBrowser components
    - Test: renders 2-column layout — left column has `w-[30%]` class, right column exists
    - Test: renders session config header with "Party Level" and "Party Size" labels
    - Test: renders placeholder text "Select an entity to view its stat block." when no entity selected
    - Test: loading skeleton renders when hasSyncedData is null
    - Test: empty state renders when hasSyncedData is false
    - Update any existing tests that reference old single-column layout structure
  </action>
  <verify>
    <automated>cd D:/pathbuddy && npx vitest run src/views/__tests__/CompendiumView.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/views/CompendiumView.vue contains `StatBlock` import
    - src/views/CompendiumView.vue contains `w-[30%]` (left column width)
    - src/views/CompendiumView.vue contains `flex-1 overflow-y-auto` (right column)
    - src/views/CompendiumView.vue contains `Select an entity to view its stat block.` placeholder text
    - src/views/CompendiumView.vue contains `Party Level` and `Party Size` label text
    - src/views/CompendiumView.vue contains `partyLevel = ref` and `partySize = ref`
    - src/views/CompendiumView.vue contains `handleEntitySelect` function
    - src/views/CompendiumView.vue contains `parsedRawData` ref
    - CompendiumView tests pass
  </acceptance_criteria>
  <done>CompendiumView renders a 2-column layout with session config header (party level/size) above the left column, CreatureBrowser in left column, and StatBlock or placeholder in right column. Entity selection parses rawData and renders StatBlock inline. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Delete CreatureDetailPanel + creatureDetail store + clean AppLayout</name>
  <files>src/components/AppLayout.vue, src/components/__tests__/AppLayout.test.ts</files>
  <read_first>
    - src/components/AppLayout.vue
    - src/components/__tests__/AppLayout.test.ts
    - src/components/CreatureDetailPanel.vue
    - src/stores/creatureDetail.ts
  </read_first>
  <action>
    **1. Delete `src/components/CreatureDetailPanel.vue`:**
    Remove the file entirely. This component is replaced by StatBlock.vue (used inline in both CompendiumView and CombatDetailPanel).

    **2. Delete `src/stores/creatureDetail.ts`:**
    Remove the file entirely. The useCreatureDetailStore Pinia store is no longer used anywhere after Plans 02 (CreatureBrowser) and 03 (CombatTracker) removed their imports.

    **3. Delete `src/components/__tests__/CreatureDetailPanel.test.ts`:**
    Remove the test file — the component no longer exists.

    **4. Modify `src/components/AppLayout.vue`:**
    Remove CreatureDetailPanel import and usage.

    Old:
    ```vue
    <script setup lang="ts">
    import AppSidebar from './AppSidebar.vue'
    import CreatureDetailPanel from './CreatureDetailPanel.vue'
    </script>

    <template>
      <div class="flex h-screen bg-charcoal-800 overflow-hidden">
        <AppSidebar />
        <main class="flex-1 overflow-y-auto">
          <RouterView />
        </main>
        <CreatureDetailPanel />
      </div>
    </template>
    ```

    New:
    ```vue
    <script setup lang="ts">
    import AppSidebar from './AppSidebar.vue'
    </script>

    <template>
      <div class="flex h-screen bg-charcoal-800 overflow-hidden">
        <AppSidebar />
        <main class="flex-1 overflow-y-auto">
          <RouterView />
        </main>
      </div>
    </template>
    ```

    **5. Update `src/components/__tests__/AppLayout.test.ts`:**
    - Remove any assertions that check for CreatureDetailPanel being rendered
    - Remove any mocks of `useCreatureDetailStore`
    - Add test: AppLayout does NOT render CreatureDetailPanel (verify the component is absent)
    - Keep existing tests for sidebar rendering, RouterView rendering

    **6. Verify no remaining imports of deleted files:**
    Run a grep across the codebase to confirm:
    - No file imports `CreatureDetailPanel` from `@/components/CreatureDetailPanel`
    - No file imports `useCreatureDetailStore` from `@/stores/creatureDetail`
    If any stale imports remain (should not after Plans 02 and 03), fix them.

    **7. Run full test suite** to confirm no regressions from deletions.
  </action>
  <verify>
    <automated>cd D:/pathbuddy && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/CreatureDetailPanel.vue does NOT exist (deleted)
    - src/stores/creatureDetail.ts does NOT exist (deleted)
    - src/components/__tests__/CreatureDetailPanel.test.ts does NOT exist (deleted)
    - src/components/AppLayout.vue does NOT contain `CreatureDetailPanel`
    - src/components/AppLayout.vue does NOT contain `creatureDetail`
    - src/components/AppLayout.vue has only `AppSidebar` import
    - No file in src/ contains `useCreatureDetailStore` import
    - No file in src/ contains `import CreatureDetailPanel`
    - Full test suite passes with no failures
  </acceptance_criteria>
  <done>CreatureDetailPanel.vue and creatureDetail.ts store are deleted. AppLayout no longer renders the slide-over. No stale imports remain. Full test suite passes.</done>
</task>

</tasks>

<verification>
- `npx vitest run` — full test suite passes
- `test -f src/components/CreatureDetailPanel.vue && echo EXISTS || echo DELETED` — DELETED
- `test -f src/stores/creatureDetail.ts && echo EXISTS || echo DELETED` — DELETED
- `grep -r "useCreatureDetailStore" src/` — no matches
- `grep -r "import CreatureDetailPanel" src/` — no matches
- CompendiumView renders 2-column layout with inline StatBlock
</verification>

<success_criteria>
- CompendiumView has 2-column layout (30/70), session config header with party level/size, inline StatBlock in right column
- CreatureDetailPanel.vue and creatureDetail.ts are completely deleted from the codebase
- AppLayout is clean (no slide-over)
- All tests pass with no regressions across the entire test suite
</success_criteria>

<output>
After completion, create `.planning/phases/06-combat-detail-panel/06-04-SUMMARY.md`
</output>
