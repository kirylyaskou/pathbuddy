---
phase: 02-reference-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-reference-analysis/GAP-ANALYSIS.md
autonomous: true
requirements:
  - ANAL-01
  - ANAL-02

must_haves:
  truths:
    - "GAP-ANALYSIS.md exists and covers all 12 PF2e domain areas"
    - "Every domain section documents what refs/pf2e/ has, what engine currently has, and what is missing"
    - "Each gap entry is tagged with a priority tier (HIGH or LOWER) per D-06"
    - "A prioritized missing-mechanics master list exists summarizing all gaps across domains"
    - "The document is organized by domain area per D-04"
  artifacts:
    - path: ".planning/phases/02-reference-analysis/GAP-ANALYSIS.md"
      provides: "Complete gap analysis between refs/pf2e/ Foundry VTT data and engine/"
      contains: "## Executive Summary"
      min_lines: 300
  key_links:
    - from: ".planning/phases/02-reference-analysis/GAP-ANALYSIS.md"
      to: "engine/index.ts"
      via: "References engine public API to identify what exists"
      pattern: "engine.*currently has"
    - from: ".planning/phases/02-reference-analysis/GAP-ANALYSIS.md"
      to: "refs/pf2e/"
      via: "References Foundry VTT content packs analyzed"
      pattern: "refs/pf2e/"
---

<objective>
Produce the comprehensive PF2e engine gap-analysis document by reading the Foundry VTT PF2e source data (`refs/pf2e/`) and comparing it against the current `/engine` modules.

Purpose: This analysis drives the scope of Phases 3 (Conditions & Statuses) and 4 (Actions & Modifier Math). Without it, those phases have no defined scope.

Output: `.planning/phases/02-reference-analysis/GAP-ANALYSIS.md` — a single document organized by domain area covering all 12 PF2e mechanical domains, with a prioritized missing-mechanics summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-reference-analysis/02-CONTEXT.md
@.planning/phases/02-reference-analysis/02-RESEARCH.md

Source files for comparison:
@engine/index.ts
@engine/types.ts
@engine/conditions/conditions.ts
@engine/damage/damage.ts
@engine/damage/damage-helpers.ts
@engine/damage/iwr.ts
@engine/damage/iwr-utils.ts
@engine/modifiers/modifiers.ts
@engine/encounter/xp.ts
@engine/encounter/weak-elite.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze all 12 domains and produce GAP-ANALYSIS.md body</name>
  <files>.planning/phases/02-reference-analysis/GAP-ANALYSIS.md</files>
  <read_first>
    .planning/phases/02-reference-analysis/02-RESEARCH.md
    .planning/phases/02-reference-analysis/02-CONTEXT.md
    engine/index.ts
    engine/types.ts
    engine/conditions/conditions.ts
    engine/damage/damage.ts
    engine/damage/iwr.ts
    engine/damage/iwr-utils.ts
    engine/modifiers/modifiers.ts
    engine/encounter/xp.ts
    engine/encounter/weak-elite.ts
    engine/damage/damage-helpers.ts
  </read_first>
  <action>
Produce `.planning/phases/02-reference-analysis/GAP-ANALYSIS.md` covering all 12 domains.

The 02-RESEARCH.md file contains extensive pre-analysis with domain-level gap tables. Use it as the foundation, but VALIDATE findings by reading representative JSON samples from refs/pf2e/ directly.

**Document structure (per D-04 — organized by domain area):**

```
# PF2e Engine Gap Analysis
## Executive Summary (placeholder — Task 2 fills this in)
## Prioritized Missing Mechanics (placeholder — Task 2 fills this in)

## Domain 1: Conditions & Statuses [HIGH PRIORITY]
### What refs/pf2e/ has
### What engine currently has
### Gaps
### Key Schema Insights

## Domain 2: Damage & IWR [HIGH PRIORITY]
(same subsection pattern)

## Domain 3: Actions [HIGH PRIORITY]
## Domain 4: Modifier Math [HIGH PRIORITY]
## Domain 5: Creatures & Stat Blocks [HIGH PRIORITY]
## Domain 6: Spells [HIGH PRIORITY]
## Domain 7: Equipment & Weapons [HIGH PRIORITY]
## Domain 8: Hazards [HIGH PRIORITY]
## Domain 9: Feats [HIGH PRIORITY]
## Domain 10: Classes & Class Features [HIGH PRIORITY]
## Domain 11: Ancestries & Heritages [LOWER PRIORITY]
## Domain 12: Downtime Actions [LOWER PRIORITY]

## Open Questions
## Appendix: Content Pack Inventory
```

**For each domain, include:**
1. **What refs/pf2e/ has** — File counts, JSON schema (key fields), representative examples. Read at least 2-3 actual JSON files per domain to document real schemas.
2. **What engine currently has** — List the specific functions, types, and constants from engine/ modules that cover this domain. If engine has nothing, state "No engine coverage."
3. **Gaps** — Table format: `| Gap | Description | Priority |` — each row describes a specific missing capability. Priority is HIGH (combat-related per D-06) or LOWER (downtime per D-06). Within each tier, no further ranking (per D-07).
4. **Key Schema Insights** — Notable fields, patterns, or structures that future implementers need to understand.

**Specific refs/ files to read for validation (DO NOT skip these):**

Domain 1 — Conditions:
- Read ALL of `refs/pf2e/conditions/*.json` (43 files — they are small)
- Compare each condition's `system.group`, `system.overrides`, `system.value.isValued`, and `system.rules` against what `engine/conditions/conditions.ts` models
- Document the 5 group values: senses, abilities, death, detection, attitudes
- Document all override relationships (Blinded->Dazzled, Restrained->Grabbed, Stunned->Slowed, attitude mutual overrides)
- Document Rule Element types found on conditions (FlatModifier, GrantItem, etc.)

Domain 2 — Damage & IWR:
- Read `engine/damage/damage.ts` for DAMAGE_TYPES, DAMAGE_CATEGORIES, MATERIAL_EFFECTS
- Read `engine/damage/iwr.ts` for IMMUNITY_TYPES, WEAKNESS_TYPES, RESISTANCE_TYPES
- Compile the COMPLETE list of immunity, weakness, and resistance type strings used across ALL bestiary creature JSON files. Run a Node.js script or manually sample 20+ creatures from `refs/pf2e/pathfinder-monster-core/` to enumerate all IWR type strings.
- List every IWR type string present in refs/ but absent from engine's typed constants
- Note the holy/unholy vs vitality/void question (Remaster context — see Research open question 1)

Domain 3 — Actions:
- Read `refs/pf2e/actions/basic/*.json` (all 30 basic actions)
- Read 5-10 samples from `refs/pf2e/actions/skill/`
- Read 3-5 samples from `refs/pf2e/actions/class/`
- Document the action schema: `system.actionType.value`, `system.actions.value`, `system.category`, `system.rules[]`
- Note that engine has ZERO action definitions

Domain 4 — Modifier Math:
- Read `engine/modifiers/modifiers.ts` for MODIFIER_TYPES, Modifier class, applyStackingRules, StatisticModifier
- Read 3-5 creature stat blocks from `refs/pf2e/pathfinder-monster-core/` and document: `system.abilities`, `system.saves`, `system.perception`, `system.skills`, `system.attributes.ac`
- Document what modifier-related mechanics exist in refs/ (proficiency, ability modifiers, MAP) but are absent from engine

Domain 5 — Creatures:
- Read 5 creature JSON files from `refs/pf2e/pathfinder-monster-core/` — pick creatures of varying levels (e.g., level -1, 1, 5, 10, 20)
- Document the COMPLETE creature stat block schema: all `system.*` fields
- Document embedded items (melee attacks, ranged attacks, spellcasting entries, actions)
- Compare against `engine/types.ts` (only has WeakEliteTier)

Domain 6 — Spells:
- Read 5-10 spell JSON files from `refs/pf2e/spells/spells/` (mix of cantrips, low-rank, high-rank)
- Read 2-3 focus spells from `refs/pf2e/spells/focus/`
- Document spell schema: `system.area`, `system.damage`, `system.defense`, `system.duration`, `system.level`, `system.range`, `system.time`, `system.traits.traditions`
- Note engine has ZERO spell coverage

Domain 7 — Equipment:
- Read 5 weapon JSON files, 3 armor JSON files, 2 shield JSON files from `refs/pf2e/equipment/`
- Document weapon schema: `system.damage`, `system.group`, `system.runes`, `system.traits`
- Document armor schema: `system.acBonus`, `system.dexCap`, `system.checkPenalty`, `system.runes`
- Note engine's DamageFormula/BaseDamage types and MATERIAL_EFFECTS are partial coverage

Domain 8 — Hazards:
- Read 5 hazard JSON files from `refs/pf2e/hazards/`
- Document hazard schema: `system.attributes`, `system.details.isComplex`, `system.saves`, embedded actions
- Note engine's `getHazardXp()` and `HazardType` as existing partial coverage

Domain 9 — Feats:
- Read 10 feat JSON files with non-empty `system.rules` arrays from `refs/pf2e/feats/` (pick from class/ and skill/ subdirectories)
- Document feat schema and the Rule Element types used
- Note engine has ZERO feat coverage

Domain 10 — Classes:
- Read 3-5 class JSON files from `refs/pf2e/classes/`
- Read 5 class feature JSON files from `refs/pf2e/class-features/`
- Document class schema: `system.hp`, `system.keyAbility`, `system.perception`, `system.saves`, `system.spellcasting`
- Note engine has ZERO class coverage

Domain 11 — Ancestries (LOWER PRIORITY per D-06):
- Read 3 ancestry JSON files from `refs/pf2e/ancestries/`
- Read 3 heritage JSON files from `refs/pf2e/heritages/`
- Document schema briefly — summary level detail is sufficient

Domain 12 — Downtime (LOWER PRIORITY per D-06):
- Read all 4 downtime action JSON files from `refs/pf2e/actions/downtime/`
- Document briefly

**Appendix: Content Pack Inventory**
- List all 94 refs/pf2e/ directories with their entry counts
- This provides a complete inventory per D-02

**Open Questions section:**
- Include the 3 open questions from Research: holy/unholy remaster context, malevolence condition source, abilities group mutual-exclusion semantics
- Add any new questions discovered during analysis

**Per D-01:** Scope covers ALL mechanical systems, not just DM-facing.
**Per D-02:** Include both data schemas AND content inventory counts.
**Per D-03:** Not limited to DM-facing — complete PF2e rules as in Foundry VTT source.
**Per D-05:** Claude's discretion on detail level — recommendation is summary-level per Research.
**Per D-08:** refs/pf2e/ is primary source.
**Per D-09:** refs/lib/ is secondary — per Research conclusion, it contains extractor tooling only, not game mechanics. Skip unless a specific question arises.
**Per D-10:** Focus on JSON data, not TypeScript code in refs/lib/.

Write the COMPLETE document in a single file creation. Leave the "Executive Summary" and "Prioritized Missing Mechanics" sections as headers with `<!-- Task 2 -->` placeholder comments. Task 2 will fill these in after reviewing the full body.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-reference-analysis/GAP-ANALYSIS.md && echo "File exists" && node -e "const fs=require('fs'); const c=fs.readFileSync('.planning/phases/02-reference-analysis/GAP-ANALYSIS.md','utf8'); const domains=['Domain 1','Domain 2','Domain 3','Domain 4','Domain 5','Domain 6','Domain 7','Domain 8','Domain 9','Domain 10','Domain 11','Domain 12']; const missing=domains.filter(d=>!c.includes(d)); console.log('Missing domains:', missing.length ? missing.join(', ') : 'NONE'); console.log('Lines:', c.split('\n').length); console.log('Has gaps tables:', (c.match(/\| Gap \|/g)||[]).length >= 10 ? 'YES' : 'NO')"</automated>
  </verify>
  <acceptance_criteria>
    - GAP-ANALYSIS.md exists at .planning/phases/02-reference-analysis/GAP-ANALYSIS.md
    - File contains all 12 domain section headers: "## Domain 1" through "## Domain 12"
    - Each domain section contains subsections: "### What refs/pf2e/ has", "### What engine currently has", "### Gaps"
    - Gap tables use format "| Gap | Description | Priority |" with at least 10 gap tables across the document
    - Each gap entry is tagged HIGH or LOWER (per D-06 two-tier system)
    - File contains "## Open Questions" section
    - File contains "## Appendix: Content Pack Inventory" section
    - File is at least 300 lines long
    - File contains concrete JSON schema field names (e.g., "system.attributes", "system.saves", "system.abilities") from direct refs/ reading
    - File references specific engine functions/types by name (e.g., "ConditionManager", "applyIWR", "IMMUNITY_TYPES", "StatisticModifier")
  </acceptance_criteria>
  <done>GAP-ANALYSIS.md body covers all 12 domains with validated gap tables, JSON schemas from direct refs/ reading, and engine comparison. Executive Summary placeholder exists for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Write executive summary and prioritized missing-mechanics master list</name>
  <files>.planning/phases/02-reference-analysis/GAP-ANALYSIS.md</files>
  <read_first>
    .planning/phases/02-reference-analysis/GAP-ANALYSIS.md
    .planning/phases/02-reference-analysis/02-CONTEXT.md
    engine/index.ts
  </read_first>
  <action>
Read the GAP-ANALYSIS.md produced by Task 1. Replace the placeholder sections with:

**1. Executive Summary (replaces `<!-- Task 2 -->` under "## Executive Summary")**

Write a 10-20 line executive summary that answers:
- How many total PF2e mechanical domains were analyzed? (12)
- How many domains does the engine currently cover? (5 partial: conditions, damage/IWR, modifiers, encounter XP, weak/elite HP)
- How many domains have ZERO engine coverage? (7: actions, spells, feats, equipment/weapons, classes, ancestries, downtime)
- What is the overall gap severity? (critical — engine covers roughly 5 of 12 domains, and even covered domains have significant gaps)
- What is the highest-impact work for Phases 3 and 4? (Conditions overrides/groups/rule-elements, IWR type expansion, full action system, modifier-to-statistic wiring, creature type definition)

**2. Prioritized Missing Mechanics Master List (replaces `<!-- Task 2 -->` under "## Prioritized Missing Mechanics")**

This is the ANAL-02 deliverable. Create a single consolidated table that lists EVERY gap from all 12 domain sections, organized by priority tier:

```markdown
### HIGH Priority (Combat-Related)

| # | Domain | Gap | Description |
|---|--------|-----|-------------|
| 1 | Conditions | Missing groups | Engine has 2 of 5 condition groups |
| 2 | Conditions | Missing overrides | No Blinded->Dazzled, Restrained->Grabbed, Stunned->Slowed override logic |
| 3 | Conditions | Rule Elements | Conditions don't apply mechanical effects (FlatModifier, GrantItem) |
| ... | ... | ... | ... |

### LOWER Priority (Non-Combat / Downtime)

| # | Domain | Gap | Description |
|---|--------|-----|-------------|
| 1 | Ancestries | Entire ancestry system | 50 ancestries, 321 heritages with no engine representation |
| ... | ... | ... | ... |
```

Number every gap sequentially within its priority tier. This master list MUST include every gap entry from every domain section — do not summarize or skip any. Cross-reference by reading each "### Gaps" subsection and collecting all table rows.

**Per D-06:** Two-tier system only: HIGH (combat-related) and LOWER (downtime/non-combat).
**Per D-07:** No ranking within tiers — all equally important, implementation order decided in later phases.

After filling in both sections, verify the document is complete by checking:
- Executive Summary is present and filled (not a placeholder)
- Prioritized Missing Mechanics list contains all gaps from all 12 domains
- No placeholder comments remain in the document
- The document reads coherently top-to-bottom
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('.planning/phases/02-reference-analysis/GAP-ANALYSIS.md','utf8'); console.log('Has Executive Summary content:', c.includes('## Executive Summary') && !c.includes('<!-- Task 2 -->') ? 'YES' : 'NO'); console.log('Has Priority tables:', c.includes('### HIGH Priority') && c.includes('### LOWER Priority') ? 'YES' : 'NO'); console.log('No placeholders remain:', !c.includes('<!-- Task 2') ? 'YES' : 'NO'); console.log('Total lines:', c.split('\n').length); const highCount = (c.match(/^\| \d+/gm)||[]).length; console.log('Numbered gap entries:', highCount)"</automated>
  </verify>
  <acceptance_criteria>
    - GAP-ANALYSIS.md "## Executive Summary" section contains filled content (not placeholder)
    - GAP-ANALYSIS.md "## Prioritized Missing Mechanics" section contains two subsections: "### HIGH Priority" and "### LOWER Priority"
    - The HIGH Priority table contains numbered rows with columns: #, Domain, Gap, Description
    - The LOWER Priority table contains numbered rows with same column format
    - No `<!-- Task 2 -->` placeholder comments remain anywhere in the document
    - Every gap from the 12 domain "### Gaps" subsections appears in the master list (spot-check: conditions missing groups, IWR type expansion, entire spell system, entire feat system all appear)
    - Document total line count exceeds 400
    - Executive summary mentions: 12 domains analyzed, 5 partially covered, 7 with zero coverage
  </acceptance_criteria>
  <done>GAP-ANALYSIS.md is complete with executive summary (ANAL-01) and prioritized missing-mechanics master list (ANAL-02). Document is ready for consumption by Phase 3 and Phase 4 planners.</done>
</task>

</tasks>

<verification>
Phase-level verification checks:

1. GAP-ANALYSIS.md exists at `.planning/phases/02-reference-analysis/GAP-ANALYSIS.md`
2. Document contains all 12 domain sections (Domain 1 through Domain 12)
3. Executive Summary section is filled with quantitative coverage assessment
4. Prioritized Missing Mechanics section contains numbered master list of ALL gaps
5. Each domain section has: "What refs/pf2e/ has", "What engine currently has", "Gaps" subsections
6. Priority tiers follow D-06: HIGH = combat-related, LOWER = downtime/non-combat
7. No placeholder comments remain
8. Document references concrete JSON schema fields from refs/ reading
9. Document references concrete engine functions/types from engine/ reading
10. `npm run typecheck` still passes (engine code not modified, but verify no accidental changes)
</verification>

<success_criteria>
- ANAL-01 satisfied: GAP-ANALYSIS.md describes what refs/pf2e/ implements vs. what engine/ currently covers, organized by domain
- ANAL-02 satisfied: A numbered, prioritized list of ALL missing mechanics exists in the document
- Phases 3 and 4 planners can read this document and determine exactly what needs to be built
- No engine code was modified (this phase is analysis-only)
</success_criteria>

<output>
After completion, create `.planning/phases/02-reference-analysis/02-01-SUMMARY.md`
</output>
