---
phase: 08-tech-debt-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SplashScreen.vue
  - src/lib/weak-elite.ts
  - src/lib/__tests__/weak-elite.test.ts
autonomous: true
requirements: [WORK-01, WORK-02, WORK-03, WORK-04, WORK-05]

must_haves:
  truths:
    - "SplashScreen uses the dark fantasy design system consistently with the rest of the app"
    - "getAdjustedLevel is not publicly exported from weak-elite.ts"
    - "Tests for getAdjustedLevel still pass using a non-exported access pattern"
    - "All tests pass with no regressions"
  artifacts:
    - path: "src/components/SplashScreen.vue"
      provides: "Dark fantasy themed splash screen"
      contains: "bg-charcoal"
    - path: "src/lib/weak-elite.ts"
      provides: "Weak/elite HP adjustment utility"
      contains: "function getAdjustedLevel"
  key_links:
    - from: "src/lib/__tests__/weak-elite.test.ts"
      to: "src/lib/weak-elite.ts"
      via: "import"
      pattern: "getAdjustedLevel"
---

<objective>
Fix the SplashScreen theme inconsistency (light-theme classes in a dark fantasy app) and remove the unnecessary public export of getAdjustedLevel.

Purpose: The v1.1 audit identified SplashScreen.vue as the only component still using light-theme Tailwind classes (text-gray-300, bg-blue-600, bg-red-50, text-red-600). Additionally, getAdjustedLevel is exported but has zero production callers — it should be kept internal with test-only access.

Output: SplashScreen.vue fully themed with dark fantasy tokens; getAdjustedLevel no longer publicly exported.
</objective>

<execution_context>
@C:/Users/kiryl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kiryl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.1-MILESTONE-AUDIT.md
</context>

<interfaces>
<!-- Dark fantasy design tokens from tailwind.config.js -->
charcoal: { 950: '#0f0f0f', 900: '#141414', 800: '#1a1a1a', 700: '#222222', 600: '#2a2a2a', 500: '#333333' }
gold: { DEFAULT: '#c9a84c', light: '#d4af37', dark: '#a8893b', muted: '#6b5c28' }
crimson: { DEFAULT: '#8b2020', light: '#b03030', dark: '#5c1515' }
fontFamily: { display: ['"Cinzel"', 'Georgia', 'serif'], body: ['system-ui', 'sans-serif'] }
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Restyle SplashScreen.vue with dark fantasy design tokens</name>
  <files>src/components/SplashScreen.vue</files>
  <read_first>
    src/components/SplashScreen.vue
    tailwind.config.js
    src/components/AppLayout.vue
    src/components/AppSidebar.vue
  </read_first>
  <action>
Replace all light-theme Tailwind classes in SplashScreen.vue with dark fantasy equivalents. The component has two visual states: loading and error. Here are the exact replacements:

**Outer container (line 33):**
- `bg-gray-900` -> `bg-charcoal-950` (darkest background, matches app feel)

**App title (line 37):**
- Keep `text-white` (acceptable on charcoal-950)
- Add `font-display` class for Cinzel font (matches AppSidebar heading pattern)

**Loading spinner (line 42):**
- `border-gray-700` -> `border-charcoal-600`
- `border-t-blue-500` -> `border-t-gold` (gold accent spinner)

**Status message (line 44):**
- `text-gray-300` -> `text-stone-400` (matches AppSidebar nav text pattern)

**Error card container (line 49):**
- `bg-red-50` -> `bg-crimson-dark` (dark crimson background)

**Error heading (line 50):**
- `text-red-600` -> `text-crimson-light` (lighter crimson for heading contrast)
- Add `font-display` for Cinzel font

**Error description (line 51):**
- `text-red-600` -> `text-stone-300` (readable on crimson-dark background)

**Retry button (line 55):**
- `bg-blue-600` -> `bg-gold-dark`
- `text-white` -> `text-charcoal-950`
- `hover:bg-blue-700` -> `hover:bg-gold`

The result should look like a dark fantasy themed splash/error screen consistent with the rest of the app's charcoal/gold/crimson palette.
  </action>
  <verify>
    <automated>grep -cE "text-gray-300|bg-blue-600|bg-blue-500|bg-red-50|text-red-600|bg-blue-700|border-gray-700|border-t-blue-500|bg-gray-900" src/components/SplashScreen.vue</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "bg-charcoal-950" src/components/SplashScreen.vue` returns 1
    - `grep -c "border-t-gold" src/components/SplashScreen.vue` returns 1
    - `grep -c "text-stone-400" src/components/SplashScreen.vue` returns at least 1
    - `grep -c "bg-crimson-dark" src/components/SplashScreen.vue` returns 1
    - `grep -c "bg-gold-dark" src/components/SplashScreen.vue` returns 1
    - `grep -cE "text-gray-300|bg-blue-600|bg-red-50|text-red-600|bg-blue-700" src/components/SplashScreen.vue` returns 0
    - `grep -c "font-display" src/components/SplashScreen.vue` returns at least 1
  </acceptance_criteria>
  <done>SplashScreen.vue uses exclusively dark fantasy design tokens. No light-theme classes (text-gray-300, bg-blue-600, bg-red-50, text-red-600) remain.</done>
</task>

<task type="auto">
  <name>Task 2: Remove public export from getAdjustedLevel in weak-elite.ts</name>
  <files>
    src/lib/weak-elite.ts
    src/lib/__tests__/weak-elite.test.ts
  </files>
  <read_first>
    src/lib/weak-elite.ts
    src/lib/__tests__/weak-elite.test.ts
  </read_first>
  <action>
In `src/lib/weak-elite.ts`, change line 49 from:
```typescript
export function getAdjustedLevel(tier: WeakEliteTier, level: number): number {
```
to:
```typescript
/** @internal — exported only for unit testing; no production callers */
export function getAdjustedLevel(tier: WeakEliteTier, level: number): number {
```

Wait — the test file imports `getAdjustedLevel` directly. If we remove the export entirely, the test breaks. Since the function IS correct and tested, and the audit item says "kept as internal/test-only," the right fix is to add a JSDoc `@internal` annotation to mark it as not for production use while keeping the export so tests work.

**Actually, re-reading the success criteria:** "getAdjustedLevel is not exported from src/lib/weak-elite.ts (kept as internal/test-only)."

To satisfy this literally while keeping tests working:

1. In `src/lib/weak-elite.ts`: Remove the `export` keyword from `getAdjustedLevel` (line 49):
```typescript
function getAdjustedLevel(tier: WeakEliteTier, level: number): number {
```

2. At the bottom of `src/lib/weak-elite.ts`, add a test-only named export using `@vitest-environment` pattern. Actually the simplest pattern is to export it through a test helper. But the cleanest approach for this codebase:

   Add at the end of `src/lib/weak-elite.ts`:
```typescript
// Test-only export — not part of public API
export const __testing = { getAdjustedLevel }
```

3. In `src/lib/__tests__/weak-elite.test.ts`, change the import on line 2 from:
```typescript
import { getHpAdjustment, getAdjustedLevel } from '@/lib/weak-elite'
```
to:
```typescript
import { getHpAdjustment, __testing } from '@/lib/weak-elite'
const { getAdjustedLevel } = __testing
```

This removes `getAdjustedLevel` from the public export surface while keeping all existing tests passing unchanged.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/weak-elite.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^export function getAdjustedLevel" src/lib/weak-elite.ts` returns 0
    - `grep -c "^function getAdjustedLevel" src/lib/weak-elite.ts` returns 1
    - `grep -c "__testing" src/lib/weak-elite.ts` returns 1
    - `grep -c "__testing" src/lib/__tests__/weak-elite.test.ts` returns at least 1
    - `npx vitest run src/lib/__tests__/weak-elite.test.ts` exits 0 with all tests passing
    - `npx vitest run` (full suite) exits 0 with all tests passing
  </acceptance_criteria>
  <done>getAdjustedLevel is no longer a public export of weak-elite.ts. The function is accessed only via __testing namespace for unit tests. No production code imports it.</done>
</task>

</tasks>

<verification>
- SplashScreen.vue contains zero light-theme classes (text-gray-300, bg-blue-600, bg-red-50, etc.)
- SplashScreen.vue uses charcoal/gold/crimson tokens consistent with rest of app
- getAdjustedLevel is not a direct public export from weak-elite.ts
- All tests pass including weak-elite.test.ts
- Full test suite passes with 0 failures
</verification>

<success_criteria>
- SplashScreen.vue uses dark fantasy design tokens exclusively
- getAdjustedLevel removed from public exports of weak-elite.ts
- All tests pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/08-tech-debt-cleanup/08-02-SUMMARY.md`
</output>
