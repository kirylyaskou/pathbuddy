---
phase: 05-vite-scaffold-nextjs-teardown
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vite.config.ts
  - index.html
  - src/main.tsx
  - src/app/styles/globals.css
  - src/shared/lib/cn.ts
  - src/shared/lib/utils.ts
  - src/vite-env.d.ts
  - src-tauri/tauri.conf.json
  - src-tauri/Cargo.toml
  - src-tauri/src/lib.rs
  - src-tauri/capabilities/default.json
autonomous: true
requirements:
  - FND-01
  - FND-03

must_haves:
  truths:
    - "npm run tauri dev starts without errors and opens a window"
    - "Vite dev server serves the index.html with React mounting"
    - "All 13 PF2e OKLCH tokens generate Tailwind utility classes (bg-pf-gold, text-pf-threat-extreme, etc.)"
    - "Dark mode is the default theme and .dark class is applied"
    - "@engine alias resolves correctly in both TypeScript and Vite"
  artifacts:
    - path: "package.json"
      provides: "Frontend project manifest with all dependencies"
      contains: "pathbuddy"
    - path: "vite.config.ts"
      provides: "Tauri-compatible Vite configuration"
      contains: "@vitejs/plugin-react"
    - path: "tsconfig.json"
      provides: "TypeScript config covering src/ and engine/"
      contains: "src/**/*.tsx"
    - path: "index.html"
      provides: "SPA entry point"
      contains: "root"
    - path: "src/main.tsx"
      provides: "React entry point"
      contains: "createRoot"
    - path: "src/app/styles/globals.css"
      provides: "Full OKLCH design token system"
      contains: "@theme inline"
    - path: "src-tauri/tauri.conf.json"
      provides: "Tauri 2 config with Vite dev server integration"
      contains: "beforeDevCommand"
  key_links:
    - from: "index.html"
      to: "src/main.tsx"
      via: "script tag type=module"
      pattern: "src/main.tsx"
    - from: "vite.config.ts"
      to: "tsconfig.json"
      via: "vite-tsconfig-paths plugin"
      pattern: "tsconfigPaths"
    - from: "src/app/styles/globals.css"
      to: "src/main.tsx"
      via: "CSS import"
      pattern: "./app/styles/globals.css"
---

<objective>
Scaffold the Tauri 2 + Vite 6 + React 19 project infrastructure and install all dependencies. Create the complete PF2e OKLCH design token system.

Purpose: This is the foundational plan. Nothing else can run until the project builds and Vite serves a page. The design system must be wired here because shadcn/ui components in Plan 02 depend on the CSS variables and Tailwind utilities being available.

Output: A buildable project that starts with `npm run tauri dev`, renders a blank React page, and has the full OKLCH token palette generating Tailwind utility classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-vite-scaffold-nextjs-teardown/05-CONTEXT.md
@.planning/phases/05-vite-scaffold-nextjs-teardown/05-RESEARCH.md
@.planning/phases/05-vite-scaffold-nextjs-teardown/05-UI-SPEC.md

<interfaces>
<!-- No prior plans to reference — this is the first plan of v0.3.0 -->
<!-- Engine barrel export must remain importable after restructure: -->
From engine/index.ts:
```typescript
// Barrel export — single public API for all PF2e engine logic.
// Consumers import from '@engine' or './engine'.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Tauri 2 + Vite 6 project and install all dependencies</name>
  <read_first>
    - D:/pathbuddy/package.json (current engine-only package — will be replaced)
    - D:/pathbuddy/tsconfig.json (current engine-only tsconfig — will be replaced)
    - D:/pathbuddy/vite.config.ts (current minimal config — will be replaced)
    - D:/parse_data/package.json (prototype — reference for exact Radix package versions)
    - D:/pathbuddy/engine/index.ts (engine barrel — must remain importable)
  </read_first>
  <files>
    package.json,
    tsconfig.json,
    vite.config.ts,
    index.html,
    src/main.tsx,
    src/vite-env.d.ts,
    src/app/providers.tsx,
    src-tauri/tauri.conf.json,
    src-tauri/Cargo.toml,
    src-tauri/src/lib.rs,
    src-tauri/capabilities/default.json
  </files>
  <action>
**Step 1: Create `src-tauri/` Tauri 2 backend files.**

The `src-tauri/` directory exists but is missing `tauri.conf.json`, `Cargo.toml`, and `src/lib.rs`. These must be created from the Tauri 2 template.

Create `src-tauri/Cargo.toml`:
```toml
[package]
name = "pathbuddy"
version = "0.3.0"
description = "Pathfinder 2e DM Assistant"
authors = ["you"]
edition = "2021"

[lib]
name = "pathbuddy_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
```

Create `src-tauri/src/lib.rs`:
```rust
#[cfg_attr(mobile, tauri::mobile_entry_point)]
pub fn run() {
    tauri::Builder::default()
        .plugin(tauri_plugin_opener::init())
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```

Create `src-tauri/src/main.rs`:
```rust
#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]

fn main() {
    pathbuddy_lib::run()
}
```

Create `src-tauri/tauri.conf.json`:
```json
{
  "productName": "PathBuddy",
  "version": "0.3.0",
  "identifier": "com.pathbuddy.app",
  "build": {
    "beforeDevCommand": "npm run dev:vite",
    "devUrl": "http://localhost:5173",
    "beforeBuildCommand": "npm run build:vite",
    "frontendDist": "../dist"
  },
  "app": {
    "title": "PathBuddy",
    "windows": [
      {
        "title": "PathBuddy",
        "width": 1280,
        "height": 800,
        "resizable": true,
        "fullscreen": false
      }
    ],
    "security": {
      "csp": null
    }
  },
  "bundle": {
    "active": true,
    "targets": "all",
    "icon": [
      "icons/32x32.png",
      "icons/128x128.png",
      "icons/128x128@2x.png",
      "icons/icon.icns",
      "icons/icon.ico"
    ]
  }
}
```

Create `src-tauri/capabilities/default.json`:
```json
{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
```

Also create `src-tauri/build.rs`:
```rust
fn main() {
    tauri_build::build()
}
```

**Step 2: Replace root `package.json`.**

Replace the entire file with:
```json
{
  "name": "pathbuddy",
  "version": "0.3.0-pre-alpha",
  "description": "Pathfinder 2e DM Assistant",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "dev:vite": "vite",
    "build": "tauri build",
    "build:vite": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/ && steiger src/"
  },
  "dependencies": {
    "react": "19.2.4",
    "react-dom": "19.2.4",
    "react-router-dom": "^7.13.2",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "tailwind-merge": "^3.3.1",
    "lucide-react": "^0.564.0",
    "next-themes": "^0.4.6",
    "sonner": "^1.7.1",
    "cmdk": "1.1.1",
    "vaul": "^1.1.2",
    "react-resizable-panels": "^2.1.7",
    "@tauri-apps/api": "^2.10.1",
    "@radix-ui/react-accordion": "1.2.12",
    "@radix-ui/react-alert-dialog": "1.1.15",
    "@radix-ui/react-aspect-ratio": "1.1.8",
    "@radix-ui/react-avatar": "1.1.11",
    "@radix-ui/react-checkbox": "1.3.3",
    "@radix-ui/react-collapsible": "1.1.12",
    "@radix-ui/react-context-menu": "2.2.16",
    "@radix-ui/react-dialog": "1.1.15",
    "@radix-ui/react-dropdown-menu": "2.1.16",
    "@radix-ui/react-hover-card": "1.1.15",
    "@radix-ui/react-label": "2.1.8",
    "@radix-ui/react-menubar": "1.1.16",
    "@radix-ui/react-navigation-menu": "1.2.14",
    "@radix-ui/react-popover": "1.1.15",
    "@radix-ui/react-progress": "1.1.8",
    "@radix-ui/react-radio-group": "1.3.8",
    "@radix-ui/react-scroll-area": "1.2.10",
    "@radix-ui/react-select": "2.2.6",
    "@radix-ui/react-separator": "1.1.8",
    "@radix-ui/react-slider": "1.3.6",
    "@radix-ui/react-slot": "1.2.4",
    "@radix-ui/react-switch": "1.2.6",
    "@radix-ui/react-tabs": "1.1.13",
    "@radix-ui/react-toast": "1.2.15",
    "@radix-ui/react-toggle": "1.1.10",
    "@radix-ui/react-toggle-group": "1.1.11",
    "@radix-ui/react-tooltip": "1.2.8",
    "date-fns": "4.1.0",
    "embla-carousel-react": "8.6.0",
    "input-otp": "1.4.2",
    "react-day-picker": "9.13.2",
    "react-hook-form": "^7.54.1",
    "@hookform/resolvers": "^3.9.1",
    "recharts": "2.15.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "vite": "^6.3.0",
    "@vitejs/plugin-react": "^6.0.1",
    "tailwindcss": "^4.2.2",
    "@tailwindcss/vite": "^4.2.2",
    "tw-animate-css": "1.3.3",
    "vite-tsconfig-paths": "^6.1.1",
    "steiger": "^0.5.11",
    "@feature-sliced/steiger-plugin": "^0.5.7",
    "eslint": "^10.1.0",
    "eslint-plugin-boundaries": "^6.0.2",
    "eslint-plugin-import": "^2.31.0",
    "eslint-import-resolver-typescript": "^3.7.0",
    "@tauri-apps/cli": "^2",
    "@types/react": "19.2.14",
    "@types/react-dom": "19.2.3",
    "@types/node": "^22",
    "typescript": "5.7.3"
  }
}
```

**Step 3: Run `npm install`.**

Run `npm install`. If Radix peer dep conflicts with React 19 arise, run `npm install --legacy-peer-deps` instead. Document which approach was needed.

**Step 4: Replace `tsconfig.json`.**

Replace entirely:
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"],
      "@engine": ["engine/index.ts"],
      "@engine/*": ["engine/*"]
    }
  },
  "include": ["src/**/*.ts", "src/**/*.tsx", "engine/**/*.ts"]
}
```

Key changes from current: added `DOM`/`DOM.Iterable` to lib, added `jsx: "react-jsx"`, added `"@/*": ["src/*"]` alias, expanded `include` to cover `src/**/*.tsx`.

**Step 5: Replace `vite.config.ts`.**

Replace entirely:
```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'
import tsconfigPaths from 'vite-tsconfig-paths'

export default defineConfig({
  plugins: [
    react(),
    tailwindcss(),
    tsconfigPaths(),
  ],
  clearScreen: false,
  server: {
    strictPort: true,
    host: process.env.TAURI_DEV_HOST || false,
    port: 5173,
  },
  envPrefix: ['VITE_', 'TAURI_ENV_'],
  build: {
    target: process.env.TAURI_ENV_PLATFORM === 'windows' ? 'chrome105' : 'safari13',
    minify: !process.env.TAURI_ENV_DEBUG ? 'esbuild' : false,
    sourcemap: !!process.env.TAURI_ENV_DEBUG,
  },
})
```

Per D-11: uses `@vitejs/plugin-react` (NOT swc). Per D-14: uses `vite-tsconfig-paths` for alias resolution. Per D-13: uses `@tailwindcss/vite` plugin (NOT postcss).

**Step 6: Create `index.html` at project root.**

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>PathBuddy</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

**Step 7: Create `src/vite-env.d.ts`.**

```typescript
/// <reference types="vite/client" />
```

**Step 8: Create minimal `src/main.tsx`.**

```tsx
import React from 'react'
import ReactDOM from 'react-dom/client'
import { AppProviders } from './app/providers'
import './app/styles/globals.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AppProviders>
      <div className="flex items-center justify-center h-screen bg-background text-foreground">
        <h1 className="text-xl font-semibold">PathBuddy</h1>
      </div>
    </AppProviders>
  </React.StrictMode>
)
```

Note: `AppRouter` is not wired yet (Plan 02). This renders a placeholder.

**Step 9: Create `src/app/providers.tsx`.**

```tsx
import { ThemeProvider } from 'next-themes'
import { Toaster } from '@/shared/ui/sonner'

export function AppProviders({ children }: { children: React.ReactNode }) {
  return (
    <ThemeProvider attribute="class" defaultTheme="dark" disableTransitionOnChange>
      {children}
      <Toaster />
    </ThemeProvider>
  )
}
```

Per D-06: uses `next-themes` for dark/light mode (works without Next.js). Per D-07: `defaultTheme="dark"`.

NOTE: The `<Toaster />` import from `@/shared/ui/sonner` will not resolve until Plan 02 copies shadcn components. For this task, temporarily comment out or use a conditional import. Alternatively, create providers.tsx without the Toaster import initially — just `ThemeProvider` wrapping `{children}`. The Toaster line will be added in Plan 02 Task 1 after components are copied.

Simpler approach: Create providers.tsx with ONLY ThemeProvider for now:
```tsx
import { ThemeProvider } from 'next-themes'

export function AppProviders({ children }: { children: React.ReactNode }) {
  return (
    <ThemeProvider attribute="class" defaultTheme="dark" disableTransitionOnChange>
      {children}
    </ThemeProvider>
  )
}
```

**Step 10: Verify `npm run dev:vite` starts without errors.**

Run `npm run dev:vite` and confirm the Vite dev server starts on port 5173. The page should render "PathBuddy" text on a dark background.
  </action>
  <verify>
    <automated>cd D:/pathbuddy && npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"name": "pathbuddy"`
    - package.json contains `"react": "19.2.4"`
    - package.json contains `"@vitejs/plugin-react"`
    - package.json contains `"@tailwindcss/vite"`
    - package.json contains `"vite-tsconfig-paths"`
    - tsconfig.json contains `"@/*": ["src/*"]`
    - tsconfig.json contains `"@engine": ["engine/index.ts"]`
    - tsconfig.json contains `"src/**/*.tsx"` in include
    - vite.config.ts contains `import react from '@vitejs/plugin-react'`
    - vite.config.ts contains `import tailwindcss from '@tailwindcss/vite'`
    - vite.config.ts contains `import tsconfigPaths from 'vite-tsconfig-paths'`
    - vite.config.ts contains `clearScreen: false`
    - index.html contains `<div id="root"></div>`
    - index.html contains `src/main.tsx`
    - src/main.tsx contains `createRoot`
    - src-tauri/tauri.conf.json contains `"beforeDevCommand": "npm run dev:vite"`
    - src-tauri/tauri.conf.json contains `"devUrl": "http://localhost:5173"`
    - src-tauri/Cargo.toml contains `[package]` and `name = "pathbuddy"`
    - src-tauri/src/lib.rs contains `tauri::Builder::default()`
    - node_modules directory exists (npm install succeeded)
    - `npm run typecheck` exits with code 0
  </acceptance_criteria>
  <done>
    Project scaffolded: package.json rewritten with all frontend deps, tsconfig covers src/ + engine/, vite.config.ts has react+tailwind+tsconfigPaths plugins, Tauri 2 backend configured, npm install succeeded, `npm run typecheck` passes, Vite dev server starts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create the complete PF2e OKLCH design token system</name>
  <read_first>
    - D:/parse_data/app/globals.css (prototype globals.css — THE authoritative source, copy verbatim)
    - D:/pathbuddy/.planning/phases/05-vite-scaffold-nextjs-teardown/05-UI-SPEC.md (design token contract)
  </read_first>
  <files>
    src/app/styles/globals.css,
    src/shared/lib/cn.ts,
    src/shared/lib/utils.ts
  </files>
  <action>
**Step 1: Create `src/app/styles/globals.css`.**

Copy the ENTIRE contents of `D:/parse_data/app/globals.css` verbatim into `src/app/styles/globals.css`. This file contains:

1. `@import 'tailwindcss';` and `@import 'tw-animate-css';` at top
2. `@custom-variant dark (&:is(.dark *));` — MANDATORY for dark mode utilities
3. `:root { ... }` block with all light-mode OKLCH token values (background, foreground, card, primary, secondary, muted, accent, destructive, border, input, ring, chart-1..5, sidebar tokens, 13 PF2e tokens)
4. `.dark { ... }` block with all dark-mode OKLCH token values
5. `@theme inline { ... }` block mapping ALL CSS variables to `--color-*` Tailwind tokens, plus `--font-sans` and `--font-mono`, plus `--radius-*` tokens
6. `@layer base { ... }` with border-border reset and body bg/text
7. `@layer utilities { ... }` with golden-glow, golden-border, text-gold, bg-gold, metallic-gold, parchment-texture, card-grimdark, stat-block-header

Per D-13 and UI-SPEC: Copy verbatim. Do NOT reconstruct manually. Do NOT add `tailwind.config.js` or `postcss.config.mjs`.

Add a Google Fonts CSS import as the FIRST line (before `@import 'tailwindcss'`). Per D-01:
```css
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap');
```

**Step 2: Create `src/shared/lib/cn.ts`.**

```typescript
import { clsx, type ClassValue } from 'clsx'
import { twMerge } from 'tailwind-merge'

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```

**Step 3: Create `src/shared/lib/utils.ts`.**

This is an alias file for shadcn compatibility — shadcn components import from `@/lib/utils` which maps to `@/shared/lib/utils`:

```typescript
export { cn } from './cn'
```

Both files ensure shadcn components can import `cn` via either `@/shared/lib/cn` or `@/shared/lib/utils`.
  </action>
  <verify>
    <automated>cd D:/pathbuddy && grep -c "@theme inline" src/app/styles/globals.css && grep -c "pf-gold" src/app/styles/globals.css && grep -c "@custom-variant dark" src/app/styles/globals.css && grep -c "golden-glow" src/app/styles/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - src/app/styles/globals.css contains `@import 'tailwindcss';`
    - src/app/styles/globals.css contains `@import 'tw-animate-css';`
    - src/app/styles/globals.css contains `@custom-variant dark (&:is(.dark *));`
    - src/app/styles/globals.css contains `@theme inline {`
    - src/app/styles/globals.css contains `--color-pf-gold: var(--pf-gold);`
    - src/app/styles/globals.css contains `--color-pf-threat-extreme: var(--pf-threat-extreme);`
    - src/app/styles/globals.css contains `--color-pf-rarity-unique: var(--pf-rarity-unique);`
    - src/app/styles/globals.css contains `--pf-gold: oklch(0.75 0.18 75);` (dark mode value)
    - src/app/styles/globals.css contains `--font-sans: 'Inter'`
    - src/app/styles/globals.css contains `.golden-glow`
    - src/app/styles/globals.css contains `.stat-block-header`
    - src/app/styles/globals.css contains `@layer base`
    - src/app/styles/globals.css contains `fonts.googleapis.com`
    - src/shared/lib/cn.ts contains `export function cn(`
    - src/shared/lib/cn.ts contains `twMerge(clsx(inputs))`
    - src/shared/lib/utils.ts contains `export { cn } from './cn'`
    - No `tailwind.config.js` or `tailwind.config.ts` file exists at project root
    - No `postcss.config.mjs` or `postcss.config.js` file exists at project root
  </acceptance_criteria>
  <done>
    Complete OKLCH design system ported from prototype — all 13 PF2e tokens, all shadcn semantic tokens, sidebar tokens, custom utilities (golden-glow, parchment-texture, etc.), and @theme inline mapping all generating Tailwind utility classes. cn() utility available at shared/lib/cn.ts and shared/lib/utils.ts.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `npm run typecheck` passes (zero errors)
2. `npm run dev:vite` starts Vite on port 5173 without errors
3. `grep -c "@theme inline" src/app/styles/globals.css` returns 1
4. `grep -c "pf-gold" src/app/styles/globals.css` returns at least 4 (light + dark + @theme inline mapping + utility)
5. No `tailwind.config.js`, `postcss.config.mjs`, or `postcss.config.js` exist
6. `src-tauri/tauri.conf.json` exists with `devUrl` pointing to localhost:5173
</verification>

<success_criteria>
- Vite dev server starts without errors on `npm run dev:vite`
- TypeScript compiles without errors
- Full OKLCH token set in globals.css with @theme inline generating all utility classes
- Tauri 2 backend configured to invoke Vite dev server
- @engine alias resolves in both TS and Vite
- No Next.js, postcss, or tailwind config files exist
</success_criteria>

<output>
After completion, create `.planning/phases/05-vite-scaffold-nextjs-teardown/05-01-SUMMARY.md`
</output>
