---
phase: 06-combat-detail-panel
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/components/CombatTracker.vue
  - src/components/CombatDetailPanel.vue
  - src/views/CombatView.vue
  - src/components/__tests__/CombatTracker.test.ts
  - src/components/__tests__/CombatDetailPanel.test.ts
  - src/views/__tests__/CombatView.test.ts
autonomous: true
requirements: [WORK-06]
must_haves:
  truths:
    - "Clicking a combat tracker row emits 'select' with creatureId — does NOT open slide-over"
    - "CombatView sets selectedCombatantId on 'select' from CombatTracker"
    - "CombatDetailPanel renders LiveCombatOverlay with HP, conditions, initiative, tier badge when creatureId is set"
    - "CombatDetailPanel renders StatBlock below the overlay with rawData fetched from DB by creature.sourceId"
    - "CombatDetailPanel renders empty state when no creature is selected"
    - "Selected row in CombatTracker has border-l-4 border-gold + bg-charcoal-700 highlight"
    - "Creatures without sourceId show 'No compendium entry found' in place of stat block"
  artifacts:
    - path: "src/components/CombatDetailPanel.vue"
      provides: "Right-column panel with LiveCombatOverlay + StatBlock"
      min_lines: 80
    - path: "src/components/CombatTracker.vue"
      provides: "Rerouted row click to emit 'select' instead of detailStore.openCreature"
    - path: "src/views/CombatView.vue"
      provides: "selectedCombatantId ref wired to CombatTracker select + CombatDetailPanel"
  key_links:
    - from: "src/components/CombatTracker.vue"
      to: "src/views/CombatView.vue"
      via: "emit('select', creatureId)"
      pattern: "emit.*select.*creature"
    - from: "src/views/CombatView.vue"
      to: "src/components/CombatDetailPanel.vue"
      via: ":creature-id prop binding"
      pattern: "creature-id.*selectedCombatantId"
    - from: "src/components/CombatDetailPanel.vue"
      to: "src/stores/combat.ts"
      via: "useCombatStore().creatures.find"
      pattern: "combatStore.*creatures.*find"
    - from: "src/components/CombatDetailPanel.vue"
      to: "src/components/StatBlock.vue"
      via: "<StatBlock :raw-data>"
      pattern: "StatBlock.*raw-data"
---

<objective>
Wire the Combat Detail Panel: reroute CombatTracker row clicks to emit 'select', create CombatDetailPanel.vue with LiveCombatOverlay + StatBlock, and connect everything through CombatView's selectedCombatantId ref.

Purpose: This is the core WORK-06 delivery — clicking a combat tracker row populates the right panel with live state and stat block without opening a slide-over.

Output: Updated `CombatTracker.vue`, new `CombatDetailPanel.vue`, updated `CombatView.vue`, and test files.
</objective>

<execution_context>
@C:/Users/kiryl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kiryl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-combat-detail-panel/06-CONTEXT.md
@.planning/phases/06-combat-detail-panel/06-RESEARCH.md
@.planning/phases/06-combat-detail-panel/06-UI-SPEC.md
@.planning/phases/06-combat-detail-panel/06-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/types/combat.ts:
```typescript
export interface Creature {
  id: string; name: string; maxHP: number; currentHP: number; ac: number;
  initiative: number; dexMod: number; isCurrentTurn: boolean; isDowned: boolean;
  conditions: Condition[]; conditionDurations?: Record<Condition, number>;
  conditionValues?: Partial<Record<Condition, number>>;
  regenAmount?: number; ongoingDamage?: number; sourceId?: string; tier?: WeakEliteTier;
}
```

From src/stores/combat.ts:
```typescript
// Available store state and computed
creatures: ref<Creature[]>
regenerationDisabled: ref<Record<string, boolean>>
sortedCreatures: computed
```

From src/components/StatBlock.vue (created in Plan 01):
```typescript
defineProps<{ rawData: object | null; isLoading?: boolean }>()
```

From src/lib/schema.ts:
```typescript
export const pf2eEntities  // Drizzle table with sourceId column
```

CombatTracker current handleOpenDetail (to be rerouted):
```typescript
const handleOpenDetail = async (creatureId: string) => {
  const creature = combatStore.creatures.find(c => c.id === creatureId)
  if (!creature || !creature.sourceId) return
  const results = await db.select().from(pf2eEntities).where(eq(pf2eEntities.sourceId, creature.sourceId))
  if (results.length === 0) return
  const rawData = JSON.parse(results[0].rawData)
  detailStore.openCreature(rawData, creature.name)
}
```

CombatView current structure:
```vue
<div class="grid grid-cols-3 h-full overflow-hidden divide-x divide-charcoal-600">
  <!-- Left: CreatureBrowser + CombatAddBar -->
  <!-- Center: CombatTracker -->
  <!-- Right: placeholder div (Phase 06 target) -->
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reroute CombatTracker row click to emit 'select' + add selectedId prop</name>
  <files>src/components/CombatTracker.vue, src/components/__tests__/CombatTracker.test.ts</files>
  <read_first>
    - src/components/CombatTracker.vue
    - src/components/__tests__/CombatTracker.test.ts
    - src/stores/creatureDetail.ts
    - .planning/phases/06-combat-detail-panel/06-CONTEXT.md
  </read_first>
  <action>
    **Modify `src/components/CombatTracker.vue`:**

    **Import changes:**
    - REMOVE `import { useCreatureDetailStore } from '@/stores/creatureDetail'`
    - REMOVE `import { db } from '@/lib/database'`
    - REMOVE `import { pf2eEntities } from '@/lib/schema'`
    - REMOVE `import { eq } from 'drizzle-orm'`
    - REMOVE `const detailStore = useCreatureDetailStore()` line

    **Add props:**
    ```typescript
    const props = defineProps<{
      selectedId?: string | null
    }>()
    ```

    **Add emits:**
    ```typescript
    const emit = defineEmits<{
      'select': [creatureId: string]
    }>()
    ```

    **Replace `handleOpenDetail`:**
    Old:
    ```typescript
    const handleOpenDetail = async (creatureId: string) => {
      const creature = combatStore.creatures.find(c => c.id === creatureId)
      if (!creature || !creature.sourceId) return
      const results = await db.select().from(pf2eEntities).where(eq(pf2eEntities.sourceId, creature.sourceId))
      if (results.length === 0) return
      const rawData = JSON.parse(results[0].rawData)
      detailStore.openCreature(rawData, creature.name)
    }
    ```
    New:
    ```typescript
    const handleOpenDetail = (creatureId: string) => {
      emit('select', creatureId)
    }
    ```
    Note: The function is no longer async — it just emits. The DB lookup moves to CombatDetailPanel.vue.

    **Make the entire creature row clickable (not just the View Stat Block button):**
    Wrap each creature card's outer div with `@click="handleOpenDetail(creature.id)"` and `cursor-pointer`.

    **Add selected row highlight:**
    On the v-for wrapper div for each creature in `sortedCreatures`, update the `:class` binding:
    ```html
    <div
      v-for="(creature, index) in combatStore.sortedCreatures"
      :key="creature.id"
      @click="handleOpenDetail(creature.id)"
      :class="[
        'cursor-pointer',
        creature.id === props.selectedId
          ? 'border-l-4 border-gold bg-charcoal-700'
          : 'border-l-4 border-transparent',
        { 'ring-1 ring-gold ring-offset-1 ring-offset-charcoal-800 rounded-lg': dragOverCreatureId === creature.id }
      ]"
      @dragover.prevent="handleDragOver"
      @dragenter.prevent="handleDragEnter(creature.id)"
      @dragleave="handleDragLeave"
      @drop="handleDrop($event, creature.id)"
    >
    ```
    Note: selection is NOT toggled — clicking the same row again keeps it selected (locked decision).

    **Update `src/components/__tests__/CombatTracker.test.ts`:**
    - Remove any mocks of `useCreatureDetailStore`
    - Remove any mocks of `@/lib/database` if they were only there for handleOpenDetail
    - Add test: clicking a creature row emits 'select' with the creature id
    - Add test: when selectedId prop matches a creature id, that row wrapper has `border-gold` class
    - Add test: when selectedId is null, no row has `border-gold` class
  </action>
  <verify>
    <automated>cd D:/pathbuddy && npx vitest run src/components/__tests__/CombatTracker.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/components/CombatTracker.vue does NOT contain `useCreatureDetailStore`
    - src/components/CombatTracker.vue does NOT contain `detailStore`
    - src/components/CombatTracker.vue does NOT contain `import { db }` (the DB lookup is gone)
    - src/components/CombatTracker.vue contains `emit('select', creatureId)` in handleOpenDetail
    - src/components/CombatTracker.vue contains `selectedId` in defineProps
    - src/components/CombatTracker.vue contains `border-gold` class in a conditional binding
    - src/components/CombatTracker.vue contains `cursor-pointer` on creature row wrapper
    - CombatTracker tests pass
  </acceptance_criteria>
  <done>CombatTracker.handleOpenDetail emits 'select' instead of calling detailStore.openCreature(). Selected row shows border-l-4 border-gold highlight. All detailStore/database imports removed. Tests updated and passing.</done>
</task>

<task type="auto">
  <name>Task 2: Create CombatDetailPanel + wire CombatView selection</name>
  <files>src/components/CombatDetailPanel.vue, src/components/__tests__/CombatDetailPanel.test.ts, src/views/CombatView.vue, src/views/__tests__/CombatView.test.ts</files>
  <read_first>
    - src/views/CombatView.vue
    - src/views/__tests__/CombatView.test.ts
    - src/stores/combat.ts
    - src/types/combat.ts
    - src/components/StatBlock.vue
    - .planning/phases/06-combat-detail-panel/06-UI-SPEC.md
    - .planning/phases/06-combat-detail-panel/06-CONTEXT.md
  </read_first>
  <action>
    **Create `src/components/CombatDetailPanel.vue`:**

    **Props:** `creatureId: { type: String as PropType<string | null>, default: null }`

    **Imports:**
    ```typescript
    import { ref, computed, watch, nextTick } from 'vue'
    import type { PropType } from 'vue'
    import { useCombatStore } from '@/stores/combat'
    import StatBlock from '@/components/StatBlock.vue'
    import { db } from '@/lib/database'
    import { pf2eEntities } from '@/lib/schema'
    import { eq } from 'drizzle-orm'
    ```

    **State:**
    ```typescript
    const combatStore = useCombatStore()
    const rawData = ref<any>(null)
    const isLoadingRawData = ref(false)
    const panelEl = ref<HTMLDivElement | null>(null)
    let currentRequestId = 0  // Race condition guard (Pitfall 4)
    ```

    **Computed: `creature`** — find creature from combat store:
    ```typescript
    const creature = computed(() => {
      if (!props.creatureId) return null
      return combatStore.creatures.find(c => c.id === props.creatureId) ?? null
    })
    ```

    **Computed: `hpPercent`:**
    ```typescript
    const hpPercent = computed(() => {
      if (!creature.value || creature.value.maxHP === 0) return 0
      return Math.round((creature.value.currentHP / creature.value.maxHP) * 100)
    })
    ```

    **Computed: `hpBarColor`** (same thresholds as CreatureCard.vue):
    ```typescript
    const hpBarColor = computed(() => {
      if (hpPercent.value > 50) return 'bg-gold'
      if (hpPercent.value > 25) return 'bg-amber-600'
      return 'bg-crimson-light'
    })
    ```

    **Computed: `regenDisabled`:**
    ```typescript
    const regenDisabled = computed(() => {
      if (!props.creatureId) return false
      return !!combatStore.regenerationDisabled[props.creatureId]
    })
    ```

    **Computed: `conditionsWithValues`** — merge conditions + conditionValues for display:
    ```typescript
    const conditionsWithValues = computed(() => {
      if (!creature.value) return []
      return creature.value.conditions.map(c => ({
        name: c,
        value: creature.value!.conditionValues?.[c] ?? null,
      }))
    })
    ```

    **Watch: `props.creatureId`** — fetch rawData from DB:
    ```typescript
    watch(() => props.creatureId, async (id) => {
      const reqId = ++currentRequestId
      rawData.value = null
      isLoadingRawData.value = false

      if (!id) return

      const c = combatStore.creatures.find(cr => cr.id === id)
      if (!c?.sourceId) {
        // No sourceId — manually added creature, no compendium data
        return
      }

      isLoadingRawData.value = true
      try {
        const results = await db.select().from(pf2eEntities).where(eq(pf2eEntities.sourceId, c.sourceId))
        if (reqId !== currentRequestId) return  // Stale request — discard
        if (results.length > 0) {
          rawData.value = JSON.parse(results[0].rawData)
        }
      } catch {
        // Silently fail — StatBlock shows no-data state
      } finally {
        if (reqId === currentRequestId) {
          isLoadingRawData.value = false
        }
      }

      // Scroll to top
      await nextTick()
      if (panelEl.value && typeof panelEl.value.scrollTo === 'function') {
        panelEl.value.scrollTo(0, 0)
      }
    }, { immediate: true })
    ```

    **Template per UI-SPEC:**
    ```html
    <div ref="panelEl" class="flex flex-col h-full overflow-y-auto bg-charcoal-800">
      <!-- Empty state -->
      <div v-if="!creatureId" class="flex-1 flex flex-col items-center justify-center text-center py-12">
        <h3 class="text-lg font-display font-bold text-stone-300">No Creature Selected</h3>
        <p class="text-sm text-stone-500 mt-2">Click a row in the combat tracker to view its stat block and live combat state.</p>
      </div>

      <!-- Live content -->
      <template v-else-if="creature">
        <!-- LiveCombatOverlay -->
        <div class="bg-charcoal-900 border-b border-charcoal-600 px-4 py-3 space-y-2 flex-shrink-0">
          <!-- Creature name -->
          <h3 class="text-base font-display font-bold text-stone-100">{{ creature.name }}</h3>

          <!-- HP row -->
          <div>
            <span class="text-xs text-stone-400">HP</span>
            <span class="text-lg font-bold text-stone-100 ml-1">{{ creature.currentHP }} / {{ creature.maxHP }}</span>
          </div>
          <!-- HP bar -->
          <div class="w-full h-1.5 rounded-full bg-charcoal-950">
            <div :class="['h-full rounded-full transition-all', hpBarColor]"
              :style="{ width: `${Math.min(100, hpPercent)}%` }" />
          </div>

          <!-- Initiative -->
          <div>
            <span class="text-xs text-stone-400">Initiative</span>
            <span class="text-sm font-bold text-stone-100 ml-1">{{ creature.initiative }}</span>
          </div>

          <!-- Tier badge (not shown for 'normal') -->
          <span v-if="creature.tier && creature.tier !== 'normal'"
            :class="[
              'inline-flex px-2 py-0.5 rounded text-xs font-bold uppercase',
              creature.tier === 'elite' ? 'bg-gold text-charcoal-950' : 'bg-charcoal-500 text-stone-400'
            ]">
            {{ creature.tier }}
          </span>

          <!-- Active conditions -->
          <div v-if="conditionsWithValues.length > 0" class="flex flex-wrap gap-1 mt-1">
            <span v-for="cond in conditionsWithValues" :key="cond.name"
              class="px-2 py-0.5 rounded-full text-xs bg-charcoal-600 border border-charcoal-500 text-stone-300">
              {{ cond.name }}{{ cond.value !== null ? ` ${cond.value}` : '' }}
            </span>
          </div>

          <!-- Ongoing damage (if > 0) -->
          <div v-if="creature.ongoingDamage && creature.ongoingDamage > 0" class="text-xs text-crimson-light">
            Ongoing: {{ creature.ongoingDamage }} dmg
          </div>

          <!-- Regen status -->
          <div v-if="creature.regenAmount && creature.regenAmount > 0">
            <span :class="['text-xs', regenDisabled ? 'text-crimson-light' : 'text-stone-400']">
              {{ regenDisabled ? 'Regen: OFF' : 'Regen: ON' }}
            </span>
          </div>

          <!-- Fast healing -->
          <div v-if="creature.regenAmount && creature.regenAmount > 0 && !regenDisabled" class="text-xs text-emerald-400">
            Fast Healing: {{ creature.regenAmount }}
          </div>
        </div>

        <!-- StatBlock below overlay -->
        <StatBlock :raw-data="rawData" :is-loading="isLoadingRawData" />
      </template>
    </div>
    ```

    **Create `src/components/__tests__/CombatDetailPanel.test.ts`:**
    - Mock `@/lib/database` with self-contained factory (Pitfall 2)
    - Mock `@/stores/combat` to provide test creatures
    - Stub `StatBlock` component
    - Test: renders empty state heading "No Creature Selected" when creatureId is null
    - Test: renders creature name and HP when creatureId matches a creature in store
    - Test: renders initiative value from creature
    - Test: renders tier badge "elite" when creature.tier is 'elite'
    - Test: does NOT render tier badge when creature.tier is 'normal' or undefined
    - Test: renders condition pills when creature has conditions
    - Test: renders "Ongoing: X dmg" when creature.ongoingDamage > 0
    - Test: renders "Regen: OFF" when regenerationDisabled is true for this creature

    **Modify `src/views/CombatView.vue`:**
    - Add `import CombatDetailPanel from '@/components/CombatDetailPanel.vue'`
    - Add `const selectedCombatantId = ref<string | null>(null)` as local ref
    - Handle @select from CombatTracker: `selectedCombatantId.value = $event`
    - Pass selectedId to CombatTracker: `:selected-id="selectedCombatantId"`
    - Replace right panel placeholder with CombatDetailPanel:

    Old right panel:
    ```html
    <div class="flex items-center justify-center text-stone-500 text-sm overflow-y-auto">
      Select a creature to view details
    </div>
    ```
    New right panel:
    ```html
    <CombatDetailPanel :creature-id="selectedCombatantId" class="overflow-y-auto" />
    ```

    Full updated CombatView template:
    ```html
    <div class="grid grid-cols-3 h-full overflow-hidden divide-x divide-charcoal-600">
      <!-- Left panel: creature browser + add bar -->
      <div class="flex flex-col overflow-hidden">
        <CreatureBrowser
          default-entity-type="creature"
          mode="combat"
          :selected-id="selectedEntity?.id ?? null"
          @select="handleBrowserSelect"
          class="flex-1 min-h-0"
        />
        <CombatAddBar
          v-if="selectedEntity"
          :entity="selectedEntity"
          @add="handleAddCreatures"
        />
      </div>

      <!-- Center panel: combat tracker -->
      <div class="overflow-y-auto">
        <CombatTracker
          :selected-id="selectedCombatantId"
          @select="selectedCombatantId = $event"
        />
      </div>

      <!-- Right panel: combat detail -->
      <CombatDetailPanel :creature-id="selectedCombatantId" />
    </div>
    ```

    **Update `src/views/__tests__/CombatView.test.ts`:**
    - Stub CombatDetailPanel component
    - Test: CombatDetailPanel component is rendered (check for stub presence)
    - Update any assertions that expect the placeholder text "Select a creature to view details" — it now lives inside CombatDetailPanel's empty state
  </action>
  <verify>
    <automated>cd D:/pathbuddy && npx vitest run src/components/__tests__/CombatTracker.test.ts src/components/__tests__/CombatDetailPanel.test.ts src/views/__tests__/CombatView.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/components/CombatDetailPanel.vue exists and is >80 lines
    - src/components/CombatDetailPanel.vue contains `useCombatStore` import
    - src/components/CombatDetailPanel.vue contains `StatBlock` import
    - src/components/CombatDetailPanel.vue contains `currentRequestId` for race condition handling
    - src/components/CombatDetailPanel.vue contains `No Creature Selected` empty state text
    - src/components/CombatDetailPanel.vue contains `Ongoing:` text (ongoing damage display)
    - src/components/CombatDetailPanel.vue contains `Regen: OFF` and `Regen: ON` text
    - src/components/CombatDetailPanel.vue contains `Fast Healing:` text
    - src/views/CombatView.vue contains `selectedCombatantId = ref`
    - src/views/CombatView.vue contains `CombatDetailPanel` import
    - src/views/CombatView.vue contains `:creature-id="selectedCombatantId"`
    - src/views/CombatView.vue contains `@select="selectedCombatantId = $event"`
    - All CombatTracker, CombatDetailPanel, and CombatView tests pass
  </acceptance_criteria>
  <done>CombatTracker emits 'select' on row click. CombatView owns selectedCombatantId ref and wires CombatTracker (select + selectedId) and CombatDetailPanel (creatureId). CombatDetailPanel renders LiveCombatOverlay (HP/conditions/initiative/tier/regen/ongoing) + StatBlock (rawData fetched by sourceId). Empty and no-data states render correct copy. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/components/__tests__/CombatTracker.test.ts src/components/__tests__/CombatDetailPanel.test.ts src/views/__tests__/CombatView.test.ts` — all pass
- `grep "useCreatureDetailStore" src/components/CombatTracker.vue` — no matches
- `grep "emit.*select" src/components/CombatTracker.vue` — matches found
- `grep "selectedCombatantId" src/views/CombatView.vue` — matches found
- `grep "CombatDetailPanel" src/views/CombatView.vue` — matches found
</verification>

<success_criteria>
- CombatTracker row click emits 'select' — no detailStore, no DB lookup, no slide-over
- CombatView owns selection state and passes it bidirectionally
- CombatDetailPanel shows live combat overlay + stat block from DB, handles race conditions and no-sourceId gracefully
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-combat-detail-panel/06-03-SUMMARY.md`
</output>
