---
phase: 06-fsd-structure-zustand-stores
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/entities/spell/model/types.ts
  - src/entities/spell/index.ts
  - src/entities/item/model/types.ts
  - src/entities/item/index.ts
  - src/entities/hazard/model/types.ts
  - src/entities/hazard/index.ts
autonomous: true
requirements:
  - ARCH-03

must_haves:
  truths:
    - "spell, item, and hazard entity directories exist with canonical FSD segment structure"
    - "Each minimal skeleton has model/types.ts with a base type stub and index.ts barrel export"
    - "No Zustand stores created for spell/item/hazard — deferred to Phase 7 (spell/item) and Phase 10 (hazard)"
    - "steiger and eslint-plugin-boundaries pass with zero violations on the new directories"
  artifacts:
    - path: "src/entities/spell/model/types.ts"
      provides: "minimal Spell stub type"
      contains: "export interface Spell"
    - path: "src/entities/spell/index.ts"
      provides: "barrel export for spell entity"
      contains: "export type { Spell }"
    - path: "src/entities/item/model/types.ts"
      provides: "minimal Item stub type"
      contains: "export interface Item"
    - path: "src/entities/item/index.ts"
      provides: "barrel export for item entity"
      contains: "export type { Item }"
    - path: "src/entities/hazard/model/types.ts"
      provides: "minimal Hazard stub type"
      contains: "export interface Hazard"
    - path: "src/entities/hazard/index.ts"
      provides: "barrel export for hazard entity"
      contains: "export type { Hazard }"
  key_links:
    - from: "src/entities/spell/index.ts"
      to: "src/entities/spell/model/types.ts"
      via: "re-export"
      pattern: "export type"
---

<objective>
Create 3 minimal entity skeletons (spell, item, hazard) with only directory structure, base type stubs, and barrel exports. No Zustand stores — full implementation deferred to Phase 7 and Phase 10.

Purpose: ARCH-03 requires all planned entity directories to exist under the FSD entities/ layer. The skeleton establishes the correct FSD segment structure (model/types.ts + index.ts) so steiger and eslint-plugin-boundaries don't flag missing barrels when these entities are referenced later.

Output: 3 entity directories with 2 files each (model/types.ts + index.ts). Total: 6 new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-fsd-structure-zustand-stores/06-CONTEXT.md

<interfaces>
<!-- FSD canonical segment names (steiger segments-by-purpose rule) -->
// Valid segment names: ui, model, api, lib, config
// Do NOT use: store/, types/, hooks/ as top-level segments
// model/ is the canonical segment for both types and stores per FSD 2.0

<!-- D-03: spell/item get minimal skeletons only — full shape deferred to Phase 7 -->
<!-- D-04: hazard gets minimal skeleton only — fleshed out Phase 10 -->
<!-- No @engine types needed: spell/item/hazard types don't exist in engine barrel yet -->

<!-- steiger.config.ts note: shared/hooks/ is excepted; all other layers use canonical FSD segment names -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create spell, item, hazard minimal skeletons</name>
  <files>
    src/entities/spell/model/types.ts
    src/entities/spell/index.ts
    src/entities/item/model/types.ts
    src/entities/item/index.ts
    src/entities/hazard/model/types.ts
    src/entities/hazard/index.ts
  </files>
  <read_first>
    - D:/pathbuddy/steiger.config.ts (confirm segment exceptions and FSD config active)
    - D:/pathbuddy/eslint.config.js (confirm entities layer allowed segments)
    - D:/pathbuddy/src/entities/creature/index.ts (reference: existing entity barrel pattern from Plan 02)
  </read_first>
  <action>
    Create 6 files. These are intentionally minimal — Phase 7 (spell/item) and Phase 10 (hazard) will add stores, ui/, and lib/ when the Foundry VTT data structure is known.

    **src/entities/spell/model/types.ts**
    ```typescript
    // Minimal stub — full shape deferred to Phase 7 when Foundry VTT data reveals structure
    export interface Spell {
      id: string
      name: string
      level: number
    }
    ```

    **src/entities/spell/index.ts**
    ```typescript
    export type { Spell } from './model/types'
    ```

    ---

    **src/entities/item/model/types.ts**
    ```typescript
    // Minimal stub — full shape deferred to Phase 7 when Foundry VTT data reveals structure
    export interface Item {
      id: string
      name: string
      level: number
    }
    ```

    **src/entities/item/index.ts**
    ```typescript
    export type { Item } from './model/types'
    ```

    ---

    **src/entities/hazard/model/types.ts**
    ```typescript
    // Minimal stub — full shape deferred to Phase 10 when hazard XP feature is implemented
    export interface Hazard {
      id: string
      name: string
      level: number
      isComplex: boolean
    }
    ```

    **src/entities/hazard/index.ts**
    ```typescript
    export type { Hazard } from './model/types'
    ```

    After creating all 6 files, run: `npm run lint && npx tsc --noEmit`
  </action>
  <verify>
    <automated>cd D:/pathbuddy && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/entities/spell/model/types.ts exists and contains `export interface Spell`
    - src/entities/spell/index.ts exists and contains `export type { Spell }`
    - src/entities/item/model/types.ts exists and contains `export interface Item`
    - src/entities/item/index.ts exists and contains `export type { Item }`
    - src/entities/hazard/model/types.ts exists and contains `export interface Hazard`
    - src/entities/hazard/index.ts exists and contains `export type { Hazard }`
    - `npm run lint` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>3 minimal entity skeletons exist; steiger and eslint-plugin-boundaries pass; typecheck clean</done>
</task>

</tasks>

<verification>
Run from D:/pathbuddy:
```bash
npm run lint && npx tsc --noEmit
ls src/entities/
```

Expected ls output includes: creature/ combatant/ condition/ encounter/ spell/ item/ hazard/
</verification>

<success_criteria>
- 7 entity directories exist total (4 full from Plan 02 + 3 minimal here)
- spell/item/hazard each have exactly model/types.ts + index.ts (no stores, no ui)
- `npm run lint` exits 0
- `npx tsc --noEmit` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/06-fsd-structure-zustand-stores/06-03-SUMMARY.md` following the summary template.
</output>
