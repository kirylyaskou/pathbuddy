---
phase: 13-combat-ux-sweep
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements:
  - CMB-06
  - CMB-10

must_haves:
  truths:
    - "The Kill button in DyingCascadeDialog sets the combatant to dead without requiring a recovery roll"
    - "After clicking Kill, the DEAD screen shows with skull icon, 'DEAD' text, and Override button"
    - "The persistent damage modal opens at turn end for combatants with persistent damage conditions"
    - "The persistent damage modal performs flat-checks (auto or manual) and applies/removes conditions correctly"
  artifacts:
    - path: "src/widgets/combatant-detail/ui/DyingCascadeDialog.tsx"
      provides: "Kill button at line 211-222"
      contains: "Kill (no check)"
    - path: "src/widgets/combatant-detail/ui/PersistentDamageDialog.tsx"
      provides: "Persistent damage flat-check dialog"
      contains: "Flat-check DC"
    - path: "src/features/combat-tracker/lib/turn-manager.ts"
      provides: "Persistent damage trigger on turn advance"
      contains: "setPendingPersistentDamage"
  key_links:
    - from: "DyingCascadeDialog Kill button"
      to: "setConditionValue + setIsDead"
      via: "onClick handler"
      pattern: "setIsDead\\(true\\)"
    - from: "turn-manager advanceTurn"
      to: "setPendingPersistentDamage"
      via: "persistent condition detection"
      pattern: "setPendingPersistentDamage"
    - from: "CombatPage pendingPersistentDamage"
      to: "PersistentDamageDialog open"
      via: "store selector"
      pattern: "pendingPersistentDamage"
---

<objective>
Verify that the Kill button and Persistent Damage modal work correctly end-to-end. Both features are already implemented — this plan confirms they function as expected.

Purpose: CMB-06 and CMB-10 are verification-only requirements. The code exists but has not been confirmed working in the current build. This checkpoint ensures both features work before the phase is marked complete.

Output: Verification confirmation that Kill button and Persistent Damage modal function correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-combat-ux-sweep/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Code-level verification of Kill button and Persistent Damage flows</name>
  <files></files>
  <read_first>
    src/widgets/combatant-detail/ui/DyingCascadeDialog.tsx
    src/widgets/combatant-detail/ui/PersistentDamageDialog.tsx
    src/features/combat-tracker/lib/turn-manager.ts
    src/pages/combat/ui/CombatPage.tsx
    src/features/combat-tracker/model/store.ts
  </read_first>
  <action>
Verify the following code paths by reading the source files. No code changes expected — if issues are found, fix them inline.

**Kill button verification (CMB-06, D-01):**
1. Read DyingCascadeDialog.tsx and confirm:
   - Line ~211-222: Kill button exists with `variant="destructive"` and text "Kill (no check)"
   - onClick handler calls `setConditionValue(combatantId, 'dying' as ConditionSlug, 4 - doomedValue)`
   - onClick handler calls `setIsDead(true)` and `setCheckResult(null)`
   - The `isDead` branch (line ~132) renders the DEAD screen with Skull icon, "DEAD" text, death threshold message
   - The "Override (DM Fiat)" button calls `handleOverride()` which resets dying to 1 and clears isDead
2. Verify the death threshold calculation: `4 - doomedValue` is correct per PF2e rules (doomed reduces maximum dying value)

**Persistent Damage modal verification (CMB-10, D-10):**
1. Read turn-manager.ts and confirm:
   - `advanceTurn()` checks for persistent conditions on the ending combatant (lines ~52-70)
   - When persistent conditions found, it calls `tracker.setPendingPersistentDamage(...)` with combatantId, combatantName, and conditions array
   - Each condition maps slug to damageType via `pc.slug.replace('persistent-', '')`
2. Read the combat tracker store and confirm:
   - `setPendingPersistentDamage` exists as a store action
   - `pendingPersistentDamage` is a readable state field
3. Read CombatPage.tsx and confirm:
   - It reads `pendingPersistentDamage` from the combat tracker store
   - It renders `<PersistentDamageDialog pending={pendingPersistentDamage} onClose={...} />`
   - The onClose handler calls `setPendingPersistentDamage(null)`
4. Read PersistentDamageDialog.tsx and confirm:
   - Dialog opens when `pending` is non-null (`open={!!pending}`)
   - Auto mode: `handleRollAll` iterates all conditions, rolls d20 per condition, calls `applyResult`
   - Manual mode: `handleManualApply` takes manual d20 input, calls `applyResult` for current condition
   - `applyResult` function: deals damage first, then flat-checks (DC 15), removes condition on success
   - Dying increment: if combatant is at 0 HP, `incrementDying` is called instead of dealing damage

**If any issues found:** Fix them inline. Common things to check:
- Does `incrementDying` correctly handle the dying/wounded interaction?
- Does the flat-check DC 15 match PF2e rules? (Yes, DC 15 is correct for persistent damage)
- Does the dialog close properly after all conditions are resolved?
  </action>
  <verify>
    <automated>cd D:/pathbuddy && npx tsc --noEmit --pretty 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - DyingCascadeDialog.tsx contains "Kill (no check)" button text
    - DyingCascadeDialog.tsx contains `setIsDead(true)` in the Kill button onClick
    - DyingCascadeDialog.tsx contains `4 - doomedValue` (death threshold calculation)
    - DyingCascadeDialog.tsx `isDead` branch renders "DEAD" text and "Override (DM Fiat)" button
    - turn-manager.ts contains `setPendingPersistentDamage` call inside advanceTurn
    - PersistentDamageDialog.tsx contains `d20 >= 15` (flat-check threshold)
    - PersistentDamageDialog.tsx contains `removeCondition` (condition removal on success)
    - CombatPage.tsx renders `PersistentDamageDialog` component
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>
    Kill button and Persistent Damage modal code paths are verified correct. Kill button sets dying to death threshold and shows DEAD screen. Persistent damage modal opens on turn advance, performs flat-checks, applies damage on failure, removes condition on success.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual verification of Kill button and Persistent Damage modal</name>
  <files></files>
  <action>
Human verification checkpoint. No code changes — the executor presents verification instructions to the user and waits for approval.

**Kill button test (CMB-06):** User starts the app, navigates to Combat, adds a creature, deals damage to drop HP to 0, then clicks "Kill (no check)" in the Dying dialog. Expected: DEAD screen appears. User clicks "Override (DM Fiat)" to confirm override works.

**Persistent Damage test (CMB-10):** User adds a creature with persistent-fire condition (formula "2d6"), advances turn. Expected: Persistent Damage dialog opens, shows flat-check results, removes condition on d20 >= 15.
  </action>
  <verify>
    <automated>echo "Human verification checkpoint — no automated check"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms Kill button transitions to DEAD screen on click
    - User confirms Override button resets dying state
    - User confirms Persistent Damage dialog opens on turn advance
    - User confirms flat-check results display correctly
  </acceptance_criteria>
  <done>
    Both Kill button and Persistent Damage modal confirmed working end-to-end by manual human testing.
  </done>
</task>

</tasks>

<verification>
1. Kill button exists and transitions to DEAD screen on click
2. Override button resets from DEAD state
3. Persistent damage dialog opens on turn advance for combatants with persistent conditions
4. Flat-check passes (d20 >= 15) remove the condition; failures continue the condition
5. Both features confirmed working by human verification
</verification>

<success_criteria>
- Kill button instantly kills a dying creature without requiring a recovery roll
- Persistent damage modal opens at turn end, performs flat-checks, and correctly applies/removes conditions
- Both features work end-to-end in the running app
</success_criteria>

<output>
After completion, create `.planning/phases/13-combat-ux-sweep/13-03-SUMMARY.md`
</output>
