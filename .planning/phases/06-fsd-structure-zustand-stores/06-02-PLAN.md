---
phase: 06-fsd-structure-zustand-stores
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/entities/creature/model/types.ts
  - src/entities/creature/model/store.ts
  - src/entities/creature/ui/CreatureCard.tsx
  - src/entities/creature/ui/CreatureStatBlock.tsx
  - src/entities/creature/index.ts
  - src/entities/combatant/model/types.ts
  - src/entities/combatant/model/store.ts
  - src/entities/combatant/index.ts
  - src/entities/condition/model/types.ts
  - src/entities/condition/model/store.ts
  - src/entities/condition/index.ts
  - src/entities/encounter/model/types.ts
  - src/entities/encounter/model/store.ts
  - src/entities/encounter/ui/XPBudgetBar.tsx
  - src/entities/encounter/index.ts
  - src/shared/ui/creature-card.tsx
  - src/shared/ui/creature-stat-block.tsx
  - src/shared/ui/xp-budget-bar.tsx
autonomous: true
requirements:
  - ARCH-03
  - ARCH-04
  - ARCH-06

must_haves:
  truths:
    - "entities/creature/ holds CreatureCard and CreatureStatBlock using @engine types (no local type stubs)"
    - "entities/combatant/ holds a Zustand store for combat-slot state (hp, tempHp, initiative, conditions[])"
    - "entities/condition/ holds a Zustand store for per-combatant condition slugs (flat array, serializable)"
    - "entities/encounter/ holds XPBudgetBar using generateEncounterBudgets from @engine"
    - "creature-card.tsx, creature-stat-block.tsx, xp-budget-bar.tsx deleted from shared/ui/"
    - "All entity stores use Zustand v5 create + immer middleware with serializable-only state fields"
  artifacts:
    - path: "src/entities/creature/model/store.ts"
      provides: "useCreatureStore — creature list + selectedId"
      exports: ["useCreatureStore", "CreatureState"]
    - path: "src/entities/combatant/model/store.ts"
      provides: "useCombatantStore — combat slot management"
      exports: ["useCombatantStore", "CombatantState"]
    - path: "src/entities/condition/model/store.ts"
      provides: "useConditionStore — per-combatant condition slugs"
      exports: ["useConditionStore", "ConditionState"]
    - path: "src/entities/encounter/model/store.ts"
      provides: "useEncounterStore — saved encounter entities"
      exports: ["useEncounterStore", "EncounterState"]
    - path: "src/entities/creature/ui/CreatureCard.tsx"
      provides: "CreatureCard component with @engine Creature type"
      contains: "from '@engine'"
    - path: "src/entities/encounter/ui/XPBudgetBar.tsx"
      provides: "XPBudgetBar component using generateEncounterBudgets from @engine"
      contains: "generateEncounterBudgets"
  key_links:
    - from: "src/entities/creature/model/store.ts"
      to: "zustand"
      via: "import { create } from 'zustand'"
      pattern: "from 'zustand'"
    - from: "src/entities/creature/model/store.ts"
      to: "zustand/middleware/immer"
      via: "import { immer } from 'zustand/middleware/immer'"
      pattern: "from 'zustand/middleware/immer'"
    - from: "src/entities/creature/ui/CreatureCard.tsx"
      to: "@engine"
      via: "import type { Creature }"
      pattern: "from '@engine'"
    - from: "src/entities/encounter/ui/XPBudgetBar.tsx"
      to: "@engine"
      via: "import { generateEncounterBudgets }"
      pattern: "generateEncounterBudgets"
---

<objective>
Create 4 full entity slices (creature, combatant, condition, encounter) with typed Zustand stores, move PF2e components from shared/ui/ to correct entity ui/ segments, and replace local type stubs with @engine imports.

Purpose: ARCH-03 (FSD organization), ARCH-04 (entity/feature state ownership), ARCH-06 (@engine consumption). The 4 full entity slices are the core data layer for Phase 7 SQLite hydration — their store shapes define exactly what gets persisted and restored.

Output: 4 entity directories with model/types.ts + model/store.ts + index.ts each; creature and encounter also get ui/ segment; 3 files deleted from shared/ui/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-fsd-structure-zustand-stores/06-CONTEXT.md
@.planning/phases/06-fsd-structure-zustand-stores/06-01-SUMMARY.md

<interfaces>
<!-- From engine/index.ts — types consumed by entity model files -->
```typescript
// Creature and related types
export type { Creature, Rarity, CreatureSize, ActionCost, WeakEliteTier } from '@engine'

// Condition types
export type { ConditionSlug, ValuedCondition } from '@engine'

// Encounter types
export type { ThreatRating, EncounterResult, EncounterCreatureEntry } from '@engine'

// Encounter functions (used by XPBudgetBar)
export { generateEncounterBudgets, calculateEncounterRating } from '@engine'
// generateEncounterBudgets(partyLevel: number, partySize: number) returns budget thresholds
```

<!-- Zustand v5 API (NOT v4 default export) -->
```typescript
import { create } from 'zustand'                      // named export (v5)
import { immer } from 'zustand/middleware/immer'       // sub-path (v5)
import { useShallow } from 'zustand/react/shallow'     // v5 path for useShallow
```

<!-- FSD rules in eslint.config.js -->
// entities layer may import from: shared only
// entities may NOT import from: features, widgets, pages, app
// @engine alias is an external module (not a boundaries element) — allowed everywhere

<!-- shared/ui files being replaced — read before deleting -->
// src/shared/ui/creature-card.tsx — uses local Creature/Rarity/Size type stubs (REPLACE with @engine)
// src/shared/ui/creature-stat-block.tsx — uses local Creature/ActionCost stubs (REPLACE with @engine)
// src/shared/ui/xp-budget-bar.tsx — uses getXPThresholds stub (REPLACE with generateEncounterBudgets from @engine)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create creature, combatant, condition entity slices</name>
  <files>
    src/entities/creature/model/types.ts
    src/entities/creature/model/store.ts
    src/entities/creature/index.ts
    src/entities/combatant/model/types.ts
    src/entities/combatant/model/store.ts
    src/entities/combatant/index.ts
    src/entities/condition/model/types.ts
    src/entities/condition/model/store.ts
    src/entities/condition/index.ts
  </files>
  <read_first>
    - D:/pathbuddy/engine/index.ts (confirm Creature, ConditionSlug, ValuedCondition export names)
    - D:/pathbuddy/src/shared/api/index.ts (from Plan 01 — verify shared/api exists)
    - D:/pathbuddy/eslint.config.js (boundary rules — entities may only import from shared)
  </read_first>
  <action>
    Create all files as specified. Entity stores hold ONLY serializable, SQLite-derivable state — no UI flags, no Map/Set/class instances, no derived values.

    **src/entities/creature/model/types.ts**
    ```typescript
    // Re-export engine types as the entity's canonical types — no local duplication (D-11)
    export type { Creature, Rarity, CreatureSize, ActionCost, WeakEliteTier } from '@engine'
    ```

    **src/entities/creature/model/store.ts**
    ```typescript
    import { create } from 'zustand'
    import { immer } from 'zustand/middleware/immer'
    import type { Creature } from '@engine'

    export interface CreatureState {
      creatures: Creature[]
      selectedId: string | null
      setCreatures: (creatures: Creature[]) => void
      setSelectedId: (id: string | null) => void
      upsertCreature: (creature: Creature) => void
      clearAll: () => void
    }

    export const useCreatureStore = create<CreatureState>()(
      immer((set) => ({
        creatures: [],
        selectedId: null,
        setCreatures: (creatures) =>
          set((state) => {
            state.creatures = creatures
          }),
        setSelectedId: (id) =>
          set((state) => {
            state.selectedId = id
          }),
        upsertCreature: (creature) =>
          set((state) => {
            const idx = state.creatures.findIndex((c) => c.name === creature.name)
            if (idx >= 0) state.creatures[idx] = creature
            else state.creatures.push(creature)
          }),
        clearAll: () =>
          set((state) => {
            state.creatures = []
            state.selectedId = null
          }),
      }))
    )
    ```

    **src/entities/creature/index.ts**
    ```typescript
    export { useCreatureStore } from './model/store'
    export type { CreatureState } from './model/store'
    export type { Creature, Rarity, CreatureSize, ActionCost, WeakEliteTier } from './model/types'
    ```

    ---

    **src/entities/combatant/model/types.ts**
    ```typescript
    // Combatant is a runtime concept: creature in an active combat slot.
    // Not in @engine (engine has no combat-slot concept) — defined here as entity layer type.
    export interface Combatant {
      id: string           // uuid — unique per combat slot
      creatureRef: string  // Creature.name or future DB id
      displayName: string
      initiative: number
      hp: number
      maxHp: number
      tempHp: number
      conditions: string[] // condition slugs — ConditionManager is module-level, not stored here
      isNPC: boolean
    }
    ```

    **src/entities/combatant/model/store.ts**
    ```typescript
    import { create } from 'zustand'
    import { immer } from 'zustand/middleware/immer'
    import type { Combatant } from './types'

    export interface CombatantState {
      combatants: Combatant[]
      addCombatant: (combatant: Combatant) => void
      removeCombatant: (id: string) => void
      updateHp: (id: string, delta: number) => void
      updateTempHp: (id: string, tempHp: number) => void
      addCondition: (id: string, slug: string) => void
      removeCondition: (id: string, slug: string) => void
      reorderInitiative: (orderedIds: string[]) => void
      clearAll: () => void
    }

    export const useCombatantStore = create<CombatantState>()(
      immer((set) => ({
        combatants: [],
        addCombatant: (combatant) =>
          set((state) => {
            state.combatants.push(combatant)
          }),
        removeCombatant: (id) =>
          set((state) => {
            state.combatants = state.combatants.filter((c) => c.id !== id)
          }),
        updateHp: (id, delta) =>
          set((state) => {
            const c = state.combatants.find((c) => c.id === id)
            if (c) c.hp = Math.max(0, Math.min(c.maxHp, c.hp + delta))
          }),
        updateTempHp: (id, tempHp) =>
          set((state) => {
            const c = state.combatants.find((c) => c.id === id)
            if (c) c.tempHp = Math.max(0, tempHp)
          }),
        addCondition: (id, slug) =>
          set((state) => {
            const c = state.combatants.find((c) => c.id === id)
            if (c && !c.conditions.includes(slug)) c.conditions.push(slug)
          }),
        removeCondition: (id, slug) =>
          set((state) => {
            const c = state.combatants.find((c) => c.id === id)
            if (c) c.conditions = c.conditions.filter((s) => s !== slug)
          }),
        reorderInitiative: (orderedIds) =>
          set((state) => {
            state.combatants.sort(
              (a, b) => orderedIds.indexOf(a.id) - orderedIds.indexOf(b.id)
            )
          }),
        clearAll: () =>
          set((state) => {
            state.combatants = []
          }),
      }))
    )
    ```

    **src/entities/combatant/index.ts**
    ```typescript
    export { useCombatantStore } from './model/store'
    export type { CombatantState } from './model/store'
    export type { Combatant } from './model/types'
    ```

    ---

    **src/entities/condition/model/types.ts**
    ```typescript
    // Re-export engine types — no local duplication (D-11)
    export type { ConditionSlug, ValuedCondition } from '@engine'

    // ActiveCondition: flat tuple stored per combatant in the entity store
    // Flat array is SQLite-serialization-friendly (easier to upsert than nested Map)
    export interface ActiveCondition {
      combatantId: string
      slug: string         // ConditionSlug value
      value?: number       // Only present for valued conditions (frightened, sickened, etc.)
    }
    ```

    **src/entities/condition/model/store.ts**
    ```typescript
    import { create } from 'zustand'
    import { immer } from 'zustand/middleware/immer'
    import type { ActiveCondition } from './types'

    export interface ConditionState {
      activeConditions: ActiveCondition[]
      setCondition: (condition: ActiveCondition) => void
      removeCondition: (combatantId: string, slug: string) => void
      decrementCondition: (combatantId: string, slug: string) => void
      clearCombatantConditions: (combatantId: string) => void
      clearAll: () => void
    }

    export const useConditionStore = create<ConditionState>()(
      immer((set) => ({
        activeConditions: [],
        setCondition: (condition) =>
          set((state) => {
            const idx = state.activeConditions.findIndex(
              (c) => c.combatantId === condition.combatantId && c.slug === condition.slug
            )
            if (idx >= 0) state.activeConditions[idx] = condition
            else state.activeConditions.push(condition)
          }),
        removeCondition: (combatantId, slug) =>
          set((state) => {
            state.activeConditions = state.activeConditions.filter(
              (c) => !(c.combatantId === combatantId && c.slug === slug)
            )
          }),
        decrementCondition: (combatantId, slug) =>
          set((state) => {
            const c = state.activeConditions.find(
              (c) => c.combatantId === combatantId && c.slug === slug
            )
            if (c && c.value !== undefined) {
              if (c.value <= 1) {
                state.activeConditions = state.activeConditions.filter(
                  (ac) => !(ac.combatantId === combatantId && ac.slug === slug)
                )
              } else {
                c.value -= 1
              }
            }
          }),
        clearCombatantConditions: (combatantId) =>
          set((state) => {
            state.activeConditions = state.activeConditions.filter(
              (c) => c.combatantId !== combatantId
            )
          }),
        clearAll: () =>
          set((state) => {
            state.activeConditions = []
          }),
      }))
    )
    ```

    **src/entities/condition/index.ts**
    ```typescript
    export { useConditionStore } from './model/store'
    export type { ConditionState } from './model/store'
    export type { ActiveCondition, ConditionSlug, ValuedCondition } from './model/types'
    ```

    After creating all files, run: `npm run lint && npx tsc --noEmit`
  </action>
  <verify>
    <automated>cd D:/pathbuddy && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/entities/creature/model/store.ts contains `import { create } from 'zustand'`
    - src/entities/creature/model/store.ts contains `import { immer } from 'zustand/middleware/immer'`
    - src/entities/creature/model/types.ts contains `from '@engine'`
    - src/entities/combatant/model/types.ts contains `export interface Combatant`
    - src/entities/combatant/model/store.ts contains `useCombatantStore`
    - src/entities/combatant/model/store.ts contains `updateHp`
    - src/entities/condition/model/types.ts contains `export interface ActiveCondition`
    - src/entities/condition/model/store.ts contains `decrementCondition`
    - src/entities/creature/index.ts contains `export { useCreatureStore }`
    - src/entities/combatant/index.ts contains `export { useCombatantStore }`
    - src/entities/condition/index.ts contains `export { useConditionStore }`
    - `npm run lint` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>creature, combatant, and condition entity slices exist with typed Zustand stores and barrel exports; lint and typecheck pass</done>
</task>

<task type="auto">
  <name>Task 2: Create encounter entity slice and move PF2e components</name>
  <files>
    src/entities/encounter/model/types.ts
    src/entities/encounter/model/store.ts
    src/entities/encounter/ui/XPBudgetBar.tsx
    src/entities/encounter/index.ts
    src/entities/creature/ui/CreatureCard.tsx
    src/entities/creature/ui/CreatureStatBlock.tsx
    src/shared/ui/creature-card.tsx
    src/shared/ui/creature-stat-block.tsx
    src/shared/ui/xp-budget-bar.tsx
  </files>
  <read_first>
    - D:/pathbuddy/src/shared/ui/creature-card.tsx (full component — copy, then replace local types with @engine)
    - D:/pathbuddy/src/shared/ui/creature-stat-block.tsx (full component — copy, then replace local types with @engine)
    - D:/pathbuddy/src/shared/ui/xp-budget-bar.tsx (full component — copy, then replace stubs with generateEncounterBudgets)
    - D:/pathbuddy/engine/index.ts (confirm generateEncounterBudgets signature and Creature type shape)
  </read_first>
  <action>
    **Step 1: Create encounter entity slice**

    **src/entities/encounter/model/types.ts**
    ```typescript
    // Encounter is a saved encounter entity (SQLite-derived).
    // Not to be confused with feature/encounter-builder runtime state.
    export interface Encounter {
      id: string
      name: string
      partyLevel: number
      partySize: number
      creatureIds: string[]  // references to Creature names / future DB ids
      createdAt: string      // ISO string — serializable
    }
    ```

    **src/entities/encounter/model/store.ts**
    ```typescript
    import { create } from 'zustand'
    import { immer } from 'zustand/middleware/immer'
    import type { Encounter } from './types'

    export interface EncounterState {
      encounters: Encounter[]
      selectedId: string | null
      setEncounters: (encounters: Encounter[]) => void
      setSelectedId: (id: string | null) => void
      upsertEncounter: (encounter: Encounter) => void
      removeEncounter: (id: string) => void
      clearAll: () => void
    }

    export const useEncounterStore = create<EncounterState>()(
      immer((set) => ({
        encounters: [],
        selectedId: null,
        setEncounters: (encounters) =>
          set((state) => {
            state.encounters = encounters
          }),
        setSelectedId: (id) =>
          set((state) => {
            state.selectedId = id
          }),
        upsertEncounter: (encounter) =>
          set((state) => {
            const idx = state.encounters.findIndex((e) => e.id === encounter.id)
            if (idx >= 0) state.encounters[idx] = encounter
            else state.encounters.push(encounter)
          }),
        removeEncounter: (id) =>
          set((state) => {
            state.encounters = state.encounters.filter((e) => e.id !== id)
          }),
        clearAll: () =>
          set((state) => {
            state.encounters = []
            state.selectedId = null
          }),
      }))
    )
    ```

    **Step 2: Create entities/encounter/ui/XPBudgetBar.tsx**

    Copy the entire visual/layout structure from src/shared/ui/xp-budget-bar.tsx verbatim (all JSX, className bindings, threatColors/threatTextColors maps). Replace ONLY the local stub functions:

    - DELETE: `function getXPThresholds(partySize: number)` (lines 8-17 of original)
    - DELETE: `function getThreatLevel(xp: number, partySize: number): ThreatLevel` (lines 20-27 of original)
    - DELETE: `type ThreatLevel = ...` (local type stub)
    - ADD at top: `import { generateEncounterBudgets } from '@engine'`
    - ADD at top: `import type { ThreatRating } from '@engine'` (replaces local ThreatLevel type — ThreatRating is the @engine equivalent)

    Replace internal logic of component body:
    - OLD: `const thresholds = getXPThresholds(partySize)` and `const threatLevel = getThreatLevel(currentXP, partySize)`
    - NEW:
      ```typescript
      const budgets = generateEncounterBudgets(partyLevel, partySize)
      // generateEncounterBudgets returns { trivial, low, moderate, severe, extreme } XP thresholds
      const thresholds = {
        trivial: budgets.trivial,
        low: budgets.low,
        moderate: budgets.moderate,
        severe: budgets.severe,
        extreme: budgets.extreme,
      }
      const threatLevel: ThreatRating = currentXP >= thresholds.extreme ? 'extreme'
        : currentXP >= thresholds.severe ? 'severe'
        : currentXP >= thresholds.moderate ? 'moderate'
        : currentXP >= thresholds.low ? 'low'
        : 'trivial'
      ```

    Update the component props interface: add `partyLevel: number` alongside the existing `partySize: number` (needed for `generateEncounterBudgets(partyLevel, partySize)`).

    IMPORTANT: First run `grep -n "generateEncounterBudgets" engine/index.ts` and check the actual return type of `generateEncounterBudgets` in the engine. If it doesn't return `{ trivial, low, moderate, severe, extreme }` directly, adapt the mapping accordingly. Read engine/encounter/xp.ts if needed.

    **src/entities/encounter/index.ts**
    ```typescript
    export { useEncounterStore } from './model/store'
    export type { EncounterState } from './model/store'
    export type { Encounter } from './model/types'
    export { XPBudgetBar } from './ui/XPBudgetBar'
    ```

    **Step 3: Create entities/creature/ui/ components**

    Create `src/entities/creature/ui/CreatureCard.tsx`:
    - Copy the entire component from src/shared/ui/creature-card.tsx
    - Delete local type stubs (lines 10-26 of original: `type Rarity`, `type Size`, `interface Creature`, `interface CreatureCardProps`)
    - Add at top: `import type { Creature, Rarity, CreatureSize } from '@engine'`
    - Update the props interface:
      ```typescript
      interface CreatureCardProps {
        creature: Creature
        compact?: boolean
        onAdd?: () => void
        onClick?: () => void
        className?: string
      }
      ```
    - Note: The original used local `Size` type; engine exports `CreatureSize`. The component only uses `creature.size` as a display string — the type change is transparent to JSX.
    - Fix internal imports: change `import { LevelBadge } from "./level-badge"` etc. to `import { LevelBadge } from "@/shared/ui/level-badge"` (these atoms stay in shared/ui per D-09)
    - Same for StatBlock/TraitList: `import { StatBlock } from "@/shared/ui/stat-badge"` and `import { TraitList } from "@/shared/ui/trait-pill"`

    Create `src/entities/creature/ui/CreatureStatBlock.tsx`:
    - Copy from src/shared/ui/creature-stat-block.tsx
    - Delete local type stubs (Rarity, Size, ActionCost, Creature interface, CreatureStatBlockProps interface)
    - Add: `import type { Creature, Rarity, CreatureSize, ActionCost } from '@engine'`
    - Fix internal imports to use @/shared/ui/ paths for level-badge, trait-pill, action-icon

    Update **src/entities/creature/index.ts** to include ui exports:
    ```typescript
    export { useCreatureStore } from './model/store'
    export type { CreatureState } from './model/store'
    export type { Creature, Rarity, CreatureSize, ActionCost, WeakEliteTier } from './model/types'
    export { CreatureCard } from './ui/CreatureCard'
    export { CreatureStatBlock } from './ui/CreatureStatBlock'
    ```

    **Step 4: Delete moved files from shared/ui/**

    First verify no consumers exist:
    ```bash
    grep -r "creature-card\|creature-stat-block\|xp-budget-bar" src --include="*.tsx" --include="*.ts" | grep -v "entities/"
    ```
    If output is empty, proceed to delete:
    - DELETE src/shared/ui/creature-card.tsx
    - DELETE src/shared/ui/creature-stat-block.tsx
    - DELETE src/shared/ui/xp-budget-bar.tsx

    If consumers exist (unexpected), update their import path before deleting.

    **Step 5: Run verification**
    ```bash
    npm run lint && npx tsc --noEmit
    ```
  </action>
  <verify>
    <automated>cd D:/pathbuddy && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/entities/encounter/model/store.ts contains `useEncounterStore`
    - src/entities/encounter/ui/XPBudgetBar.tsx contains `from '@engine'`
    - src/entities/encounter/ui/XPBudgetBar.tsx contains `generateEncounterBudgets`
    - src/entities/creature/ui/CreatureCard.tsx contains `from '@engine'`
    - src/entities/creature/ui/CreatureStatBlock.tsx contains `from '@engine'`
    - src/entities/creature/ui/CreatureCard.tsx does NOT contain `type Rarity = '` (no local type stubs)
    - src/entities/creature/ui/CreatureStatBlock.tsx does NOT contain `type Rarity = '` (no local type stubs)
    - src/shared/ui/creature-card.tsx does NOT exist (deleted)
    - src/shared/ui/creature-stat-block.tsx does NOT exist (deleted)
    - src/shared/ui/xp-budget-bar.tsx does NOT exist (deleted)
    - `npm run lint` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All 4 full entity slices exist with Zustand stores; creature and encounter have ui/ segments with @engine-typed components; 3 legacy files deleted from shared/ui/; lint and typecheck pass</done>
</task>

</tasks>

<verification>
Run from D:/pathbuddy:
```bash
npm run lint && npx tsc --noEmit
```

Manual review checklist (ARCH-04 semantic check):
- [ ] entities/creature/model/store.ts: state fields are `creatures: Creature[]` and `selectedId: string | null` — both serializable
- [ ] entities/combatant/model/store.ts: state field is `combatants: Combatant[]` — Combatant has only primitive/string[] fields
- [ ] entities/condition/model/store.ts: state field is `activeConditions: ActiveCondition[]` — ActiveCondition has only string/number fields
- [ ] entities/encounter/model/store.ts: state field is `encounters: Encounter[]` — Encounter has only string/number/string[] fields
- [ ] No entity store has: boolean UI flags, Map/Set instances, class instances, or function references in state
</verification>

<success_criteria>
- 4 entity directories exist: creature/, combatant/, condition/, encounter/
- All entity stores use `create + immer` from Zustand v5 named imports
- XPBudgetBar uses generateEncounterBudgets from @engine (no local stub functions)
- CreatureCard and CreatureStatBlock import types from @engine (no local type stubs)
- shared/ui/ no longer contains creature-card.tsx, creature-stat-block.tsx, xp-budget-bar.tsx
- `npm run lint` exits 0 (zero FSD boundary violations)
- `npx tsc --noEmit` exits 0 (zero TypeScript errors)
</success_criteria>

<output>
After completion, create `.planning/phases/06-fsd-structure-zustand-stores/06-02-SUMMARY.md` following the summary template.
</output>
