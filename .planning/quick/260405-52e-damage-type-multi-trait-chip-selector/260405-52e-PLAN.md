# Quick Task 260405-52e: Damage Type Multi-Trait Chip Selector

**Created:** 2026-04-05
**Status:** In progress

## Goal

Replace the single-type dropdown in `HpControls` with a chip-based multi-select dialog. A single hit can carry multiple damage traits (e.g. holy + piercing + cold-iron + magic). IWR is computed per damage type with equal amount split; material traits passed to each instance.

## Task 1: Refactor HpControls to multi-trait chip selector

**Files:**
- `src/widgets/combatant-detail/ui/HpControls.tsx`

**Action:**
1. Replace `damageType: DamageType | null` state with `selectedTraits: string[]`
2. Add `traitSelectorOpen: boolean` state
3. Remove Popover/Command imports, add Dialog imports
4. Add `TRAIT_GROUPS` constant covering damage types + material traits
5. Replace dropdown trigger button with a compact chip display button that opens a Dialog
6. Dialog body: grouped chip buttons (toggle-able) for all traits
7. Update `iwrPreview` useMemo to handle multiple damage types:
   - Extract DamageTypes from selectedTraits
   - Extract MaterialEffects from selectedTraits
   - If multiple damage types: split amount equally, run applyIWR per type
8. Update `handleAction('damage')` to sum finalDamage across all IWR instances

**must_haves:**
- Selected traits shown as chips in the trigger area
- "Untyped" shown when no traits selected
- Dialog closes on backdrop click
- IWR preview works for multi-type (holy + piercing → two IWR rows summed)
- Materials (cold-iron) passed to each damage instance
- Clearing all traits resets to untyped behavior
