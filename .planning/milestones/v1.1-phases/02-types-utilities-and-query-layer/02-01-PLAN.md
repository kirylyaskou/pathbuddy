---
phase: 02-types-utilities-and-query-layer
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/types/entity.ts
  - src/lib/weak-elite.ts
  - src/lib/__tests__/weak-elite.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "getHpAdjustment('elite', 2) returns 10 and getHpAdjustment('elite', 3) returns 15 (bracket boundary at level 2/3)"
    - "getHpAdjustment('weak', 15) returns -90 (not -20 from simplified 3-bracket table)"
    - "getHpAdjustment('normal', any) returns 0"
    - "getAdjustedLevel('elite', -1) returns 1 (not 0)"
    - "getAdjustedLevel('elite', 0) returns 2 (not 1)"
    - "getAdjustedLevel('weak', 1) returns -1 (not 0)"
    - "getHpAdjustment('weak', 0) returns 0 (not applicable)"
  artifacts:
    - path: "src/types/entity.ts"
      provides: "WeakEliteTier type definition"
      contains: "export type WeakEliteTier"
    - path: "src/lib/weak-elite.ts"
      provides: "HP adjustment and level adjustment pure functions"
      exports: ["getHpAdjustment", "getAdjustedLevel"]
      min_lines: 40
    - path: "src/lib/__tests__/weak-elite.test.ts"
      provides: "Unit tests for all 12 bracket boundaries + 4 edge cases"
      min_lines: 80
  key_links:
    - from: "src/lib/weak-elite.ts"
      to: "src/types/entity.ts"
      via: "import WeakEliteTier"
      pattern: "import.*WeakEliteTier.*from.*entity"
---

<objective>
Create the WeakEliteTier type and the weak/elite HP adjustment utility with full TDD coverage.

Purpose: The getHpAdjustment() and getAdjustedLevel() functions are consumed by Phase 05 (Combat Workspace) to auto-adjust creature HP and display level when adding weak/elite creatures to combat. Getting the 12-bracket HP table wrong produces silently incorrect HP values for every creature above level 4. This plan uses TDD to guarantee correctness at every bracket boundary before any UI touches these functions.

Output: `src/types/entity.ts` (type), `src/lib/weak-elite.ts` (utility), `src/lib/__tests__/weak-elite.test.ts` (tests)
</objective>

<execution_context>
@C:/Users/kiryl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kiryl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-types-utilities-and-query-layer/02-CONTEXT.md
@.planning/phases/02-types-utilities-and-query-layer/02-RESEARCH.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/types/combat.ts (pattern to follow for entity.ts):
```typescript
export type Condition = 'stunned' | 'paralyzed' | ...
export interface Creature { id: string; name: string; ... }
```
Pattern: plain type/interface exports, no default export, no class.
</interfaces>
</context>

<feature>
  <name>Weak/Elite HP Adjustment Utility</name>
  <files>src/types/entity.ts, src/lib/weak-elite.ts, src/lib/__tests__/weak-elite.test.ts</files>
  <behavior>
    getHpAdjustment(tier, level):
    - ('normal', any) -> 0
    - ('elite', 1) -> 10, ('elite', 2) -> 10
    - ('elite', 3) -> 15, ('elite', 4) -> 15
    - ('elite', 5) -> 20, ('elite', 6) -> 20
    - ('elite', 7) -> 30, ('elite', 8) -> 30
    - ('elite', 9) -> 40, ('elite', 10) -> 40
    - ('elite', 11) -> 55, ('elite', 12) -> 55
    - ('elite', 13) -> 70, ('elite', 14) -> 70
    - ('elite', 15) -> 90, ('elite', 16) -> 90
    - ('elite', 17) -> 110, ('elite', 18) -> 110
    - ('elite', 19) -> 135, ('elite', 20) -> 135
    - ('elite', 21) -> 160, ('elite', 22) -> 160
    - ('elite', 23) -> 185, ('elite', 24) -> 185
    - ('elite', 25) -> 185 (uses highest bracket)
    - ('elite', -1) -> 10 (clamped to level 1 bracket)
    - ('elite', 0) -> 10 (clamped to level 1 bracket)
    - ('weak', 15) -> -90 (negative delta, same magnitude)
    - ('weak', 0) -> 0 (not applicable per locked decision)
    - ('weak', -1) -> 0 (not applicable per locked decision)

    getAdjustedLevel(tier, level):
    - ('normal', any) -> level unchanged
    - ('elite', 5) -> 6
    - ('elite', -1) -> 1 (NOT 0; special case: +2 instead of +1)
    - ('elite', 0) -> 2 (NOT 1; special case: +2 instead of +1)
    - ('weak', 5) -> 4
    - ('weak', 1) -> -1 (NOT 0; special case: -2 instead of -1)
    - ('weak', 0) -> 0 (no change; weak on level <= 0 is undefined)
    - ('weak', -1) -> -1 (no change; weak on level <= 0 is undefined)
  </behavior>
  <implementation>
    RED: Write tests first covering all 12 bracket boundaries for getHpAdjustment (elite and weak), normal tier, all 4 level edge cases for getAdjustedLevel, and weak-on-level-zero special case. Tests import from weak-elite.ts which does not exist yet -> tests fail.

    GREEN: Create src/types/entity.ts with WeakEliteTier type. Create src/lib/weak-elite.ts with:
    1. HP_TABLE lookup array (12 entries from PITFALLS.md authoritative table)
    2. getHpAdjustment() using find() on HP_TABLE
    3. getAdjustedLevel() with explicit edge case branches
    Run tests -> all pass.

    REFACTOR: No refactor expected for this size.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Write failing tests for weak-elite utility</name>
  <files>src/types/entity.ts, src/lib/__tests__/weak-elite.test.ts</files>
  <read_first>
    - src/types/combat.ts
    - src/lib/__tests__/creature-resolver.test.ts
    - .planning/research/PITFALLS.md
    - .planning/phases/02-types-utilities-and-query-layer/02-CONTEXT.md
  </read_first>
  <action>
1. Create `src/types/entity.ts` with exactly:
```typescript
export type WeakEliteTier = 'normal' | 'weak' | 'elite'
```
No default export. Follow `src/types/combat.ts` pattern (plain type exports).

2. Create `src/lib/__tests__/weak-elite.test.ts` with these test groups:

**describe('getHpAdjustment'):**
- test 'normal tier returns 0 for any level': test levels 1, 10, 20 all return 0
- test 'elite bracket 1-2 returns +10': test levels 1 and 2
- test 'elite bracket 3-4 returns +15': test levels 3 and 4
- test 'elite bracket 5-6 returns +20': test levels 5 and 6
- test 'elite bracket 7-8 returns +30': test levels 7 and 8
- test 'elite bracket 9-10 returns +40': test levels 9 and 10
- test 'elite bracket 11-12 returns +55': test levels 11 and 12
- test 'elite bracket 13-14 returns +70': test levels 13 and 14
- test 'elite bracket 15-16 returns +90': test levels 15 and 16
- test 'elite bracket 17-18 returns +110': test levels 17 and 18
- test 'elite bracket 19-20 returns +135': test levels 19 and 20
- test 'elite bracket 21-22 returns +160': test levels 21 and 22
- test 'elite bracket 23-24 returns +185': test levels 23 and 24
- test 'elite level 25 uses highest bracket (185)': `getHpAdjustment('elite', 25)` -> 185
- test 'weak returns negative delta (same magnitude)': `getHpAdjustment('weak', 15)` -> -90, `getHpAdjustment('weak', 3)` -> -15
- test 'elite level -1 clamps to bracket minimum (+10)': `getHpAdjustment('elite', -1)` -> 10
- test 'elite level 0 clamps to bracket minimum (+10)': `getHpAdjustment('elite', 0)` -> 10
- test 'weak level 0 returns 0 (not applicable)': `getHpAdjustment('weak', 0)` -> 0
- test 'weak level -1 returns 0 (not applicable)': `getHpAdjustment('weak', -1)` -> 0

**describe('getAdjustedLevel'):**
- test 'normal tier returns level unchanged': levels -1, 0, 1, 10, 20 all return same
- test 'elite adds 1 to standard levels': `getAdjustedLevel('elite', 5)` -> 6
- test 'elite level -1 returns 1 (special +2)': `getAdjustedLevel('elite', -1)` -> 1
- test 'elite level 0 returns 2 (special +2)': `getAdjustedLevel('elite', 0)` -> 2
- test 'weak subtracts 1 from standard levels': `getAdjustedLevel('weak', 5)` -> 4
- test 'weak level 1 returns -1 (special -2)': `getAdjustedLevel('weak', 1)` -> -1
- test 'weak level 0 returns 0 (no change)': `getAdjustedLevel('weak', 0)` -> 0
- test 'weak level -1 returns -1 (no change)': `getAdjustedLevel('weak', -1)` -> -1

All tests import `{ getHpAdjustment, getAdjustedLevel }` from `'@/lib/weak-elite'`. No mocks needed — these are pure functions.

3. Run tests — they MUST fail (module not found). Commit: `test(02-01): add failing tests for weak-elite utility`
  </action>
  <verify>
    <automated>cd D:/pathbuddy && npx vitest run src/lib/__tests__/weak-elite.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/types/entity.ts contains `export type WeakEliteTier = 'normal' | 'weak' | 'elite'`
    - src/lib/__tests__/weak-elite.test.ts contains `describe('getHpAdjustment'`
    - src/lib/__tests__/weak-elite.test.ts contains `describe('getAdjustedLevel'`
    - src/lib/__tests__/weak-elite.test.ts contains `getHpAdjustment('elite', 15)` (bracket 15-16 = 90)
    - src/lib/__tests__/weak-elite.test.ts contains `getHpAdjustment('weak', 0)` (edge case)
    - src/lib/__tests__/weak-elite.test.ts contains `getAdjustedLevel('elite', -1)` (edge case)
    - src/lib/__tests__/weak-elite.test.ts contains `getAdjustedLevel('weak', 1)` (edge case)
    - Test run fails with module not found (RED phase)
  </acceptance_criteria>
  <done>All 20+ tests written and committed. Tests fail because src/lib/weak-elite.ts does not exist yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Implement weak-elite utility to pass all tests</name>
  <files>src/lib/weak-elite.ts</files>
  <read_first>
    - src/lib/__tests__/weak-elite.test.ts
    - src/types/entity.ts
    - .planning/research/PITFALLS.md
    - .planning/phases/02-types-utilities-and-query-layer/02-RESEARCH.md
  </read_first>
  <action>
Create `src/lib/weak-elite.ts` with:

1. Import: `import type { WeakEliteTier } from '@/types/entity'`

2. HP_TABLE constant — the ONLY acceptable source is PITFALLS.md 12-bracket table:
```typescript
const HP_TABLE: Array<{ maxLevel: number; delta: number }> = [
  { maxLevel: 2,  delta: 10  },
  { maxLevel: 4,  delta: 15  },
  { maxLevel: 6,  delta: 20  },
  { maxLevel: 8,  delta: 30  },
  { maxLevel: 10, delta: 40  },
  { maxLevel: 12, delta: 55  },
  { maxLevel: 14, delta: 70  },
  { maxLevel: 16, delta: 90  },
  { maxLevel: 18, delta: 110 },
  { maxLevel: 20, delta: 135 },
  { maxLevel: 22, delta: 160 },
  { maxLevel: 24, delta: 185 },
]
```
Add a code comment: `// Source: PF2e Monster Core — Archives of Nethys IDs 3264/3265`

3. `getHpAdjustment(tier: WeakEliteTier, level: number): number`:
   - if tier === 'normal' -> return 0
   - if tier === 'weak' && level <= 0 -> return 0 (not applicable per locked decision)
   - clamp lookup level: `const lookupLevel = Math.max(level, 1)`
   - find bracket: `HP_TABLE.find(b => lookupLevel <= b.maxLevel) ?? HP_TABLE[HP_TABLE.length - 1]`
   - if tier === 'elite' -> return +bracket.delta
   - if tier === 'weak' -> return -bracket.delta

4. `getAdjustedLevel(tier: WeakEliteTier, level: number): number`:
   - if tier === 'normal' -> return level
   - if tier === 'elite':
     - if level === -1 || level === 0 -> return level + 2
     - else -> return level + 1
   - if tier === 'weak':
     - if level <= 0 -> return level (no change; undefined in rules)
     - if level === 1 -> return -1 (special: -2 instead of -1)
     - else -> return level - 1

5. Export both functions.

6. Run tests — all MUST pass. Commit: `feat(02-01): implement weak-elite HP adjustment utility`
  </action>
  <verify>
    <automated>cd D:/pathbuddy && npx vitest run src/lib/__tests__/weak-elite.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/weak-elite.ts contains `export function getHpAdjustment(`
    - src/lib/weak-elite.ts contains `export function getAdjustedLevel(`
    - src/lib/weak-elite.ts contains `import type { WeakEliteTier } from '@/types/entity'`
    - src/lib/weak-elite.ts contains `maxLevel: 16, delta: 90` (bracket 15-16)
    - src/lib/weak-elite.ts contains `maxLevel: 24, delta: 185` (highest bracket)
    - src/lib/weak-elite.ts contains `Archives of Nethys` (source comment)
    - src/lib/weak-elite.ts does NOT contain `if level < 5 return 10` (anti-pattern: simplified 3-bracket)
    - All tests in weak-elite.test.ts pass (exit code 0)
  </acceptance_criteria>
  <done>All weak-elite tests pass. getHpAdjustment returns correct deltas for all 12 brackets. getAdjustedLevel handles all 4 edge cases. Full test suite green.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/__tests__/weak-elite.test.ts` exits 0 with 20+ tests passing
- `npx vitest run` full suite passes with no regressions
- src/types/entity.ts exports WeakEliteTier type
- src/lib/weak-elite.ts exports getHpAdjustment and getAdjustedLevel
- No simplified 3-bracket HP table anywhere in codebase
</verification>

<success_criteria>
- WeakEliteTier type exported from src/types/entity.ts
- getHpAdjustment('elite', 15) returns 90 (12-bracket table, not 20)
- getHpAdjustment('weak', 0) returns 0 (not applicable)
- getAdjustedLevel('elite', -1) returns 1 (special +2 case)
- getAdjustedLevel('weak', 1) returns -1 (special -2 case)
- All tests pass, no regressions in full suite
</success_criteria>

<output>
After completion, create `.planning/phases/02-types-utilities-and-query-layer/02-01-SUMMARY.md`
</output>
