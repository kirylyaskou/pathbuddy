---
phase: 04-actions-modifier-math
plan: 04
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - engine/index.ts
autonomous: true
requirements: [ENG-02, ENG-03]

must_haves:
  truths:
    - "All Phase 4 symbols are accessible via import from '@engine' or './engine'"
    - "Action types, ACTIONS map, and ACTION_OUTCOMES are exported"
    - "DegreeOfSuccess types and calculateDegreeOfSuccess are exported"
    - "Statistic class, resolveSelector, CreatureStatistics, and buildAttackModifierSets are exported"
    - "Expanded Creature interface types (AbilityKey, CreatureSize, Rarity, etc.) are exported"
    - "No circular dependency errors at compile time"
  artifacts:
    - path: "engine/index.ts"
      provides: "Complete barrel export for all Phase 4 additions"
      contains: "calculateDegreeOfSuccess"
  key_links:
    - from: "engine/index.ts"
      to: "engine/actions/types.ts"
      via: "re-export Action types"
      pattern: "export.*from.*actions/types"
    - from: "engine/index.ts"
      to: "engine/actions/action-data.ts"
      via: "re-export ACTIONS"
      pattern: "export.*ACTIONS.*from.*action-data"
    - from: "engine/index.ts"
      to: "engine/degree-of-success/degree-of-success.ts"
      via: "re-export degree-of-success module"
      pattern: "export.*from.*degree-of-success"
    - from: "engine/index.ts"
      to: "engine/statistics/statistic.ts"
      via: "re-export Statistic"
      pattern: "export.*Statistic.*from.*statistic"
    - from: "engine/index.ts"
      to: "engine/statistics/creature-statistics.ts"
      via: "re-export CreatureStatistics and buildAttackModifierSets"
      pattern: "export.*CreatureStatistics"
---

<objective>
Wire all Phase 4 modules into the engine barrel export (engine/index.ts) so the complete public API is accessible from a single import. This is the final integration step.

Purpose: Per ARCH-02 and Phase 1 decision D-02, the engine has a single barrel export at engine/index.ts. All Phase 4 additions must be exported here for downstream consumers. No per-subdirectory index.ts files.

Output: engine/index.ts updated with all Phase 4 exports
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-actions-modifier-math/04-CONTEXT.md

<interfaces>
<!-- All Phase 4 modules and their exports, to be wired into barrel -->

From engine/types.ts (Plan 01 expanded):
```typescript
// New type exports:
export type { AbilityKey, CreatureSize, Rarity, CreatureSense, CreatureSpeed, DamageRoll, CreatureAttack }
// Existing (already exported):
export type { WeakEliteTier, Creature }
```

From engine/actions/types.ts (Plan 01):
```typescript
export type { ActionType, ActionCost, ActionCategory, DegreeKey, ActionOutcome, ActionOutcomeMap, Action }
```

From engine/actions/action-data.ts (Plan 02):
```typescript
export { ACTIONS }
```

From engine/actions/action-outcomes.ts (Plan 02):
```typescript
export { ACTION_OUTCOMES }
```

From engine/degree-of-success/degree-of-success.ts (Plan 01):
```typescript
export { calculateDegreeOfSuccess, upgradeDegree, downgradeDegree, INCAPACITATION_ADJUSTMENT, basicSaveDamageMultiplier }
export type { DegreeOfSuccess, DegreeAdjustment }
```

From engine/statistics/statistic.ts (Plan 03):
```typescript
export { Statistic }
```

From engine/statistics/selector-resolver.ts (Plan 03):
```typescript
export { resolveSelector }
```

From engine/statistics/creature-statistics.ts (Plan 03):
```typescript
export { CreatureStatistics, buildAttackModifierSets }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update engine/index.ts barrel export with all Phase 4 modules</name>
  <files>engine/index.ts</files>
  <read_first>
    - engine/index.ts (current barrel — must preserve ALL existing exports and add new ones)
    - engine/types.ts (verify new type exports: AbilityKey, CreatureSize, Rarity, CreatureSense, CreatureSpeed, DamageRoll, CreatureAttack)
    - engine/actions/types.ts (verify exports: ActionType, ActionCost, ActionCategory, DegreeKey, ActionOutcome, ActionOutcomeMap, Action)
    - engine/actions/action-data.ts (verify exports: ACTIONS)
    - engine/actions/action-outcomes.ts (verify exports: ACTION_OUTCOMES)
    - engine/degree-of-success/degree-of-success.ts (verify exports: calculateDegreeOfSuccess, upgradeDegree, downgradeDegree, INCAPACITATION_ADJUSTMENT, basicSaveDamageMultiplier, DegreeOfSuccess, DegreeAdjustment)
    - engine/statistics/statistic.ts (verify exports: Statistic)
    - engine/statistics/selector-resolver.ts (verify exports: resolveSelector)
    - engine/statistics/creature-statistics.ts (verify exports: CreatureStatistics, buildAttackModifierSets)
  </read_first>
  <action>
Update engine/index.ts to add Phase 4 exports. Follow the established barrel-export pattern:
- Merge new exports into existing sections where they fit
- Add new sections for new domains

**Add to the existing Types section (line ~7):**
```typescript
export type { WeakEliteTier, Creature, AbilityKey, CreatureSize, Rarity, CreatureSense, CreatureSpeed, DamageRoll, CreatureAttack } from './types'
```
(Replace the existing `export type { WeakEliteTier, Creature } from './types'` line — extend it with the new types.)

**Add new Actions section after the existing Modifiers section:**
```typescript
// ── Actions ──────────────────────────────────────────────────────────────────
export { ACTIONS } from './actions/action-data'
export { ACTION_OUTCOMES } from './actions/action-outcomes'
export type {
  ActionType,
  ActionCost,
  ActionCategory,
  DegreeKey,
  ActionOutcome,
  ActionOutcomeMap,
  Action,
} from './actions/types'
```

**Add new Degree of Success section:**
```typescript
// ── Degree of Success ────────────────────────────────────────────────────────
export {
  calculateDegreeOfSuccess,
  upgradeDegree,
  downgradeDegree,
  INCAPACITATION_ADJUSTMENT,
  basicSaveDamageMultiplier,
} from './degree-of-success/degree-of-success'
export type {
  DegreeOfSuccess,
  DegreeAdjustment,
} from './degree-of-success/degree-of-success'
```

**Add new Statistics section:**
```typescript
// ── Statistics ───────────────────────────────────────────────────────────────
export { Statistic } from './statistics/statistic'
export { resolveSelector } from './statistics/selector-resolver'
export { CreatureStatistics, buildAttackModifierSets } from './statistics/creature-statistics'
```

**Preserve ALL existing exports unchanged.** The existing sections (Types, Conditions, Damage, Modifiers, Encounter) must remain exactly as they are. Only the Types section is extended with new type names.
  </action>
  <verify>
    <automated>cd D:/pathbuddy && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - engine/index.ts contains `export type { WeakEliteTier, Creature, AbilityKey, CreatureSize, Rarity, CreatureSense, CreatureSpeed, DamageRoll, CreatureAttack } from './types'` (or equivalent with these type names)
    - engine/index.ts contains `export { ACTIONS } from './actions/action-data'`
    - engine/index.ts contains `export { ACTION_OUTCOMES } from './actions/action-outcomes'`
    - engine/index.ts contains `export type {` block with `ActionType`, `ActionCost`, `ActionCategory`, `DegreeKey`, `ActionOutcome`, `ActionOutcomeMap`, `Action`
    - engine/index.ts contains `export { calculateDegreeOfSuccess,` or `export {` block including `calculateDegreeOfSuccess`
    - engine/index.ts contains `export { upgradeDegree,`
    - engine/index.ts contains `export { downgradeDegree,` or same export block
    - engine/index.ts contains `export.*INCAPACITATION_ADJUSTMENT`
    - engine/index.ts contains `export.*basicSaveDamageMultiplier`
    - engine/index.ts contains `export type.*DegreeOfSuccess`
    - engine/index.ts contains `export type.*DegreeAdjustment`
    - engine/index.ts contains `export { Statistic } from './statistics/statistic'`
    - engine/index.ts contains `export { resolveSelector } from './statistics/selector-resolver'`
    - engine/index.ts contains `export { CreatureStatistics, buildAttackModifierSets } from './statistics/creature-statistics'`
    - engine/index.ts still contains all Phase 3 exports: `CONDITION_EFFECTS`, `CONDITION_OVERRIDES`, `performRecoveryCheck`, `ConditionManager`, `CONDITION_SLUGS`, etc.
    - engine/index.ts still contains all pre-Phase 3 exports: `DAMAGE_TYPES`, `applyIWR`, `calculateCreatureXP`, etc.
    - TypeScript compiles with zero errors (`npx tsc --noEmit` exits 0)
    - No circular dependency errors
  </acceptance_criteria>
  <done>All Phase 4 symbols are accessible via `import { ... } from '@engine'`. Complete API surface: actions (ACTIONS, ACTION_OUTCOMES, Action types), degree-of-success (calculateDegreeOfSuccess, adjustments), statistics (Statistic, resolveSelector, CreatureStatistics, buildAttackModifierSets), and expanded Creature types. All existing exports preserved.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits with 0 errors — no circular dependencies, all imports resolve
- engine/index.ts exports every Phase 4 symbol
- engine/index.ts preserves every pre-Phase 4 symbol
- A consumer can `import { ACTIONS, calculateDegreeOfSuccess, CreatureStatistics, Statistic, resolveSelector, buildAttackModifierSets } from '@engine'`
</verification>

<success_criteria>
The engine barrel export provides the complete Phase 4 public API alongside all prior exports. ENG-02 (actions) and ENG-03 (modifier math) are fully accessible. No breaking changes to existing consumers.
</success_criteria>

<output>
After completion, create `.planning/phases/04-actions-modifier-math/04-04-SUMMARY.md`
</output>
