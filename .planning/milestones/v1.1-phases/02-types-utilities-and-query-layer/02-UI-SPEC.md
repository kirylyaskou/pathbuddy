---
phase: 02
slug: types-utilities-and-query-layer
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-20
---

# Phase 02 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Phase UI Surface Assessment

Phase 02 is a zero-UI phase by locked decision (CONTEXT.md — Phase Boundary). The only frontend artifact is a single placeholder view: `CompendiumView.vue` containing one `<h1>` heading. No interactive components, no Pinia store connections, no layout changes.

**UI deliverables in this phase:**
- `src/views/CompendiumView.vue` — one heading, padded container, no interactivity
- `/compendium` route registered in `src/router/index.ts` — no sidebar link

**UI deliverables explicitly deferred to later phases:**
- Sidebar nav link for Compendium — Phase 04
- All Compendium page layout and components — Phase 03–04
- All Combat Workspace panels — Phase 05–06

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — hand-crafted Tailwind design system |
| Preset | not applicable |
| Component library | none (custom Vue components, no shadcn/radix) |
| Icon library | inline SVG (existing pattern — AppSidebar.vue) |
| Font | Cinzel (display/headings), system-ui (body) |

Source: `tailwind.config.js` (direct inspection).

No `components.json` found. shadcn gate not applicable — project uses a bespoke dark fantasy Tailwind design system established in Phase 01.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing |
| md | 16px | Default element spacing — container padding for CompendiumView stub (`p-6` = 24px) |
| lg | 24px | Section padding — `p-6` used in CompendiumView stub (locked in CONTEXT.md) |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions: none for this phase.

Source: 8-point scale default; `p-6` (24px) confirmed as stub padding from CONTEXT.md locked decision and RESEARCH.md CompendiumView skeleton.

---

## Typography

| Role | Size | Weight | Line Height | Font Family |
|------|------|--------|-------------|-------------|
| Body | 14px (`text-sm`) | 400 regular | 1.5 | system-ui |
| Label | 14px (`text-sm`) | 400 regular | 1.5 | system-ui |
| Heading | 24px (`text-2xl`) | 700 bold | 1.2 | system-ui |
| Display | 14px (`text-sm`) | 400 regular | 1.2 | Cinzel (brand/logo only) |

**Phase 02 active usage:** Only the Heading row applies. CompendiumView stub renders `<h1 class="text-2xl font-bold">Compendium</h1>` — size 24px, weight 700, line-height default (1.2).

Source: CONTEXT.md locked decision (`text-2xl font-bold`); existing `AppSidebar.vue` uses `text-sm` body and `font-display` (Cinzel) for brand mark.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#1a1a1a` (`charcoal-800`) | App background, main content area — `AppLayout.vue` `bg-charcoal-800` |
| Secondary (30%) | `#2a2a2a` (`charcoal-600`) | Sidebar, nav surface — `AppSidebar.vue` `bg-charcoal-600` |
| Accent (10%) | `#c9a84c` (`gold`) | Active nav indicator, brand icon, active text — `border-gold`, `text-gold` |
| Destructive | `#8b2020` (`crimson`) | HP damage indicators, condition badges (existing use) — not used in Phase 02 |

Accent reserved for: active sidebar nav border-left indicator, brand shield icon, active nav item text. Not used on the CompendiumView stub heading.

**Phase 02 active usage:** CompendiumView stub heading uses default text color (inherits `text-stone-100` from parent context or unstyled white). No accent or destructive colors appear in the Phase 02 UI surface.

Source: `tailwind.config.js` color tokens; `AppLayout.vue` and `AppSidebar.vue` (direct inspection); Phase 01 design system (established).

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | Not applicable — no interactive actions in Phase 02 |
| Empty state heading | Not applicable — placeholder view only; no data-driven states |
| Empty state body | Not applicable |
| Error state | Not applicable |
| Destructive confirmation | Not applicable — no destructive actions in Phase 02 |

**Phase 02 only copywriting:** The single heading reads "Compendium" — locked in CONTEXT.md (`<h1 class="text-2xl font-bold">Compendium</h1>`).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized |
| third-party | none | not applicable |

No third-party registries. No new component blocks added in this phase. Inline SVG icons follow the existing pattern from `AppSidebar.vue` — no icon library dependency.

---

## CompendiumView Stub — Complete Markup Contract

The full component is specified here so the executor has zero ambiguity:

```vue
<template>
  <div class="p-6">
    <h1 class="text-2xl font-bold">Compendium</h1>
  </div>
</template>
```

No `<script setup>`, no imports, no interactivity. This is the complete component for Phase 02. Any additions belong to Phase 03 or Phase 04.

Source: CONTEXT.md locked decision + RESEARCH.md `CompendiumView.vue minimal stub`.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
