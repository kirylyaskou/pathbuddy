---
phase: 07-remove-addcreatureform
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/AddCreatureForm.vue
  - src/components/__tests__/AddCreatureForm.test.ts
  - src/components/CombatTracker.vue
  - src/components/__tests__/CombatTracker.test.ts
autonomous: true
requirements:
  - WORK-07

must_haves:
  truths:
    - "AddCreatureForm.vue does not exist in the codebase"
    - "No button with text 'Add Creature' is rendered in CombatTracker"
    - "CombatTracker.vue has zero references to AddCreatureForm or formOpen"
    - "All tests pass (349+ tests) with no AddCreatureForm references"
  artifacts:
    - path: "src/components/CombatTracker.vue"
      provides: "Combat tracker toolbar without Add Creature button"
      not_contains: ["AddCreatureForm", "formOpen"]
    - path: "src/components/__tests__/CombatTracker.test.ts"
      provides: "Test suite with negative assertion for Add Creature button"
      contains: "no Add Creature button is rendered"
  key_links:
    - from: "CombatAddBar.vue"
      to: "combatStore.addCreature"
      via: "existing Phase 05 wiring (unchanged)"
      pattern: "combatStore\\.addCreature"
---

<objective>
Delete the superseded AddCreatureForm component and all references. The creature browser (CombatAddBar + CreatureBrowser in combat mode) is the sole entry point for adding creatures to combat since Phase 05.

Purpose: Clean up dead code — AddCreatureForm has been superseded since Phase 05, and its continued presence is confusing. WORK-07 requires its removal.
Output: Two files deleted, two files surgically edited, full test suite green.
</objective>

<execution_context>
@C:/Users/kiryl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kiryl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-remove-addcreatureform/07-CONTEXT.md
@.planning/phases/07-remove-addcreatureform/07-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- No new interfaces created. This is a deletion-only plan. -->

From src/components/CombatTracker.vue (current state — lines to remove):
```
Line 6:  import AddCreatureForm from './AddCreatureForm.vue'
Line 17: const formOpen = ref(false)
Lines 106-112: <!-- Add Creature button (primary CTA) --> block
Line 154: <AddCreatureForm v-model="formOpen" @add="combatStore.addCreature" />
```

From src/components/__tests__/CombatTracker.test.ts (current state — block to remove):
```
Lines 46-65: it('+ Add Creature button opens form', async () => { ... })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete AddCreatureForm files and clean CombatTracker.vue</name>
  <read_first>
    - src/components/CombatTracker.vue
    - src/components/AddCreatureForm.vue
  </read_first>
  <files>
    src/components/AddCreatureForm.vue (DELETE)
    src/components/__tests__/AddCreatureForm.test.ts (DELETE)
    src/components/CombatTracker.vue (EDIT)
  </files>
  <action>
1. Delete `src/components/AddCreatureForm.vue` entirely (180 lines).
2. Delete `src/components/__tests__/AddCreatureForm.test.ts` entirely (84 lines).
3. Edit `src/components/CombatTracker.vue` — make these four surgical removals:

   a. **Remove line 6** — the import:
      ```
      import AddCreatureForm from './AddCreatureForm.vue'
      ```

   b. **Remove line 17** — the formOpen ref:
      ```
      const formOpen = ref(false)
      ```

   c. **Remove lines 106-112** — the entire Add Creature button block (comment + button element):
      ```
      <!-- Add Creature button (primary CTA) -->
      <button
        @click="formOpen = true"
        class="px-3 py-1.5 rounded bg-gold hover:bg-gold-light text-charcoal-950 text-sm font-bold transition-colors"
      >
        + Add Creature
      </button>
      ```

   d. **Remove line 154** — the component usage:
      ```
      <AddCreatureForm v-model="formOpen" @add="combatStore.addCreature" />
      ```

4. **KEEP** the `import { ref, computed } from 'vue'` on line 2 — `ref` is still used by `dragOverCreatureId` on line 55. Do NOT remove the ref import.

5. **KEEP** the empty state text "Add a creature to begin tracking initiative and HP." — it remains accurate for the browser flow.

The toolbar right-side `<div class="flex items-center gap-3">` will contain three children after removal: Round badge, New Round button, Next Creature button. The gap-3 flex layout handles spacing automatically — no class changes needed.
  </action>
  <verify>
    <automated>cd D:/pathbuddy && npx vitest run src/components/__tests__/CombatTracker.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/AddCreatureForm.vue` does not exist
    - File `src/components/__tests__/AddCreatureForm.test.ts` does not exist
    - `src/components/CombatTracker.vue` does NOT contain the string "AddCreatureForm"
    - `src/components/CombatTracker.vue` does NOT contain the string "formOpen"
    - `src/components/CombatTracker.vue` does NOT contain the string "+ Add Creature"
    - `src/components/CombatTracker.vue` DOES contain `import { ref, computed } from 'vue'`
    - `src/components/CombatTracker.vue` DOES contain `const dragOverCreatureId = ref`
    - `src/components/CombatTracker.vue` DOES contain "Add a creature to begin tracking initiative and HP"
    - CombatTracker.test.ts tests pass (existing tests minus the removed one)
  </acceptance_criteria>
  <done>AddCreatureForm component and test file deleted. CombatTracker.vue has zero references to AddCreatureForm or formOpen. The ref import is preserved for dragOverCreatureId. Existing CombatTracker tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update CombatTracker tests and run full suite</name>
  <read_first>
    - src/components/__tests__/CombatTracker.test.ts
  </read_first>
  <files>
    src/components/__tests__/CombatTracker.test.ts (EDIT)
  </files>
  <action>
1. **Remove** the entire test block at lines 46-65 (the `it('+ Add Creature button opens form', async () => { ... })` block). Remove from line 46 (`it('+ Add Creature button opens form',`) through line 65 (closing `})`), inclusive.

2. **Add** the following new test case in the same location (after the "displays creature list when creatures exist" test, before the "has exactly one Next Creature button" test):

```typescript
  it('no Add Creature button is rendered', () => {
    const wrapper = mount(CombatTracker, {
      global: {
        plugins: [pinia],
      },
    })

    const buttons = wrapper.findAll('button')
    const addButton = buttons.find(b => b.text().includes('Add Creature'))
    expect(addButton).toBeUndefined()
  })
```

This test:
- Is synchronous (no async/await) because CombatTracker renders without async data loading
- Uses the `buttons.find(...)` pattern consistent with other tests in this file
- Asserts `.toBeUndefined()` because `Array.prototype.find()` returns `undefined` when no match

3. Run the full test suite: `npx vitest run`

All 349+ tests must pass. The count will drop by 4 (AddCreatureForm.test.ts had 4 tests) and increase by 1 (new negative assertion), netting approximately 346 tests.
  </action>
  <verify>
    <automated>cd D:/pathbuddy && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/__tests__/CombatTracker.test.ts` does NOT contain the string "opens form"
    - `src/components/__tests__/CombatTracker.test.ts` does NOT contain the string "AddCreatureForm"
    - `src/components/__tests__/CombatTracker.test.ts` DOES contain `it('no Add Creature button is rendered'`
    - `src/components/__tests__/CombatTracker.test.ts` DOES contain `expect(addButton).toBeUndefined()`
    - `npx vitest run` exits with code 0 (all tests pass)
    - No test file in `src/__tests__/` or `src/components/__tests__/` references "AddCreatureForm"
  </acceptance_criteria>
  <done>CombatTracker.test.ts has the old "opens form" test removed and a new "no Add Creature button is rendered" test added. The full test suite passes with no AddCreatureForm references anywhere in the test codebase.</done>
</task>

</tasks>

<verification>
1. `ls src/components/AddCreatureForm.vue` returns "No such file"
2. `ls src/components/__tests__/AddCreatureForm.test.ts` returns "No such file"
3. `grep -r "AddCreatureForm" src/` returns no matches
4. `grep -r "formOpen" src/` returns no matches
5. `npx vitest run` — full suite green, ~346 tests pass
</verification>

<success_criteria>
- AddCreatureForm.vue and its test file are deleted from the codebase
- CombatTracker.vue has zero references to AddCreatureForm or formOpen
- A new test confirms no "Add Creature" button exists in CombatTracker
- Full test suite passes with no regressions
- WORK-07 requirement is satisfied: creature browser is the sole entry point for adding to combat
</success_criteria>

<output>
After completion, create `.planning/phases/07-remove-addcreatureform/07-01-SUMMARY.md`
</output>
