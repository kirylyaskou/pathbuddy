# Quick Task 260405-64b Summary

## What was done

Replaced the narrow condition picker Popover (w-80) with a Dialog modal (sm:max-w-lg), giving each condition pill ~156px in the 3-column grid instead of ~97px. Removed `truncate` from `ConditionPill` and switched to `break-words` so full condition names are always visible. Increased grid `max-h-48` → `max-h-64`. Polished the empty state in `ConditionSection` from floating plain text to a dashed-border box.

## Files changed

- `src/features/combat-tracker/ui/ConditionCombobox.tsx` — Popover → Dialog, full pill names
- `src/widgets/combatant-detail/ui/ConditionSection.tsx` — empty state dashed box

## Commit

96ed79ce
