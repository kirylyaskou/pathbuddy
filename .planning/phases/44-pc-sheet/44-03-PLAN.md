---
plan: "03"
wave: 3
depends_on: ["02"]
files_modified:
  - src/features/characters/ui/PCSheetPanel.tsx
autonomous: true
requirements_addressed:
  - SHEET-04
  - SHEET-05
  - SHEET-06
must_haves:
  - Spells tab shows per-caster blocks (tradition · type header), spells grouped by spell level with level label (Cantrips / 1st Level / 2nd Level / ...), Focus entries shown with "Focus" badge; empty state "No spellcasting" when build.spellCasters is empty
  - Feats tab shows two sub-sections "Feats" and "Class Features", each feat displays name (semibold) + meta line "type · level" + optional note (italic); empty state "No feats recorded"
  - Notes tab has Textarea (resize-none, min-h-[200px], placeholder "DM notes for this character..."), saves to SQLite on onBlur via updateCharacterNotes(character.id, value)
---

# Phase 44 — Plan 03: Spells + Feats + Notes Tabs

## Objective

Replace stub functions `SpellsContent`, `FeatsContent`, `NotesContent` in `PCSheetPanel.tsx` with complete implementations.

Covers SHEET-04 (spellcasting reference), SHEET-05 (feats & class features), SHEET-06 (DM notes persistence).

<tasks>

<task id="T1" title="Implement SpellsContent — caster blocks with spells grouped by level">
<action>
Edit `src/features/characters/ui/PCSheetPanel.tsx`:

Find the entire stub:
```tsx
function SpellsContent({ build: _build }: { build: PathbuilderBuild }) {
  return <div className="text-sm text-muted-foreground">Spells — stub</div>
}
```

Replace with the complete implementation:

```tsx
function SpellsContent({ build }: { build: PathbuilderBuild }) {
  const casters = build.spellCasters ?? []

  if (casters.length === 0) {
    return (
      <div className="flex items-center justify-center text-sm text-muted-foreground py-8">
        No spellcasting
      </div>
    )
  }

  const spellLevelLabel = (lvl: number): string => {
    if (lvl === 0) return 'Cantrips'
    if (lvl === 1) return '1st Level'
    if (lvl === 2) return '2nd Level'
    if (lvl === 3) return '3rd Level'
    return `${lvl}th Level`
  }

  return (
    <div className="space-y-6">
      {casters.map((caster, i) => {
        const isFocus = caster.spellcastingType === 'focus'
        const tradition =
          caster.magicTradition.charAt(0).toUpperCase() + caster.magicTradition.slice(1)
        const type = isFocus
          ? 'Focus'
          : caster.spellcastingType.charAt(0).toUpperCase() + caster.spellcastingType.slice(1)

        return (
          <div key={i}>
            <div className="flex items-center gap-2 mb-2">
              <h3 className="text-base font-semibold">
                {tradition} · {type}
              </h3>
              {isFocus && (
                <span className="text-xs font-semibold px-1.5 py-0.5 rounded-sm bg-pf-rarity-rare/15 text-pf-rarity-rare">
                  Focus
                </span>
              )}
            </div>
            <div className="space-y-3">
              {(caster.spells ?? []).map((group) => (
                <div key={group.spellLevel}>
                  <p className="text-xs text-muted-foreground mb-1">
                    {spellLevelLabel(group.spellLevel)}
                  </p>
                  <div className="space-y-0.5 pl-2">
                    {group.list.map((spellName) => (
                      <div key={spellName} className="text-sm">
                        {spellName}
                      </div>
                    ))}
                  </div>
                </div>
              ))}
            </div>
          </div>
        )
      })}
    </div>
  )
}
```
</action>
<read_first>
- src/features/characters/ui/PCSheetPanel.tsx — read full file to find SpellsContent stub
- engine/pc/types.ts — verify PathbuilderSpellEntry fields (magicTradition, spellcastingType, spells array structure with spellLevel + list)
</read_first>
<acceptance_criteria>
- `grep "Spells — stub" src/features/characters/ui/PCSheetPanel.tsx` returns 0 matches (stub replaced)
- `grep "No spellcasting" src/features/characters/ui/PCSheetPanel.tsx` returns a match (empty state)
- `grep "spellLevelLabel" src/features/characters/ui/PCSheetPanel.tsx` returns matches (level label helper)
- `grep "pf-rarity-rare/15" src/features/characters/ui/PCSheetPanel.tsx` returns a match (Focus badge)
- `grep "magicTradition\|spellcastingType" src/features/characters/ui/PCSheetPanel.tsx` returns matches
</acceptance_criteria>
</task>

<task id="T2" title="Implement FeatsContent — Feats and Class Features sub-sections">
<action>
Edit `src/features/characters/ui/PCSheetPanel.tsx`:

Find the entire stub:
```tsx
function FeatsContent({ build: _build }: { build: PathbuilderBuild }) {
  return <div className="text-sm text-muted-foreground">Feats — stub</div>
}
```

Replace with the complete implementation:

```tsx
function FeatsContent({ build }: { build: PathbuilderBuild }) {
  const feats = build.feats ?? []
  const specials = build.specials ?? []

  if (feats.length === 0 && specials.length === 0) {
    return (
      <div className="flex items-center justify-center text-sm text-muted-foreground py-8">
        No feats recorded
      </div>
    )
  }

  return (
    <div className="space-y-6">

      {feats.length > 0 && (
        <div>
          <h3 className="text-base font-semibold mb-2">Feats</h3>
          <div className="divide-y divide-border/50">
            {feats.map(([name, , type, level, note], i) => (
              <div key={i} className="py-1.5">
                <p className="text-sm font-semibold">{name}</p>
                <p className="text-xs text-muted-foreground">
                  {type} · {level}
                </p>
                {note && (
                  <p className="text-xs text-muted-foreground italic pl-2 mt-0.5">{note}</p>
                )}
              </div>
            ))}
          </div>
        </div>
      )}

      {specials.length > 0 && (
        <div>
          <h3 className="text-base font-semibold mb-2">Class Features</h3>
          <div className="divide-y divide-border/50">
            {specials.map(([name, level, note], i) => (
              <div key={i} className="py-1.5">
                <p className="text-sm font-semibold">{name}</p>
                <p className="text-xs text-muted-foreground">Level {level}</p>
                {note && (
                  <p className="text-xs text-muted-foreground italic pl-2 mt-0.5">{note}</p>
                )}
              </div>
            ))}
          </div>
        </div>
      )}

    </div>
  )
}
```
</action>
<read_first>
- src/features/characters/ui/PCSheetPanel.tsx — read full file to find FeatsContent stub
- engine/pc/types.ts — verify PathbuilderBuild.feats type (Array<[name, source, type, level, note]>) and .specials type (Array<[name, level, note]>)
</read_first>
<acceptance_criteria>
- `grep "Feats — stub" src/features/characters/ui/PCSheetPanel.tsx` returns 0 matches (stub replaced)
- `grep "No feats recorded" src/features/characters/ui/PCSheetPanel.tsx` returns a match (empty state)
- `grep "Class Features" src/features/characters/ui/PCSheetPanel.tsx` returns a match (sub-section heading)
- `grep "divide-border/50" src/features/characters/ui/PCSheetPanel.tsx` returns matches (row separators)
</acceptance_criteria>
</task>

<task id="T3" title="Implement NotesContent — editable Textarea that persists on blur">
<action>
Edit `src/features/characters/ui/PCSheetPanel.tsx`:

**Step 1 — Add imports.** Modify the React import:
  Change: `import { useMemo } from 'react'`
  To: `import { useState, useMemo } from 'react'`

Add these two import lines after the existing import block:
```ts
import { Textarea } from '@/shared/ui/textarea'
import { updateCharacterNotes } from '@/shared/api/characters'
```

**Step 2 — Replace NotesContent stub.** Find the entire stub:
```tsx
function NotesContent({ character: _character }: { character: CharacterRecord }) {
  return <div className="text-sm text-muted-foreground">Notes — stub</div>
}
```

Replace with the complete implementation:

```tsx
function NotesContent({ character }: { character: CharacterRecord }) {
  const [value, setValue] = useState(character.notes ?? '')

  async function handleBlur() {
    await updateCharacterNotes(character.id, value)
  }

  return (
    <div>
      <Textarea
        className="w-full resize-none min-h-[200px] p-2 text-sm"
        placeholder="DM notes for this character..."
        value={value}
        onChange={(e) => setValue(e.target.value)}
        onBlur={handleBlur}
      />
    </div>
  )
}
```
</action>
<read_first>
- src/features/characters/ui/PCSheetPanel.tsx — read full file to find NotesContent stub and existing React import
- src/shared/ui/textarea.tsx — verify Textarea export
- src/shared/api/characters.ts — verify updateCharacterNotes(id: string, notes: string) signature
</read_first>
<acceptance_criteria>
- `grep "Notes — stub" src/features/characters/ui/PCSheetPanel.tsx` returns 0 matches (stub replaced)
- `grep "useState" src/features/characters/ui/PCSheetPanel.tsx` returns matches (import + usage in NotesContent)
- `grep "updateCharacterNotes" src/features/characters/ui/PCSheetPanel.tsx` returns at least 2 matches (import + usage)
- `grep "onBlur" src/features/characters/ui/PCSheetPanel.tsx` returns a match (save trigger)
- `grep "DM notes for this character" src/features/characters/ui/PCSheetPanel.tsx` returns a match (placeholder text)
- `grep "resize-none" src/features/characters/ui/PCSheetPanel.tsx` returns a match
</acceptance_criteria>
</task>

</tasks>

## Verification

After all 3 tasks complete:

```bash
# All stubs replaced
grep "stub" src/features/characters/ui/PCSheetPanel.tsx
# → 0 matches

# All empty states present
grep "No spellcasting\|No feats recorded\|DM notes for this character" src/features/characters/ui/PCSheetPanel.tsx
# → 3 matches

# Persistence wired
grep "updateCharacterNotes\|onBlur" src/features/characters/ui/PCSheetPanel.tsx
# → 2+ matches

# All required imports present
grep "useState\|Textarea\|updateCharacterNotes\|calculatePCMaxHP" src/features/characters/ui/PCSheetPanel.tsx
# → 4 matches

# SHEET-04: spellcasting renders
grep "spellLevelLabel\|magicTradition" src/features/characters/ui/PCSheetPanel.tsx
# → 2 matches

# SHEET-05: feats + class features
grep "Class Features\|divide-border/50" src/features/characters/ui/PCSheetPanel.tsx
# → 2 matches

# SHEET-06: notes save on blur
grep "handleBlur\|updateCharacterNotes" src/features/characters/ui/PCSheetPanel.tsx
# → 2 matches
```

## Phase 44 Final Verification (run after all 3 plans complete)

All 6 success criteria from ROADMAP.md must be TRUE:

1. **SHEET-01** Core stats: `grep "calculatePCMaxHP\|acTotal\|attributes.speed" src/features/characters/ui/PCSheetPanel.tsx` → matches
2. **SHEET-02** Skills: `grep "rankLabel\|modWithProf\|SKILL_ABILITY" src/features/characters/ui/PCSheetPanel.tsx` → matches
3. **SHEET-03** Equipment: `grep "inventoryByCategory\|runes.join\|pot > 0" src/features/characters/ui/PCSheetPanel.tsx` → matches
4. **SHEET-04** Spells: `grep "spellCasters\|spellLevelLabel" src/features/characters/ui/PCSheetPanel.tsx` → matches
5. **SHEET-05** Feats: `grep "Class Features\|build.feats\|build.specials" src/features/characters/ui/PCSheetPanel.tsx` → matches
6. **SHEET-06** Notes: `grep "updateCharacterNotes\|onBlur" src/features/characters/ui/PCSheetPanel.tsx` → matches

Integration check:
- `grep "PCSheetPanel\|selectedCharacter" src/pages/characters/ui/CharactersPage.tsx` → matches (panel wired to CharactersPage)
- `grep "onView\|stopPropagation" src/features/characters/ui/CharacterCard.tsx` → matches (card clickable)
