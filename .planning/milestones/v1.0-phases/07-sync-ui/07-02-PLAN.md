---
phase: 07-sync-ui
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/views/DashboardView.vue
autonomous: false
requirements: [SYNCUI-01, SYNCUI-02, SYNCUI-03]

must_haves:
  truths:
    - "DashboardView renders SyncButton component instead of static placeholder tile"
    - "Combat Tracker tile remains alongside SyncButton in the grid"
    - "Full sync flow works end-to-end in running app"
  artifacts:
    - path: "src/views/DashboardView.vue"
      provides: "Dashboard with live SyncButton replacing disabled placeholder"
      contains: "SyncButton"
  key_links:
    - from: "src/views/DashboardView.vue"
      to: "src/components/SyncButton.vue"
      via: "import SyncButton + <SyncButton /> in template"
      pattern: "import SyncButton from.*SyncButton.vue"
---

<objective>
Wire SyncButton.vue into DashboardView.vue, replacing the disabled "Sync Data" placeholder tile. Then visually verify the full sync flow in the running app.

Purpose: Completes Phase 7 by integrating the SyncButton into the dashboard where users will interact with it.
Output: Updated src/views/DashboardView.vue
</objective>

<execution_context>
@C:/Users/kiryl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kiryl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-sync-ui/07-CONTEXT.md
@.planning/phases/07-sync-ui/07-01-SUMMARY.md

@src/views/DashboardView.vue
@src/components/SyncButton.vue

<interfaces>
From src/components/SyncButton.vue (created in Plan 01):
- Standalone component, no props needed
- Self-contained: reads DB, calls syncPacks, manages own state
- Usage: `<SyncButton />` — drop-in replacement for static tile
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace static Sync Data tile with SyncButton component in DashboardView</name>
  <files>src/views/DashboardView.vue</files>
  <read_first>
    src/views/DashboardView.vue
    src/components/SyncButton.vue
  </read_first>
  <action>
Modify `src/views/DashboardView.vue`:

1. **Add import** in `<script setup lang="ts">`:
```typescript
import { RouterLink } from 'vue-router';
import SyncButton from '@/components/SyncButton.vue'
```

2. **Replace lines 22-30** (the static disabled "Sync Data" div):
```html
<!-- REMOVE THIS: -->
<!-- Sync Data tile (not a link, coming soon) -->
<div class="bg-white rounded-lg shadow-md p-4 opacity-60 cursor-default">
  <h2 class="text-xl font-bold text-gray-800">Sync Data</h2>
  <p class="text-sm text-gray-500 mt-2">
    Import Pathfinder 2e content from the official data source. (Coming soon)
  </p>
</div>

<!-- REPLACE WITH: -->
<SyncButton />
```

3. **Keep everything else unchanged** — the grid layout (`grid-cols-1 sm:grid-cols-2 gap-4`), the Combat Tracker RouterLink tile, and all surrounding structure remain as-is.

The existing grid layout naturally places SyncButton alongside Combat Tracker. SyncButton.vue already includes its own `bg-white rounded-lg shadow-md p-4` wrapper to match the dashboard tile style.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - DashboardView.vue contains `import SyncButton from '@/components/SyncButton.vue'`
    - DashboardView.vue template contains `<SyncButton` tag
    - DashboardView.vue does NOT contain "Coming soon" text
    - DashboardView.vue does NOT contain `opacity-60 cursor-default` (the old disabled styling)
    - DashboardView.vue still contains `RouterLink to="/combat"` (Combat Tracker tile preserved)
    - `npx vitest run` passes (full suite green)
  </acceptance_criteria>
  <done>DashboardView renders SyncButton in place of the static placeholder, Combat Tracker tile unchanged, full test suite passes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete Sync UI flow</name>
  <files>src/views/DashboardView.vue, src/components/SyncButton.vue</files>
  <action>
Human visually verifies the complete sync flow in the running Tauri app. No code changes needed — this is a verification checkpoint.
  </action>
  <what-built>Complete Sync UI: SyncButton component wired into Dashboard, replacing the placeholder tile. The component shows version info, sync progress with stages and progress bar, error handling with retry.</what-built>
  <how-to-verify>
    1. Run `npm run tauri dev` (or however you start the app)
    2. On the Dashboard, verify:
       a. The "Sync Data" tile is no longer grayed out / disabled
       b. SyncButton shows either version info (if previously synced) or "No PF2e data yet" prompt
       c. Combat Tracker tile still appears alongside
    3. Click "Sync Now" button:
       a. Button should disable (show "Syncing...")
       b. Stage pipeline should show progress through stages with checkmarks
       c. During importing stage: progress bar should fill based on entity count
       d. After completion: result summary should appear briefly, then settle to version display
    4. (Optional) Test error handling: disconnect internet, try syncing — should show inline error with retry button
    5. Verify the visual progress bar animation (indeterminate pulse for non-importing stages, determinate fill for importing)
  </how-to-verify>
  <verify>Human confirms sync flow works visually in running app</verify>
  <done>User approves the visual sync flow, progress bar, stage pipeline, error handling, and version display</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run` — full test suite passes
- DashboardView imports and renders SyncButton
- Static placeholder tile fully removed
- Human verifies visual sync flow in running app
</verification>

<success_criteria>
- DashboardView.vue renders `<SyncButton />` instead of static disabled tile
- Combat Tracker tile preserved in grid
- Full sync flow visually confirmed by user
- All tests remain green
</success_criteria>

<output>
After completion, create `.planning/phases/07-sync-ui/07-02-SUMMARY.md`
</output>
