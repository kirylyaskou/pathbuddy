---
phase: 03-conditions-statuses
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/types.ts
  - engine/conditions/condition-effects.ts
  - engine/damage/damage.ts
  - engine/damage/iwr.ts
autonomous: true
requirements: [ENG-01]

must_haves:
  truths:
    - "Creature interface exists with immunities, conditions, hp, level, deathDoor fields"
    - "CONDITION_EFFECTS map covers all 15 conditions with FlatModifier effects and all 5 GrantItem chains"
    - "CONDITION_OVERRIDES map covers blinded->dazzled, stunned->slowed, and all attitude overrides"
    - "IMMUNITY_TYPES includes all 49 new condition+effect immunity strings"
    - "WEAKNESS_TYPES includes all 17 new weakness strings"
    - "RESISTANCE_TYPES includes all 14 new resistance strings including all-damage"
    - "holy and unholy are valid DamageType values categorized as other"
    - "all-damage resistance matches any damage type in typeMatches()"
  artifacts:
    - path: "engine/types.ts"
      provides: "Creature interface"
      contains: "export interface Creature"
    - path: "engine/conditions/condition-effects.ts"
      provides: "Condition effect data map, override map, effect types"
      exports: ["CONDITION_EFFECTS", "CONDITION_OVERRIDES", "EXCLUSIVE_GROUPS"]
    - path: "engine/damage/damage.ts"
      provides: "holy and unholy damage types"
      contains: "holy"
    - path: "engine/damage/iwr.ts"
      provides: "Expanded IWR type arrays, all-damage handling"
      contains: "all-damage"
  key_links:
    - from: "engine/conditions/condition-effects.ts"
      to: "engine/conditions/conditions.ts"
      via: "ConditionSlug type import"
      pattern: "import.*ConditionSlug.*from.*conditions"
    - from: "engine/types.ts"
      to: "engine/damage/iwr.ts"
      via: "Immunity type import for Creature interface"
      pattern: "import.*Immunity.*from.*iwr"
    - from: "engine/damage/iwr.ts"
      to: "engine/damage/damage.ts"
      via: "DAMAGE_TYPES spread in IWR arrays"
      pattern: "DAMAGE_TYPES"
---

<objective>
Create all type definitions, data maps, and type array expansions that Phase 3 behavioral code (Plans 02 and 03) will consume. This is the "contracts first" plan: no behavioral changes, only new types, constants, and data structures.

Purpose: Plans 02 and 03 implement behavior against these contracts in parallel. By establishing all data first, executors have concrete types and maps to code against without interpretation.

Output: engine/types.ts (Creature interface), engine/conditions/condition-effects.ts (new file with effect map + override map), engine/damage/damage.ts (holy/unholy), engine/damage/iwr.ts (expanded type arrays + all-damage).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-conditions-statuses/03-CONTEXT.md
@.planning/phases/03-conditions-statuses/03-RESEARCH.md

@engine/types.ts
@engine/conditions/conditions.ts
@engine/damage/damage.ts
@engine/damage/iwr.ts
@engine/index.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From engine/conditions/conditions.ts:
```typescript
export const CONDITION_SLUGS = [
  'blinded', 'broken', 'clumsy', 'concealed', 'confused', 'controlled',
  'cursebound', 'dazzled', 'deafened', 'doomed', 'drained', 'dying',
  'encumbered', 'enfeebled', 'fascinated', 'fatigued', 'fleeing', 'friendly',
  'frightened', 'grabbed', 'helpful', 'hidden', 'hostile', 'immobilized',
  'indifferent', 'invisible', 'malevolence', 'observed', 'off-guard',
  'paralyzed', 'persistent-damage', 'petrified', 'prone', 'quickened',
  'restrained', 'sickened', 'slowed', 'stunned', 'stupefied', 'unconscious',
  'undetected', 'unfriendly', 'unnoticed', 'wounded',
] as const
export type ConditionSlug = (typeof CONDITION_SLUGS)[number]
```

From engine/damage/damage.ts:
```typescript
export const OTHER_DAMAGE_TYPES = ['spirit', 'mental', 'poison', 'untyped'] as const
export type OtherDamageType = (typeof OTHER_DAMAGE_TYPES)[number]
export const DAMAGE_TYPES = [...PHYSICAL_DAMAGE_TYPES, ...ENERGY_DAMAGE_TYPES, ...OTHER_DAMAGE_TYPES] as const
export type DamageType = PhysicalDamageType | EnergyDamageType | OtherDamageType
export const DAMAGE_TYPE_CATEGORY: Record<DamageType, DamageCategory> = { ... }
```

From engine/damage/iwr.ts:
```typescript
export const IMMUNITY_TYPES = [...DAMAGE_TYPES, ...DAMAGE_CATEGORIES, 'critical-hits', 'precision'] as const
export type ImmunityType = (typeof IMMUNITY_TYPES)[number]
export const WEAKNESS_TYPES = [...DAMAGE_TYPES, ...DAMAGE_CATEGORIES] as const
export type WeaknessType = (typeof WEAKNESS_TYPES)[number]
export const RESISTANCE_TYPES = [...DAMAGE_TYPES, ...DAMAGE_CATEGORIES] as const
export type ResistanceType = (typeof RESISTANCE_TYPES)[number]
export interface Immunity { readonly type: ImmunityType; readonly exceptions: DamageType[] }
```

From engine/modifiers/modifiers.ts:
```typescript
export const MODIFIER_TYPES = ['ability', 'circumstance', 'item', 'potency', 'proficiency', 'status', 'untyped'] as const
export type ModifierType = (typeof MODIFIER_TYPES)[number]
```

From engine/types.ts:
```typescript
export type WeakEliteTier = 'normal' | 'weak' | 'elite'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add holy/unholy damage types and expand IWR type arrays with all-damage handling</name>
  <files>engine/damage/damage.ts, engine/damage/iwr.ts</files>
  <read_first>engine/damage/damage.ts, engine/damage/iwr.ts, engine/damage/damage-helpers.ts</read_first>
  <action>
**In engine/damage/damage.ts:**

1. Add `'holy'` and `'unholy'` to the `OTHER_DAMAGE_TYPES` array (per D-23). The array becomes:
```typescript
export const OTHER_DAMAGE_TYPES = ['spirit', 'mental', 'poison', 'untyped', 'holy', 'unholy'] as const
```

2. Add the two new entries to `DAMAGE_TYPE_CATEGORY`:
```typescript
holy: 'other',
unholy: 'other',
```

No other changes to damage.ts. The `DamageType` union auto-derives from `DAMAGE_TYPES` via `as const`.

**In engine/damage/iwr.ts:**

1. Add two new sub-arrays before IMMUNITY_TYPES for organizational clarity, then spread them into IMMUNITY_TYPES (per D-20). Create these arrays:

```typescript
export const CONDITION_IMMUNITY_TYPES = [
  'blinded', 'clumsy', 'confused', 'controlled', 'dazzled', 'deafened',
  'doomed', 'drained', 'enfeebled', 'fascinated', 'fatigued', 'grabbed',
  'immobilized', 'off-guard', 'paralyzed', 'persistent-damage', 'petrified',
  'prone', 'restrained', 'sickened', 'slowed', 'stunned', 'stupefied', 'unconscious',
] as const

export const EFFECT_IMMUNITY_TYPES = [
  'auditory', 'curse', 'death-effects', 'disease', 'emotion', 'fear-effects',
  'fortune-effects', 'healing', 'illusion', 'inhaled', 'light', 'magic',
  'misfortune-effects', 'nonlethal-attacks', 'object-immunities', 'olfactory',
  'polymorph', 'possession', 'radiation', 'scrying', 'sleep', 'spell-deflection',
  'swarm-attacks', 'swarm-mind', 'visual',
] as const
```

2. Rewrite IMMUNITY_TYPES to spread all sub-arrays:
```typescript
export const IMMUNITY_TYPES = [
  ...DAMAGE_TYPES,
  ...DAMAGE_CATEGORIES,
  'critical-hits',
  'precision',
  ...CONDITION_IMMUNITY_TYPES,
  ...EFFECT_IMMUNITY_TYPES,
] as const
```

3. Add missing weakness type strings to WEAKNESS_TYPES (per D-21). The existing spread of DAMAGE_TYPES and DAMAGE_CATEGORIES stays; append the 17 new strings:
```typescript
export const WEAKNESS_TYPES = [
  ...DAMAGE_TYPES,
  ...DAMAGE_CATEGORIES,
  'alchemical', 'area-damage', 'axe-vulnerability', 'cold-iron', 'earth',
  'orichalcum', 'peachwood', 'salt', 'salt-water', 'silver', 'splash-damage',
  'vorpal', 'vulnerable-to-sunlight', 'water', 'air',
] as const
```
Note: `holy` and `unholy` are already in DAMAGE_TYPES (added in damage.ts), so they flow in via the spread. Do NOT duplicate them as explicit strings.

4. Add missing resistance type strings to RESISTANCE_TYPES (per D-22):
```typescript
export const RESISTANCE_TYPES = [
  ...DAMAGE_TYPES,
  ...DAMAGE_CATEGORIES,
  'all-damage', 'critical-hits', 'earth', 'metal', 'mythic', 'plant',
  'precision', 'protean-anatomy', 'silver', 'spells', 'water', 'wood', 'air',
] as const
```
Note: `unholy` is already in DAMAGE_TYPES via spread. Do NOT duplicate.

5. Add `all-damage` early return in `typeMatches()` (per D-24). Add as the FIRST line of the function body:
```typescript
function typeMatches(iwrType: ImmunityType | WeaknessType | ResistanceType, instance: DamageInstance): boolean {
  if (iwrType === 'all-damage') return true
  return (
    iwrType === instance.type ||
    iwrType === DamageCategorization.getCategory(instance.type)
  )
}
```

6. Export the two new sub-array types for downstream consumers:
```typescript
export type ConditionImmunityType = (typeof CONDITION_IMMUNITY_TYPES)[number]
export type EffectImmunityType = (typeof EFFECT_IMMUNITY_TYPES)[number]
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - engine/damage/damage.ts contains `'holy', 'unholy'` in OTHER_DAMAGE_TYPES array
    - engine/damage/damage.ts contains `holy: 'other'` in DAMAGE_TYPE_CATEGORY
    - engine/damage/damage.ts contains `unholy: 'other'` in DAMAGE_TYPE_CATEGORY
    - engine/damage/iwr.ts contains `export const CONDITION_IMMUNITY_TYPES`
    - engine/damage/iwr.ts contains `export const EFFECT_IMMUNITY_TYPES`
    - engine/damage/iwr.ts contains `...CONDITION_IMMUNITY_TYPES` inside IMMUNITY_TYPES
    - engine/damage/iwr.ts contains `...EFFECT_IMMUNITY_TYPES` inside IMMUNITY_TYPES
    - engine/damage/iwr.ts contains `'all-damage'` in RESISTANCE_TYPES
    - engine/damage/iwr.ts contains `'cold-iron'` in WEAKNESS_TYPES
    - engine/damage/iwr.ts contains `'silver'` in WEAKNESS_TYPES
    - engine/damage/iwr.ts contains `if (iwrType === 'all-damage') return true` in typeMatches
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>holy/unholy are valid DamageType values. IMMUNITY_TYPES includes 49 new condition+effect strings. WEAKNESS_TYPES includes 15 new strings. RESISTANCE_TYPES includes 13 new strings plus all-damage. typeMatches returns true for all-damage. TypeScript compiles with zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create condition-effects.ts with CONDITION_EFFECTS map, CONDITION_OVERRIDES map, and group metadata</name>
  <files>engine/conditions/condition-effects.ts</files>
  <read_first>engine/conditions/conditions.ts, engine/modifiers/modifiers.ts, .planning/phases/03-conditions-statuses/03-RESEARCH.md</read_first>
  <action>
Create a NEW file `engine/conditions/condition-effects.ts`. This is the data-driven condition effect map (per D-01). It contains:

1. **Type imports:**
```typescript
import type { ConditionSlug } from './conditions'
import type { ModifierType } from '../modifiers/modifiers'
```

2. **ConditionSelector type** (per D-03):
```typescript
export type ConditionSelector = string | string[]
```

3. **Effect interfaces** (per D-01, D-02, D-04):
```typescript
export interface ConditionModifierEffect {
  readonly type: 'modifier'
  readonly selector: ConditionSelector
  readonly modifierType: ModifierType
  readonly valuePerLevel: number
  readonly fixed?: true
}

export interface ConditionGrantEffect {
  readonly type: 'grant'
  readonly grants: ConditionSlug[]
}

export interface ConditionDrainedHpEffect {
  readonly type: 'drained-hp'
  // HP loss = max(1, creature.level) * conditionValue
  // Max HP reduction = same amount
}

export type ConditionEffect =
  | ConditionModifierEffect
  | ConditionGrantEffect
  | ConditionDrainedHpEffect
```

4. **CONDITION_EFFECTS data map** — every entry verified against refs/pf2e/conditions/*.json per the RESEARCH.md "Complete Condition Effect Map" section (per D-01, D-02, D-05):
```typescript
export const CONDITION_EFFECTS: Partial<Record<ConditionSlug, readonly ConditionEffect[]>> = {
  frightened: [
    { type: 'modifier', selector: 'all', modifierType: 'status', valuePerLevel: -1 },
  ],
  sickened: [
    { type: 'modifier', selector: 'all', modifierType: 'status', valuePerLevel: -1 },
  ],
  clumsy: [
    { type: 'modifier', selector: 'dex-based', modifierType: 'status', valuePerLevel: -1 },
  ],
  enfeebled: [
    { type: 'modifier', selector: ['str-based', 'str-damage'], modifierType: 'status', valuePerLevel: -1 },
  ],
  stupefied: [
    { type: 'modifier', selector: ['cha-based', 'int-based', 'wis-based'], modifierType: 'status', valuePerLevel: -1 },
  ],
  drained: [
    { type: 'modifier', selector: 'con-based', modifierType: 'status', valuePerLevel: -1 },
    { type: 'drained-hp' },
  ],
  blinded: [
    { type: 'modifier', selector: 'perception', modifierType: 'status', valuePerLevel: -4, fixed: true },
  ],
  unconscious: [
    { type: 'modifier', selector: ['ac', 'perception', 'reflex'], modifierType: 'status', valuePerLevel: -4, fixed: true },
    { type: 'grant', grants: ['blinded', 'off-guard', 'prone'] },
  ],
  grabbed: [
    { type: 'grant', grants: ['off-guard', 'immobilized'] },
  ],
  dying: [
    { type: 'grant', grants: ['unconscious'] },
  ],
  paralyzed: [
    { type: 'grant', grants: ['off-guard'] },
  ],
  restrained: [
    { type: 'grant', grants: ['off-guard', 'immobilized'] },
  ],
  'off-guard': [
    { type: 'modifier', selector: 'ac', modifierType: 'circumstance', valuePerLevel: -2, fixed: true },
  ],
}
```

5. **CONDITION_OVERRIDES map** — extracted from refs/pf2e/conditions/*.json system.overrides fields (per D-07):
```typescript
export const CONDITION_OVERRIDES: Partial<Record<ConditionSlug, ConditionSlug[]>> = {
  blinded:     ['dazzled'],
  stunned:     ['slowed'],
  hostile:     ['unfriendly', 'indifferent', 'friendly', 'helpful'],
  unfriendly:  ['hostile', 'indifferent', 'friendly', 'helpful'],
  indifferent: ['hostile', 'unfriendly', 'friendly', 'helpful'],
  friendly:    ['hostile', 'unfriendly', 'indifferent', 'helpful'],
  helpful:     ['hostile', 'unfriendly', 'indifferent', 'friendly'],
}
```

6. **Extended CONDITION_GROUPS constant** (per D-06, exported for ConditionManager to import):
```typescript
export const CONDITION_GROUPS_EXTENDED: Record<string, ConditionSlug[]> = {
  detection:  ['observed', 'hidden', 'undetected', 'unnoticed'],
  attitudes:  ['hostile', 'unfriendly', 'indifferent', 'friendly', 'helpful'],
  senses:     ['blinded', 'concealed', 'dazzled', 'deafened', 'invisible'],
  abilities:  ['clumsy', 'cursebound', 'drained', 'enfeebled', 'stupefied'],
  death:      ['doomed', 'dying', 'unconscious', 'wounded'],
}
```

7. **EXCLUSIVE_GROUPS set** (per D-08, D-09, D-10 — only detection and attitudes enforce mutual exclusivity):
```typescript
export const EXCLUSIVE_GROUPS = new Set(['detection', 'attitudes'])
```

Add source attribution comments referencing Foundry VTT PF2e refs/ JSON files for each data section.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - engine/conditions/condition-effects.ts exists
    - engine/conditions/condition-effects.ts contains `export const CONDITION_EFFECTS`
    - engine/conditions/condition-effects.ts contains `export const CONDITION_OVERRIDES`
    - engine/conditions/condition-effects.ts contains `export const CONDITION_GROUPS_EXTENDED`
    - engine/conditions/condition-effects.ts contains `export const EXCLUSIVE_GROUPS`
    - engine/conditions/condition-effects.ts contains `export type ConditionSelector`
    - engine/conditions/condition-effects.ts contains `export interface ConditionModifierEffect`
    - engine/conditions/condition-effects.ts contains `export interface ConditionGrantEffect`
    - engine/conditions/condition-effects.ts contains `export interface ConditionDrainedHpEffect`
    - engine/conditions/condition-effects.ts contains `export type ConditionEffect`
    - CONDITION_EFFECTS has entries for: frightened, sickened, clumsy, enfeebled, stupefied, drained, blinded, unconscious, grabbed, dying, paralyzed, restrained, off-guard (13 entries)
    - CONDITION_OVERRIDES has entries for: blinded, stunned, hostile, unfriendly, indifferent, friendly, helpful (7 entries)
    - EXCLUSIVE_GROUPS contains 'detection' and 'attitudes' only
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>condition-effects.ts contains the complete CONDITION_EFFECTS data map (13 conditions with 15 FlatModifier entries and 5 GrantItem chains), CONDITION_OVERRIDES map (7 entries), extended CONDITION_GROUPS (5 groups), EXCLUSIVE_GROUPS set (2 entries), and all associated TypeScript types. Compiles with zero errors.</done>
</task>

<task type="auto">
  <name>Task 3: Add Creature interface to engine/types.ts</name>
  <files>engine/types.ts</files>
  <read_first>engine/types.ts, engine/damage/iwr.ts, engine/conditions/conditions.ts</read_first>
  <action>
Extend `engine/types.ts` with the minimal Creature interface (per D-17). Add imports and the interface AFTER the existing WeakEliteTier type:

```typescript
// ─── Shared Engine Types ──────────────────────────────────────────────────────

/** Weak/Elite creature adjustment tier per PF2e Monster Core */
export type WeakEliteTier = 'normal' | 'weak' | 'elite'

// ─── Creature Interface (Phase 3) ────────────────────────────────────────────
// Source: D-17 — Minimal creature interface for Phase 3 condition/death mechanics.
// Phase 4 extends with AC, saves, abilities, speed, attacks, traits (D-18).

import type { Immunity } from './damage/iwr'
import type { ConditionManager } from './conditions/conditions'

export interface Creature {
  immunities: Immunity[]
  conditions: ConditionManager
  hp: {
    current: number
    max: number
    temp: number
  }
  level: number
  deathDoor: boolean
}
```

Note: Import statements should be at the TOP of the file (before WeakEliteTier), following TypeScript conventions. The comment and interface go after WeakEliteTier. Reorganize accordingly:

```typescript
import type { Immunity } from './damage/iwr'
import type { ConditionManager } from './conditions/conditions'

// ─── Shared Engine Types ──────────────────────────────────────────────────────

/** Weak/Elite creature adjustment tier per PF2e Monster Core */
export type WeakEliteTier = 'normal' | 'weak' | 'elite'

// ─── Creature Interface (Phase 3) ────────────────────────────────────────────
// Source: D-17 — Minimal creature interface for Phase 3 condition/death mechanics.
// Phase 4 extends with AC, saves, abilities, speed, attacks, traits (D-18).

export interface Creature {
  immunities: Immunity[]
  conditions: ConditionManager
  hp: {
    current: number
    max: number
    temp: number
  }
  level: number
  deathDoor: boolean
}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - engine/types.ts contains `import type { Immunity } from './damage/iwr'`
    - engine/types.ts contains `import type { ConditionManager } from './conditions/conditions'`
    - engine/types.ts contains `export interface Creature`
    - engine/types.ts contains `immunities: Immunity[]`
    - engine/types.ts contains `conditions: ConditionManager`
    - engine/types.ts contains `hp:` with `current: number`, `max: number`, `temp: number`
    - engine/types.ts contains `level: number`
    - engine/types.ts contains `deathDoor: boolean`
    - engine/types.ts still contains `export type WeakEliteTier`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Creature interface exists in engine/types.ts with exactly the fields specified by D-17: immunities, conditions, hp (current/max/temp), level, deathDoor. WeakEliteTier preserved. TypeScript compiles with zero errors.</done>
</task>

</tasks>

<verification>
After all 3 tasks:
1. `npx tsc --noEmit` passes with zero errors
2. engine/damage/damage.ts has 18 damage types (16 original + holy + unholy)
3. engine/damage/iwr.ts IMMUNITY_TYPES has 69+ entries (18 damage + 3 categories + 2 special + 24 condition + 25 effect)
4. engine/damage/iwr.ts typeMatches handles 'all-damage' as first check
5. engine/conditions/condition-effects.ts has 13 CONDITION_EFFECTS entries, 7 CONDITION_OVERRIDES entries, 5 groups, 2 exclusive groups
6. engine/types.ts has Creature interface with 5 fields
</verification>

<success_criteria>
- All type arrays compile without error (the `as const` + derived union pattern works with the new entries)
- CONDITION_EFFECTS covers exactly the 13 conditions documented in RESEARCH.md
- CONDITION_OVERRIDES matches exactly the 7 entries from refs/ JSON system.overrides
- Creature interface is minimal per D-17 (no AC, saves, abilities — those are Phase 4)
- holy/unholy appear in DamageType union and DAMAGE_TYPE_CATEGORY
- all-damage appears only in RESISTANCE_TYPES (not IMMUNITY_TYPES or WEAKNESS_TYPES per Pitfall 6)
</success_criteria>

<output>
After completion, create `.planning/phases/03-conditions-statuses/03-01-SUMMARY.md`
</output>
