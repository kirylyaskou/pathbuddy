---
phase: 03-tauri-sqlite-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/src/main.rs
  - src-tauri/src/lib.rs
  - src-tauri/build.rs
  - src-tauri/capabilities/default.json
  - src-tauri/tauri.conf.json
  - package.json
autonomous: true
requirements: [TAURI-01]

must_haves:
  truths:
    - "Tauri 2 app compiles on Rust side without errors"
    - "Frontend dev server starts and connects to Tauri 2 backend"
    - "Three Tauri plugins registered: sql (sqlite), http, fs"
    - "Rust deps reqwest and zip available for future sync commands"
  artifacts:
    - path: "src-tauri/Cargo.toml"
      provides: "Tauri 2 Rust dependencies"
      contains: 'tauri = { version = "2"'
    - path: "src-tauri/src/lib.rs"
      provides: "Tauri 2 plugin registration"
      contains: "tauri_plugin_sql"
    - path: "src-tauri/build.rs"
      provides: "Tauri 2 build script"
      contains: "tauri_build"
    - path: "src-tauri/capabilities/default.json"
      provides: "Tauri 2 capability permissions for plugins"
      contains: "sql:default"
    - path: "package.json"
      provides: "Updated JS dependencies for Tauri 2"
      contains: "@tauri-apps/plugin-sql"
  key_links:
    - from: "src-tauri/src/lib.rs"
      to: "src-tauri/Cargo.toml"
      via: "Rust plugin crates imported and registered"
      pattern: "tauri_plugin_sql"
    - from: "package.json"
      to: "src-tauri/tauri.conf.json"
      via: "JS packages match Tauri 2 config format"
      pattern: "@tauri-apps/api.*2"
---

<objective>
Upgrade the existing Tauri v1 project to Tauri v2 in-place, installing all three required plugins (sql, http, fs) and future Rust deps (reqwest, zip).

Purpose: TAURI-01 requires the app to run as a Tauri 2 desktop application. This plan migrates the Rust backend, JS frontend packages, and Tauri config from v1 to v2. It is the foundation for all subsequent database and sync work.

Output: A Tauri 2 project that compiles (`cargo check` passes in src-tauri) and runs the existing Vue 3 combat tracker unchanged.
</objective>

<execution_context>
@C:/Users/kiryl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kiryl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-tauri-sqlite-foundation/03-CONTEXT.md
@plans/plan.txt (Phase 1 section — Tauri plugin setup and Cargo.toml)

<interfaces>
<!-- Current Rust entry point (src-tauri/src/main.rs) — must be rewritten for Tauri 2 -->
fn main() {
    tauri::Builder::default()
        .plugin(tauri_plugin_window_state::Builder::default().build())
        .invoke_handler(tauri::generate_handler![greet])
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}

<!-- Current Cargo.toml dependencies — all v1, must be upgraded -->
tauri-build = { version = "1.5", features = [] }
tauri = { version = "1.6", features = [] }
tauri-plugin-window-state = "0.1"

<!-- Current package.json Tauri dep — must be upgraded to v2 -->
"@tauri-apps/api": "^1.6.0"

<!-- Current tauri.conf.json — already uses v2 structure (app.windows), keep structure -->
"productName": "pathbuddy"
"identifier": "com.pathbuddy.app"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade Rust backend to Tauri 2 with plugins</name>
  <files>src-tauri/Cargo.toml, src-tauri/src/main.rs, src-tauri/src/lib.rs, src-tauri/build.rs, src-tauri/capabilities/default.json</files>
  <read_first>
    - src-tauri/Cargo.toml (current v1 deps to replace)
    - src-tauri/src/main.rs (current v1 entry point to rewrite)
    - plans/plan.txt (lines 37-45 for Cargo.toml plugin deps)
    - .planning/phases/03-tauri-sqlite-foundation/03-CONTEXT.md (locked decisions for plugin list)
  </read_first>
  <action>
1. **Rewrite `src-tauri/Cargo.toml`** with Tauri 2 dependencies:

```toml
[package]
name = "pathbuddy"
version = "0.1.0"
description = "Pathfinder 2e DM Assistant - Combat Tracker"
authors = ["you"]
edition = "2021"

[lib]
name = "pathbuddy_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-sql = { version = "2", features = ["sqlite"] }
tauri-plugin-http = "2"
tauri-plugin-fs = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
reqwest = { version = "0.12", features = ["json"] }
zip = "2"
```

Remove `tauri-plugin-window-state` (v1 only). Remove `[features]` section with `custom-protocol` (Tauri 2 does not use this pattern).

2. **Create `src-tauri/build.rs`** (Tauri 2 requires this explicit file):

```rust
fn main() {
    tauri_build::build()
}
```

3. **Create `src-tauri/src/lib.rs`** (Tauri 2 uses lib.rs as the entry point, main.rs just calls it):

```rust
#[cfg_attr(mobile, tauri::mobile_entry_point)]
pub fn run() {
    tauri::Builder::default()
        .plugin(tauri_plugin_sql::Builder::default().build())
        .plugin(tauri_plugin_http::init())
        .plugin(tauri_plugin_fs::init())
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```

4. **Rewrite `src-tauri/src/main.rs`** to just call the lib entry:

```rust
#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]

fn main() {
    pathbuddy_lib::run();
}
```

5. **Create `src-tauri/capabilities/default.json`** for Tauri 2 plugin permissions:

```json
{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for pathbuddy",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "sql:default",
    "sql:allow-execute",
    "sql:allow-select",
    "http:default",
    "http:allow-fetch",
    "http:allow-fetch-cancel",
    "http:allow-fetch-read-body",
    "http:allow-fetch-send",
    "fs:default"
  ]
}
```
  </action>
  <verify>
    <automated>cd D:/pathbuddy/src-tauri && cargo check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src-tauri/Cargo.toml contains `tauri = { version = "2"`
    - src-tauri/Cargo.toml contains `tauri-plugin-sql = { version = "2", features = ["sqlite"] }`
    - src-tauri/Cargo.toml contains `tauri-plugin-http = "2"`
    - src-tauri/Cargo.toml contains `tauri-plugin-fs = "2"`
    - src-tauri/Cargo.toml contains `reqwest`
    - src-tauri/Cargo.toml contains `zip = "2"`
    - src-tauri/Cargo.toml does NOT contain `tauri-plugin-window-state`
    - src-tauri/src/lib.rs contains `tauri_plugin_sql::Builder::default().build()`
    - src-tauri/src/lib.rs contains `tauri_plugin_http::init()`
    - src-tauri/src/lib.rs contains `tauri_plugin_fs::init()`
    - src-tauri/src/main.rs contains `pathbuddy_lib::run()`
    - src-tauri/build.rs contains `tauri_build::build()`
    - src-tauri/capabilities/default.json contains `"sql:default"`
    - `cargo check` in src-tauri exits 0
  </acceptance_criteria>
  <done>Rust backend compiles with Tauri 2 and all three plugins registered. No v1 dependencies remain.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade JS frontend packages for Tauri 2</name>
  <files>package.json, src-tauri/tauri.conf.json</files>
  <read_first>
    - package.json (current v1 @tauri-apps/api to replace)
    - src-tauri/tauri.conf.json (current config to verify/update for v2)
    - .planning/phases/03-tauri-sqlite-foundation/03-CONTEXT.md (window settings: 900x700, resizable, title)
  </read_first>
  <action>
1. **Update `package.json`** dependencies:

Remove `"@tauri-apps/api": "^1.6.0"` from dependencies.

Add to dependencies:
```json
"@tauri-apps/api": "^2.0.0",
"@tauri-apps/plugin-sql": "^2.0.0",
"@tauri-apps/plugin-http": "^2.0.0",
"@tauri-apps/plugin-fs": "^2.0.0",
"drizzle-orm": "^0.38.0",
"vue-router": "^4.5.0"
```

Update scripts — replace `"tauri": "tauri build"` and `"tauri:dev": "tauri dev"` with:
```json
"tauri": "tauri",
"tauri:dev": "tauri dev",
"tauri:build": "tauri build"
```

Add `@tauri-apps/cli` to devDependencies:
```json
"@tauri-apps/cli": "^2.0.0"
```

2. **Verify/update `src-tauri/tauri.conf.json`** — the existing file already uses Tauri 2 structure. Ensure it has these exact contents:

```json
{
  "productName": "pathbuddy",
  "version": "0.1.0",
  "identifier": "com.pathbuddy.app",
  "build": {
    "beforeDevCommand": "npm run dev",
    "devUrl": "http://localhost:5173",
    "beforeBuildCommand": "npm run build",
    "frontendDist": "../dist"
  },
  "app": {
    "withGlobalTauri": true,
    "windows": [
      {
        "title": "Pathbuddy - PF2e DM Assistant",
        "width": 900,
        "height": 700,
        "resizable": true,
        "fullscreen": false
      }
    ],
    "security": {
      "csp": null
    }
  },
  "bundle": {
    "active": true,
    "targets": "all",
    "icon": []
  }
}
```

3. **Run `npm install`** to install all new dependencies and generate lock file.

4. **Run `npm run build`** (vue-tsc && vite build) to verify the frontend still compiles. This validates that existing Vue components are not broken by the package upgrades.
  </action>
  <verify>
    <automated>cd D:/pathbuddy && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"@tauri-apps/api": "^2.0.0"`
    - package.json contains `"@tauri-apps/plugin-sql"`
    - package.json contains `"@tauri-apps/plugin-http"`
    - package.json contains `"@tauri-apps/plugin-fs"`
    - package.json contains `"drizzle-orm"`
    - package.json contains `"vue-router"`
    - package.json contains `"@tauri-apps/cli"` in devDependencies
    - package.json does NOT contain `"@tauri-apps/api": "^1`
    - src-tauri/tauri.conf.json contains `"width": 900`
    - src-tauri/tauri.conf.json contains `"height": 700`
    - `npm run build` exits 0 (frontend compiles clean)
  </acceptance_criteria>
  <done>JS frontend has all Tauri 2 packages installed. Frontend build passes. Existing combat tracker code is not broken.</done>
</task>

</tasks>

<verification>
1. `cargo check` passes in src-tauri/ (Rust side compiles with Tauri 2 + plugins)
2. `npm run build` passes (frontend compiles with updated packages)
3. No Tauri v1 dependencies remain in Cargo.toml or package.json
4. All three plugins (sql, http, fs) are registered in lib.rs
5. Capabilities file grants required permissions for sql plugin
</verification>

<success_criteria>
- Tauri 2 Rust backend compiles with all plugins
- Vue 3 frontend builds without errors using updated @tauri-apps packages
- Zero Tauri v1 artifacts remain
- Ready for Plan 02 to add database layer and UI routing
</success_criteria>

<output>
After completion, create `.planning/phases/03-tauri-sqlite-foundation/03-01-SUMMARY.md`
</output>
