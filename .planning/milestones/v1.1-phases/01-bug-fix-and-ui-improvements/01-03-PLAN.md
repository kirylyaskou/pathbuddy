---
phase: 01-bug-fix-and-ui-improvements
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/components/CreatureCard.vue
  - src/components/CombatTracker.vue
  - src/components/AddCreatureForm.vue
autonomous: true
requirements: []

must_haves:
  truths:
    - "CreatureCard shows initiative badge (circle), creature name, AC+Dex stats row, HP bar with threshold colors, HP fraction, condition badges, regen/detail buttons"
    - "Active turn creature has gold left border, gold glow shadow, gold initiative badge, gold creature name"
    - "Downed creature has opacity-50 and crimson-dark left border"
    - "CombatTracker has dark-themed toolbar with round badge, New Round, Next Creature, and Add Creature buttons"
    - "CombatTracker empty state shows 'No Creatures in Combat' heading and 'Add a creature...' body text"
    - "Drag-over highlights use gold ring instead of blue ring"
    - "AddCreatureForm modal has dark charcoal background, gold submit button, and uses ConditionBadge-style pills for condition selection"
  artifacts:
    - path: "src/components/CreatureCard.vue"
      provides: "Full dark fantasy creature card with initiative badge, HP bar, condition badges"
      min_lines: 60
    - path: "src/components/CombatTracker.vue"
      provides: "Dark-themed combat tracker with toolbar and creature list"
      min_lines: 80
    - path: "src/components/AddCreatureForm.vue"
      provides: "Dark-themed modal form for adding creatures"
      min_lines: 60
  key_links:
    - from: "src/components/CreatureCard.vue"
      to: "src/components/HPController.vue"
      via: "child component"
      pattern: "HPController"
    - from: "src/components/CreatureCard.vue"
      to: "src/components/ConditionBadge.vue"
      via: "child component"
      pattern: "ConditionBadge"
    - from: "src/components/CombatTracker.vue"
      to: "src/components/CreatureCard.vue"
      via: "v-for rendering"
      pattern: "CreatureCard"
    - from: "src/components/CombatTracker.vue"
      to: "src/components/AddCreatureForm.vue"
      via: "modal toggle"
      pattern: "AddCreatureForm"
---

<objective>
Fully redesign CreatureCard with dark fantasy theme (initiative badge, HP bar, condition badges), redesign CombatTracker with dark toolbar and gold accents, and restyle AddCreatureForm modal.

Purpose: This is the visual heart of the app — the combat tracker and its creature cards are the primary UI. After this plan, the combat experience is fully themed.

Output: Dark fantasy combat tracker with initiative badges, HP bars with threshold colors, condition pill badges, and a dark-themed creature form modal.
</objective>

<execution_context>
@C:/Users/kiryl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kiryl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bug-fix-and-ui-improvements/01-CONTEXT.md
@.planning/phases/01-bug-fix-and-ui-improvements/01-RESEARCH.md
@.planning/phases/01-bug-fix-and-ui-improvements/01-UI-SPEC.md

<!-- Plan 01 SUMMARY needed: AppLayout sets overflow constraints that CombatTracker must respect -->
@.planning/phases/01-bug-fix-and-ui-improvements/01-01-SUMMARY.md
<!-- Plan 02 SUMMARY needed: HPController and ConditionBadge component contracts -->
@.planning/phases/01-bug-fix-and-ui-improvements/01-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 outputs — component contracts executor needs -->

HPController.vue props/emits (unchanged from v1):
```typescript
defineProps<{ currentHp: number; maxHp: number; creatureId: string }>()
defineEmits<{ modifyHp: [delta: number] }>()
```

ConditionBadge.vue props/emits (new in Plan 02):
```typescript
defineProps<{
  conditions: Condition[]
  conditionValues: Partial<Record<Condition, number>> | undefined
  creatureId: string
}>()
defineEmits<{
  toggleCondition: [condition: Condition]
  setConditionValue: [condition: Condition, value: number]
}>()
```

From src/stores/combat.ts (Plan 02 additions):
```typescript
setConditionValue(id: string, condition: Condition, value: number): void
getConditionValue(id: string, condition: Condition): number
```

From src/types/combat.ts (Plan 02 addition):
```typescript
export interface Creature {
  // ... existing fields ...
  conditionValues?: Partial<Record<Condition, number>>
}
```

From src/composables/useConditions.ts (Plan 02 additions):
```typescript
export const CONDITION_BADGE_CLASSES: Record<Condition, string>
export const CONDITIONS_WITH_VALUES: Set<Condition>
export function conditionHasValue(condition: Condition): boolean
// Existing (unchanged):
export const CONDITION_DEFS: ConditionDef[]
export function formatCondition(condition: Condition): string
```

CreatureCard.vue current props/emits (MUST preserve):
```typescript
defineProps<{
  creature: Creature
  isCurrentTurn: boolean
  turnIndex: number
  regenerationDisabled: boolean
}>()
defineEmits<{
  modifyHp: [id: string, delta: number]
  toggleCondition: [id: string, condition: string]
  toggleRegenerationDisabled: [creatureId: string]
  openDetail: [creatureId: string]
}>()
```

CombatTracker.vue (wires events from CreatureCard — all event handlers stay):
```typescript
handleModifyHp(id, delta), handleToggleCondition(id, condition),
handleToggleRegeneration(creatureId), handleOpenDetail(creatureId)
getRegenerationDisabled(creatureId): boolean
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CreatureCard full dark fantasy redesign</name>
  <files>src/components/CreatureCard.vue</files>
  <read_first>
    - src/components/CreatureCard.vue
    - src/components/HPController.vue
    - src/components/ConditionBadge.vue
    - src/types/combat.ts
    - src/composables/useConditions.ts
    - .planning/phases/01-bug-fix-and-ui-improvements/01-UI-SPEC.md
  </read_first>
  <action>
Fully replace the template and update the script of `src/components/CreatureCard.vue`. The props/emits interface stays IDENTICAL — do NOT change any prop names or emit signatures.

**Script changes:**
- Replace `import ConditionToggle from './ConditionToggle.vue'` with `import ConditionBadge from './ConditionBadge.vue'`
- Remove `import { CONDITION_DEFS } from '@/composables/useConditions'` (ConditionBadge handles this internally)
- Add: `import type { Condition } from '@/types/combat'`
- Keep `import HPController from './HPController.vue'`
- Keep ALL existing props, emits, computed, and handler functions
- Add a new emit handler for condition values:
```typescript
const handleSetConditionValue = (condition: Condition, value: number) => {
  // Delegate to parent — CombatTracker will wire this to store
  emit('setConditionValue', props.creature.id, condition, value)
}
```
- Add `setConditionValue` to the emits definition:
```typescript
const emit = defineEmits<{
  modifyHp: [id: string, delta: number]
  toggleCondition: [id: string, condition: string]
  toggleRegenerationDisabled: [creatureId: string]
  openDetail: [creatureId: string]
  setConditionValue: [id: string, condition: Condition, value: number]
}>()
```
- Add HP bar percentage computed:
```typescript
const hpPercent = computed(() => {
  if (props.creature.maxHP === 0) return 0
  return Math.round((props.creature.currentHP / props.creature.maxHP) * 100)
})

const hpBarColor = computed(() => {
  if (hpPercent.value > 50) return 'bg-gold'
  if (hpPercent.value > 25) return 'bg-amber-600'
  return 'bg-crimson-light'
})
```

**Template — full replacement:**
Card outer div:
```html
<div
  :class="[
    'rounded-lg p-4 mb-2 flex gap-4 items-start shadow-card transition-colors',
    creature.isDowned && !isCurrentTurn
      ? 'opacity-50 border-l-4 border-crimson-dark bg-charcoal-600'
      : isCurrentTurn
        ? 'border-l-4 border-gold shadow-gold-glow bg-charcoal-600'
        : 'border-l-4 border-charcoal-500 bg-charcoal-600'
  ]"
  :draggable="canDrag"
  @dragstart="canDrag ? $event.dataTransfer?.setData('text/plain', creature.id) : null"
>
```

Inside the card, three columns:

**Column 1 — Initiative Badge:**
```html
<div
  :class="[
    'w-8 h-8 rounded-full flex items-center justify-center font-bold text-sm flex-shrink-0',
    isCurrentTurn
      ? 'bg-gold text-charcoal-950'
      : 'bg-charcoal-500 text-stone-300'
  ]"
>
  {{ creature.initiative }}
</div>
```

**Column 2 — Creature Info (`flex-1 flex flex-col gap-1`):**
```html
<div class="flex-1 flex flex-col gap-1">
  <!-- Creature name -->
  <h3 :class="['text-base font-display font-bold', isCurrentTurn ? 'text-gold' : 'text-stone-100']">
    {{ creature.name }}
  </h3>
  <!-- Stats row -->
  <div class="text-xs text-stone-400">
    AC {{ creature.ac }} · Dex {{ creature.dexMod >= 0 ? '+' : '' }}{{ creature.dexMod }}
  </div>
  <!-- Condition badges -->
  <ConditionBadge
    :conditions="creature.conditions"
    :condition-values="creature.conditionValues"
    :creature-id="creature.id"
    @toggle-condition="(condition) => handleToggleCondition(condition)"
    @set-condition-value="handleSetConditionValue"
  />
</div>
```

**Column 3 — HP + Controls (`flex flex-col items-end gap-2`):**
```html
<div class="flex flex-col items-end gap-2">
  <!-- HP fraction -->
  <span class="text-sm font-normal text-stone-200">
    {{ creature.currentHP }} / {{ creature.maxHP }}
  </span>
  <!-- HP bar -->
  <div class="w-32 h-1.5 rounded-full bg-charcoal-900">
    <div
      :class="['h-full rounded-full transition-all duration-300', hpBarColor]"
      :style="{ width: hpPercent + '%' }"
    ></div>
  </div>
  <!-- HP controls -->
  <HPController
    :current-hp="creature.currentHP"
    :max-hp="creature.maxHP"
    :creature-id="creature.id"
    @modify-hp="handleModifyHp"
  />
  <!-- Regen + Detail buttons -->
  <div class="flex gap-1">
    <button
      @click="handleToggleRegeneration"
      :class="[
        'px-2 py-1 text-xs rounded font-bold transition-colors',
        props.regenerationDisabled
          ? 'bg-crimson text-stone-100 hover:bg-crimson-light'
          : 'bg-charcoal-500 text-stone-400 hover:bg-charcoal-400'
      ]"
      :title="props.regenerationDisabled ? 'Regeneration disabled' : 'Enable regeneration'"
    >
      {{ props.regenerationDisabled ? 'Regen: OFF' : 'Regen: ON' }}
    </button>
    <button
      v-if="creature.sourceId"
      @click="handleOpenDetail"
      class="px-2 py-1 text-xs rounded font-bold bg-charcoal-500 text-gold hover:bg-charcoal-400 transition-colors"
    >
      View Stat Block
    </button>
  </div>
</div>
```

Remove the old template entirely — no ConditionToggle v-for, no gray/blue/white classes remain.
  </action>
  <verify>
    <automated>grep -c "ConditionBadge" src/components/CreatureCard.vue && grep -c "border-gold" src/components/CreatureCard.vue && grep -c "hpPercent" src/components/CreatureCard.vue && grep -c "bg-charcoal-600" src/components/CreatureCard.vue && grep -c "font-display" src/components/CreatureCard.vue && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/CreatureCard.vue imports ConditionBadge (NOT ConditionToggle)
    - src/components/CreatureCard.vue does NOT import CONDITION_DEFS
    - src/components/CreatureCard.vue has `hpPercent` and `hpBarColor` computed properties
    - src/components/CreatureCard.vue HP bar uses width style binding: `:style="{ width: hpPercent + '%' }"`
    - src/components/CreatureCard.vue HP bar color is `bg-gold` when HP > 50%, `bg-amber-600` when 26-50%, `bg-crimson-light` when <= 25%
    - src/components/CreatureCard.vue active turn state uses `border-gold shadow-gold-glow bg-charcoal-600`
    - src/components/CreatureCard.vue inactive state uses `border-charcoal-500 bg-charcoal-600`
    - src/components/CreatureCard.vue downed state uses `opacity-50 border-crimson-dark`
    - src/components/CreatureCard.vue initiative badge is a circle (`rounded-full w-8 h-8`)
    - src/components/CreatureCard.vue creature name uses `font-display font-bold` and is `text-gold` when active
    - src/components/CreatureCard.vue stats row shows "AC {n} · Dex {mod}" (not "Initiative: {n} | AC: {n} | Dex Mod: {n}")
    - src/components/CreatureCard.vue has NO `bg-blue-50`, `border-blue-500`, `bg-white`, `text-gray-*` classes
    - src/components/CreatureCard.vue preserves all 4 original emits AND adds `setConditionValue`
    - src/components/CreatureCard.vue preserves `canDrag` computed and dragstart handler
    - src/components/CreatureCard.vue passes `condition-values` prop to ConditionBadge
    - src/components/CreatureCard.vue Regen button uses `bg-crimson` when disabled, `bg-charcoal-500` when enabled
    - src/components/CreatureCard.vue "View Stat Block" button uses `text-gold`
  </acceptance_criteria>
  <done>CreatureCard fully redesigned with dark fantasy theme, initiative badge, HP bar with threshold colors, and ConditionBadge integration</done>
</task>

<task type="auto">
  <name>Task 2: CombatTracker dark theme redesign + AddCreatureForm dark modal</name>
  <files>src/components/CombatTracker.vue, src/components/AddCreatureForm.vue</files>
  <read_first>
    - src/components/CombatTracker.vue
    - src/components/AddCreatureForm.vue
    - src/components/CreatureCard.vue
    - src/stores/combat.ts
    - src/composables/useConditions.ts
    - .planning/phases/01-bug-fix-and-ui-improvements/01-UI-SPEC.md
  </read_first>
  <action>
**1. Redesign `src/components/CombatTracker.vue`:**

Keep ALL script logic, imports, and event handlers unchanged. The only script addition is a handler for the new `setConditionValue` emit from CreatureCard:
```typescript
import type { Condition } from '@/types/combat'

const handleSetConditionValue = (id: string, condition: Condition, value: number) => {
  combatStore.setConditionValue(id, condition, value)
}
```

Replace the entire template:

**Remove** the `min-h-screen bg-gradient-to-br from-gray-100 to-gray-200` wrapper. The component now fills `<RouterView>` which fills `<main>` in AppLayout. The outermost element should be a plain `<div>` or `<template>` with no min-height or background (AppLayout provides `bg-charcoal-800`).

**Toolbar** (replaces white header):
```html
<div class="sticky top-0 z-10 bg-charcoal-900 border-b border-charcoal-500 px-6 py-3 flex items-center justify-between">
  <h1 class="text-xl font-display font-bold text-stone-100">Combat Tracker</h1>
  <div class="flex items-center gap-3">
    <!-- Round badge -->
    <div class="px-3 py-1 rounded-md bg-charcoal-600 border border-charcoal-500 flex items-center gap-2">
      <span class="text-xs text-stone-400 font-normal">Round</span>
      <span class="text-lg font-bold text-gold">{{ combatStore.roundNumber }}</span>
    </div>
    <!-- New Round button -->
    <button
      @click="handleAdvanceRound"
      :disabled="!hasAllActed || combatStore.creatures.length === 0"
      class="px-3 py-1.5 rounded bg-charcoal-500 hover:bg-charcoal-400 border border-charcoal-400 text-sm font-bold text-stone-200 transition-colors disabled:opacity-40 disabled:cursor-not-allowed"
    >
      New Round
    </button>
    <!-- Next Creature button -->
    <button
      @click="handleNextCreature"
      :disabled="combatStore.creatures.length === 0"
      class="px-3 py-1.5 rounded bg-charcoal-500 hover:bg-charcoal-400 border border-charcoal-400 text-sm font-bold text-stone-200 transition-colors disabled:opacity-40 disabled:cursor-not-allowed"
    >
      Next Creature
    </button>
    <!-- Add Creature button (primary CTA) -->
    <button
      @click="formOpen = true"
      class="px-3 py-1.5 rounded bg-gold hover:bg-gold-light text-charcoal-950 text-sm font-bold transition-colors"
    >
      + Add Creature
    </button>
  </div>
</div>
```

**Empty state:**
```html
<div v-if="combatStore.creatures.length === 0" class="text-center py-12">
  <h2 class="text-lg font-display font-bold text-stone-300">No Creatures in Combat</h2>
  <p class="text-stone-500 mt-2 text-sm">Add a creature to begin tracking initiative and HP.</p>
</div>
```

**Creature list area:**
```html
<div v-else class="px-6 py-4 space-y-2">
  <div
    v-for="(creature, index) in combatStore.sortedCreatures"
    :key="creature.id"
    @dragover.prevent="handleDragOver"
    @dragenter.prevent="handleDragEnter(creature.id)"
    @dragleave="handleDragLeave"
    @drop="handleDrop($event, creature.id)"
    :class="{ 'ring-1 ring-gold ring-offset-1 ring-offset-charcoal-800 rounded-lg': dragOverCreatureId === creature.id }"
  >
    <CreatureCard
      :creature="creature"
      :is-current-turn="creature.isCurrentTurn"
      :turn-index="index"
      :regeneration-disabled="getRegenerationDisabled(creature.id)"
      @modify-hp="handleModifyHp"
      @toggle-condition="handleToggleCondition"
      @toggle-regeneration-disabled="handleToggleRegeneration"
      @open-detail="handleOpenDetail"
      @set-condition-value="handleSetConditionValue"
    />
  </div>
</div>
```

Note the drag-over highlight changes: `ring-1 ring-gold ring-offset-1 ring-offset-charcoal-800` replaces `ring-2 ring-blue-400 ring-offset-2`.

Keep `<AddCreatureForm v-model="formOpen" @add="combatStore.addCreature" />` and `<CreatureDetailPanel />` at the bottom, unchanged.

**2. Redesign `src/components/AddCreatureForm.vue`:**

Keep ALL script logic unchanged — form reactive, toggleCondition, handleSubmit, cancel, emit/props, watchers.

Replace the template with dark fantasy theme:

**Backdrop:** Change `bg-black bg-opacity-50` to `bg-charcoal-950/80`

**Modal panel:** Change `bg-white rounded-lg p-6 w-full max-w-md shadow-xl` to `bg-charcoal-600 rounded-xl border border-charcoal-500 shadow-card p-6 w-full max-w-md`

**Heading:** Change `text-xl font-bold mb-4` to `text-xl font-display font-bold text-stone-100 mb-4`

**Form labels:** Change `block text-sm font-medium mb-1` to `block text-xs font-normal text-stone-400 mb-1`

**Text inputs:** Change `w-full px-3 py-2 border rounded focus:outline-none focus:ring-2 focus:ring-blue-500` to `w-full px-3 py-2 rounded bg-charcoal-900 border border-charcoal-500 text-sm text-stone-200 placeholder-stone-500 focus:outline-none focus:border-gold transition-colors`

**Number inputs:** Same as text inputs plus `font-mono`

**Condition toggles in form:** Replace the dynamic `bg-${def.color}-600` class pattern with static classes. Use the CONDITION_BADGE_CLASSES map from useConditions:
```typescript
import { CONDITION_DEFS, CONDITION_BADGE_CLASSES } from '@/composables/useConditions'
```
Active condition: `CONDITION_BADGE_CLASSES[def.name]`
Inactive condition: `'bg-charcoal-500 text-stone-400 hover:bg-charcoal-400'`

**Cancel button:** Change label from "Cancel" to "Discard" per UI-SPEC copywriting contract. Change classes to `px-4 py-2 rounded bg-charcoal-500 hover:bg-charcoal-400 text-stone-300 text-sm font-bold`

**Submit button:** Change `bg-blue-600 hover:bg-blue-700 rounded text-white disabled:opacity-50` to `px-4 py-2 rounded bg-gold hover:bg-gold-light text-charcoal-950 text-sm font-bold disabled:opacity-40 disabled:cursor-not-allowed`

Keep the existing `<Transition name="fade">` and `<style scoped>` section for the fade transition — no change needed.
  </action>
  <verify>
    <automated>grep -c "bg-charcoal-900" src/components/CombatTracker.vue && grep -c "font-display" src/components/CombatTracker.vue && grep -c "ring-gold" src/components/CombatTracker.vue && grep -c "setConditionValue" src/components/CombatTracker.vue && grep -c "bg-charcoal-600" src/components/AddCreatureForm.vue && grep -c "bg-gold" src/components/AddCreatureForm.vue && grep -c "Discard" src/components/AddCreatureForm.vue && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/CombatTracker.vue has NO `min-h-screen`, `bg-gradient-to-br`, `from-gray-100`, `bg-white`, `shadow-md` classes
    - src/components/CombatTracker.vue toolbar uses `sticky top-0 z-10 bg-charcoal-900 border-b border-charcoal-500`
    - src/components/CombatTracker.vue heading uses `font-display font-bold text-stone-100`
    - src/components/CombatTracker.vue round badge uses `bg-charcoal-600 border border-charcoal-500` with `text-gold` number
    - src/components/CombatTracker.vue "New Round" and "Next Creature" buttons use `bg-charcoal-500 hover:bg-charcoal-400` with `disabled:opacity-40`
    - src/components/CombatTracker.vue "Add Creature" button uses `bg-gold hover:bg-gold-light text-charcoal-950`
    - src/components/CombatTracker.vue empty state shows "No Creatures in Combat" (h2) and "Add a creature to begin tracking initiative and HP." (p)
    - src/components/CombatTracker.vue drag-over uses `ring-1 ring-gold ring-offset-1 ring-offset-charcoal-800` (NOT `ring-blue-400`)
    - src/components/CombatTracker.vue has `@set-condition-value="handleSetConditionValue"` on CreatureCard
    - src/components/CombatTracker.vue has `handleSetConditionValue` function calling `combatStore.setConditionValue`
    - src/components/CombatTracker.vue preserves ALL existing event handlers and store interactions
    - src/components/CombatTracker.vue still renders `<AddCreatureForm>` and `<CreatureDetailPanel />`
    - src/components/AddCreatureForm.vue backdrop uses `bg-charcoal-950/80` (not `bg-black bg-opacity-50`)
    - src/components/AddCreatureForm.vue modal panel uses `bg-charcoal-600 rounded-xl border border-charcoal-500 shadow-card`
    - src/components/AddCreatureForm.vue heading uses `font-display font-bold text-stone-100`
    - src/components/AddCreatureForm.vue inputs use `bg-charcoal-900 border border-charcoal-500 text-stone-200 focus:border-gold`
    - src/components/AddCreatureForm.vue condition toggles use `CONDITION_BADGE_CLASSES` from useConditions (NOT dynamic `bg-${color}-600`)
    - src/components/AddCreatureForm.vue cancel button text is "Discard" (not "Cancel")
    - src/components/AddCreatureForm.vue submit button uses `bg-gold text-charcoal-950`
    - src/components/AddCreatureForm.vue has NO `bg-white`, `bg-blue-600`, `ring-blue-500`, `bg-gray-200` classes
    - src/components/AddCreatureForm.vue preserves fade Transition and scoped styles
  </acceptance_criteria>
  <done>CombatTracker and AddCreatureForm fully redesigned with dark fantasy theme, gold accents, and proper condition badge integration</done>
</task>

</tasks>

<verification>
- `grep "bg-blue\|bg-gray\|bg-white\|bg-green\|bg-purple\|ring-blue" src/components/CreatureCard.vue src/components/CombatTracker.vue src/components/AddCreatureForm.vue` returns NO matches — all old theme classes removed
- `grep "charcoal\|gold\|crimson" src/components/CreatureCard.vue` returns multiple matches for dark theme tokens
- `grep "font-display" src/components/CombatTracker.vue` confirms Cinzel heading usage
- `grep "setConditionValue" src/components/CombatTracker.vue` confirms new event wiring
</verification>

<success_criteria>
- CreatureCard has three-column layout: initiative badge, creature info, HP+controls
- CreatureCard HP bar shows gold/amber/crimson based on HP threshold
- CreatureCard active turn has gold border + glow, inactive has charcoal border
- CombatTracker has dark toolbar with gold accents, no white/gray remnants
- CombatTracker empty state uses correct copywriting from UI-SPEC
- CombatTracker drag-over uses gold ring
- AddCreatureForm is fully dark-themed with gold submit and "Discard" cancel
- All three components have zero old light-theme class remnants
</success_criteria>

<output>
After completion, create `.planning/phases/01-bug-fix-and-ui-improvements/01-03-SUMMARY.md`
</output>
