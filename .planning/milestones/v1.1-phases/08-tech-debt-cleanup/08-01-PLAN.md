---
phase: 08-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-3-panel-combat-workspace/05-01-SUMMARY.md
  - .planning/phases/05-3-panel-combat-workspace/05-02-SUMMARY.md
  - .planning/phases/05-3-panel-combat-workspace/05-03-SUMMARY.md
  - src/components/ConditionToggle.vue
  - src/components/__tests__/ConditionToggle.test.ts
  - src/components/SyncButton.vue
  - src/components/__tests__/SyncButton.test.ts
  - src/views/DashboardView.vue
autonomous: true
requirements: [WORK-01, WORK-02, WORK-03, WORK-04, WORK-05]

must_haves:
  truths:
    - "Phase 05 SUMMARY files document which WORK requirements they completed"
    - "No dead code from Phase 01 superseded components remains in codebase"
    - "All existing tests pass with no regressions after dead code deletion"
  artifacts:
    - path: ".planning/phases/05-3-panel-combat-workspace/05-01-SUMMARY.md"
      provides: "Requirements traceability for WORK-03, WORK-04, WORK-05"
      contains: "requirements_completed"
    - path: ".planning/phases/05-3-panel-combat-workspace/05-02-SUMMARY.md"
      provides: "Requirements traceability for WORK-02"
      contains: "requirements_completed"
    - path: ".planning/phases/05-3-panel-combat-workspace/05-03-SUMMARY.md"
      provides: "Requirements traceability for WORK-01 through WORK-05"
      contains: "requirements_completed"
  key_links: []
---

<objective>
Close the Phase 05 SUMMARY frontmatter gap (5 partial requirements become fully satisfied) and delete all dead code from Phase 01 superseded components.

Purpose: The v1.1 milestone audit identified WORK-01 through WORK-05 as "partial" solely because Phase 05 SUMMARY files lack `requirements_completed` frontmatter. Additionally, three Phase 01 components (ConditionToggle, SyncButton, DashboardView) are dead code — superseded but never deleted.

Output: Updated SUMMARY files with correct frontmatter; 5 dead files removed from codebase.
</objective>

<execution_context>
@C:/Users/kiryl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kiryl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.1-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add requirements_completed frontmatter to Phase 05 SUMMARY files</name>
  <files>
    .planning/phases/05-3-panel-combat-workspace/05-01-SUMMARY.md
    .planning/phases/05-3-panel-combat-workspace/05-02-SUMMARY.md
    .planning/phases/05-3-panel-combat-workspace/05-03-SUMMARY.md
  </files>
  <read_first>
    .planning/phases/05-3-panel-combat-workspace/05-01-SUMMARY.md
    .planning/phases/05-3-panel-combat-workspace/05-02-SUMMARY.md
    .planning/phases/05-3-panel-combat-workspace/05-03-SUMMARY.md
    .planning/v1.1-MILESTONE-AUDIT.md
  </read_first>
  <action>
Add `requirements_completed` field to the YAML frontmatter of each Phase 05 SUMMARY file. The field goes inside the existing `---` frontmatter block, after the existing `requirements` field.

**05-01-SUMMARY.md** already has `requirements: [WORK-03, WORK-04, WORK-05]`. Add below it:
```yaml
requirements_completed: [WORK-03, WORK-04, WORK-05]
```

**05-02-SUMMARY.md** has no `requirements` field. Add both fields after the `metrics` block:
```yaml
requirements: [WORK-02]
requirements_completed: [WORK-02]
```

**05-03-SUMMARY.md** already has `requirements` listing WORK-01 through WORK-05. Add below it:
```yaml
requirements_completed: [WORK-01, WORK-02, WORK-03, WORK-04, WORK-05]
```

Do NOT change any other content in these files. Only add the frontmatter field(s).
  </action>
  <verify>
    <automated>grep -c "requirements_completed" .planning/phases/05-3-panel-combat-workspace/05-01-SUMMARY.md .planning/phases/05-3-panel-combat-workspace/05-02-SUMMARY.md .planning/phases/05-3-panel-combat-workspace/05-03-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - 05-01-SUMMARY.md frontmatter contains `requirements_completed: [WORK-03, WORK-04, WORK-05]`
    - 05-02-SUMMARY.md frontmatter contains `requirements: [WORK-02]` and `requirements_completed: [WORK-02]`
    - 05-03-SUMMARY.md frontmatter contains `requirements_completed: [WORK-01, WORK-02, WORK-03, WORK-04, WORK-05]`
    - grep for `requirements_completed` returns count 1 for each of the 3 files
  </acceptance_criteria>
  <done>All three Phase 05 SUMMARY files have requirements_completed frontmatter, closing the documentation gap for WORK-01 through WORK-05</done>
</task>

<task type="auto">
  <name>Task 2: Delete dead code — ConditionToggle, SyncButton, DashboardView</name>
  <files>
    src/components/ConditionToggle.vue
    src/components/__tests__/ConditionToggle.test.ts
    src/components/SyncButton.vue
    src/components/__tests__/SyncButton.test.ts
    src/views/DashboardView.vue
  </files>
  <read_first>
    src/components/ConditionToggle.vue
    src/components/__tests__/ConditionToggle.test.ts
    src/components/SyncButton.vue
    src/components/__tests__/SyncButton.test.ts
    src/views/DashboardView.vue
  </read_first>
  <action>
Delete these 5 files — they are confirmed dead code with no remaining imports outside their own test/view files:

1. `src/components/ConditionToggle.vue` — superseded by ConditionBadge.vue in Phase 01. No imports anywhere except its own test file.
2. `src/components/__tests__/ConditionToggle.test.ts` — test for the deleted component.
3. `src/components/SyncButton.vue` — superseded by SyncView.vue in Phase 01. Only imported by DashboardView.vue (also being deleted).
4. `src/components/__tests__/SyncButton.test.ts` — test for the deleted component.
5. `src/views/DashboardView.vue` — removed from router in Phase 01. Imports SyncButton (also being deleted). No other references.

Use `rm` (or git rm) to delete each file. Then run the full test suite to confirm no regressions.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ConditionToggle.vue` does not exist (ls returns error)
    - `src/components/__tests__/ConditionToggle.test.ts` does not exist
    - `src/components/SyncButton.vue` does not exist
    - `src/components/__tests__/SyncButton.test.ts` does not exist
    - `src/views/DashboardView.vue` does not exist
    - `npx vitest run` exits 0 with all tests passing (expected: ~325+ tests, down from 345 due to deleted test files)
    - `grep -r "ConditionToggle" src/` returns no matches
    - `grep -r "SyncButton" src/` returns no matches
    - `grep -r "DashboardView" src/` returns no matches
  </acceptance_criteria>
  <done>All 5 dead code files deleted. No imports of ConditionToggle, SyncButton, or DashboardView remain anywhere in src/. Full test suite passes.</done>
</task>

</tasks>

<verification>
- grep "requirements_completed" across all 3 Phase 05 SUMMARY files returns 3 matches
- ls confirms 5 dead code files no longer exist
- grep -r for deleted component names in src/ returns 0 matches
- Full test suite passes with 0 failures
</verification>

<success_criteria>
- Phase 05 SUMMARY files have requirements_completed frontmatter listing WORK-01 through WORK-05
- ConditionToggle.vue, SyncButton.vue, DashboardView.vue and their test files are deleted
- All remaining tests pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/08-tech-debt-cleanup/08-01-SUMMARY.md`
</output>
