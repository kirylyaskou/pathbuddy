# Quick Task 260405-64b: Redesign Condition Picker — Modal + Full Names + Grouping

**Created:** 2026-04-05
**Status:** pending

## Task

Redesign the condition picker in the combat tracker:
1. Replace the narrow `Popover` (w-80) with a `Dialog` modal — larger surface, no truncation
2. Remove `truncate` from `ConditionPill` so full condition names are visible
3. Grouping by type already works (tabs exist) — just needs the wider canvas to display properly
4. Improve "No active conditions" empty state in `ConditionSection`

## Files

- `src/features/combat-tracker/ui/ConditionCombobox.tsx` — main change: Popover → Dialog
- `src/widgets/combatant-detail/ui/ConditionSection.tsx` — empty state polish

## Tasks

### Task 1: Replace Popover with Dialog in ConditionCombobox

**Files:** `src/features/combat-tracker/ui/ConditionCombobox.tsx`

**Action:**
- Import `Dialog, DialogContent, DialogTrigger, DialogHeader, DialogTitle` from `@/shared/ui/dialog`
- Remove `Popover, PopoverTrigger, PopoverContent` imports
- Wrap component in `Dialog open={open} onOpenChange={...}` (same state logic)
- `DialogTrigger asChild` wraps the "+ Condition" button
- `DialogContent` with `className="sm:max-w-lg"` — gives ~156px per pill in 3-col grid
- Add `DialogHeader` + `DialogTitle` ("Add Condition") for accessibility
- `ConditionPill`: remove `truncate`, use `text-wrap break-words` for full names
- Grid `max-h-48` → `max-h-64` for more breathing room
- On condition apply: `setOpen(false)` closes dialog — same behavior

### Task 2: Polish empty state in ConditionSection

**Files:** `src/widgets/combatant-detail/ui/ConditionSection.tsx`

**Action:**
- Replace plain `<p className="text-xs text-muted-foreground">No active conditions</p>`
  with `<div className="rounded border border-dashed border-border/40 py-3 text-center text-xs text-muted-foreground">No active conditions</div>`
