---
phase: 06-conditions-module
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/pf2e/conditions.ts
  - src/lib/pf2e/__tests__/conditions.test.ts
  - src/lib/pf2e/index.ts
autonomous: true
requirements:
  - COND-01
  - COND-02
  - COND-03

must_haves:
  truths:
    - "CONDITION_SLUGS exports exactly 44 PF2e condition strings as a const array"
    - "VALUED_CONDITIONS exports the 11 conditions that carry numeric severity"
    - "CONDITION_GROUPS exports detection (4 members) and attitudes (5 members) group mappings"
    - "ConditionManager.add() stores a condition and ConditionManager.has()/get() retrieves it"
    - "ConditionManager.remove('dying') auto-increments wounded by 1"
    - "ConditionManager.add() of a group member removes all other members of that group"
  artifacts:
    - path: "src/lib/pf2e/conditions.ts"
      provides: "CONDITION_SLUGS, VALUED_CONDITIONS, CONDITION_GROUPS constants and ConditionManager class"
      exports: ["CONDITION_SLUGS", "VALUED_CONDITIONS", "CONDITION_GROUPS", "ConditionManager"]
      contains: "class ConditionManager"
    - path: "src/lib/pf2e/__tests__/conditions.test.ts"
      provides: "Unit tests for COND-01, COND-02, COND-03"
      contains: "describe('COND-01"
    - path: "src/lib/pf2e/index.ts"
      provides: "Barrel re-exports for conditions module"
      contains: "from './conditions'"
  key_links:
    - from: "src/lib/pf2e/__tests__/conditions.test.ts"
      to: "src/lib/pf2e/conditions.ts"
      via: "import from @/lib/pf2e"
      pattern: "import.*CONDITION_SLUGS.*from '@/lib/pf2e'"
    - from: "src/lib/pf2e/index.ts"
      to: "src/lib/pf2e/conditions.ts"
      via: "barrel re-export"
      pattern: "export.*from './conditions'"
---

<objective>
Implement the PF2e conditions module as a TDD plan: constants for all 44 condition slugs, valued conditions subset, condition groups, and a ConditionManager class that enforces dying/wounded interaction and group exclusivity rules.

Purpose: Final phase of v2.0 milestone -- provides the conditions foundation for future combat tracking UI integration.
Output: `src/lib/pf2e/conditions.ts` with full test coverage, barrel-exported via `index.ts`.
</objective>

<execution_context>
@C:/Users/kiryl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kiryl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-conditions-module/06-CONTEXT.md
@.planning/phases/06-conditions-module/06-RESEARCH.md

@src/lib/pf2e/index.ts
@src/lib/pf2e/damage.ts
@src/lib/pf2e/iwr.ts

<interfaces>
<!-- Existing patterns the executor must follow. Extracted from codebase. -->

From src/lib/pf2e/damage.ts (as const + derived union pattern):
```typescript
export const DAMAGE_CATEGORIES = ['physical', 'energy', 'other'] as const
export type DamageCategory = (typeof DAMAGE_CATEGORIES)[number]
```

From src/lib/pf2e/index.ts (barrel export pattern):
```typescript
export { DAMAGE_TYPES, DAMAGE_TYPE_CATEGORY, ... } from './damage'
export type { DamageType, DamageCategory, ... } from './damage'
```

From src/lib/pf2e/__tests__/iwr.test.ts (test import pattern):
```typescript
import { describe, it, expect } from 'vitest'
import { applyIWR, createImmunity, ... } from '@/lib/pf2e'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for conditions constants and ConditionManager</name>
  <files>src/lib/pf2e/__tests__/conditions.test.ts, src/lib/pf2e/conditions.ts</files>
  <read_first>
    - src/lib/pf2e/__tests__/iwr.test.ts (test file pattern: imports, describe blocks, section headers)
    - src/lib/pf2e/damage.ts (as const array + derived union type pattern to replicate)
    - src/lib/pf2e/index.ts (current barrel exports -- will need conditions added)
    - .planning/phases/06-conditions-module/06-RESEARCH.md (exact 44 slug list, valued conditions list, code examples)
  </read_first>
  <behavior>
    COND-01 Constants:
    - CONDITION_SLUGS.length === 44
    - CONDITION_SLUGS includes 'blinded', 'off-guard', 'cursebound', 'malevolence', 'wounded', 'dying'
    - CONDITION_SLUGS does NOT include 'flat-footed' (deprecated Remaster)
    - Every element of VALUED_CONDITIONS is also in CONDITION_SLUGS
    - VALUED_CONDITIONS contains exactly: 'clumsy', 'doomed', 'drained', 'dying', 'enfeebled', 'frightened', 'sickened', 'slowed', 'stunned', 'stupefied', 'wounded' (11 items)
    - CONDITION_GROUPS.detection deep-equals ['observed', 'hidden', 'undetected', 'unnoticed']
    - CONDITION_GROUPS.attitudes deep-equals ['hostile', 'unfriendly', 'indifferent', 'friendly', 'helpful']

    COND-02 ConditionManager operations:
    - new ConditionManager().has('blinded') === false (empty state)
    - After add('blinded'), has('blinded') === true and get('blinded') === 1
    - After add('clumsy', 3), get('clumsy') === 3 (valued condition stores value)
    - After add('clumsy', 3) then add('clumsy', 2), get('clumsy') === 2 (overwrite)
    - After add('blinded') then add('blinded'), has('blinded') === true, get('blinded') === 1 (idempotent boolean)
    - After add('blinded') then remove('blinded'), has('blinded') === false
    - remove('blinded') on empty manager does not throw (no-op)

    COND-03 Rule interactions:
    - After add('dying', 1) then remove('dying'), has('wounded') === true and get('wounded') === 1
    - After add('wounded', 2) then add('dying', 1) then remove('dying'), get('wounded') === 3
    - remove('dying') on manager without dying does NOT create wounded (no-op guard)
    - After add('friendly') then add('hostile'), has('friendly') === false and has('hostile') === true
    - After add('observed') then add('hidden'), has('observed') === false and has('hidden') === true
    - After add('friendly') then add('friendly'), has('friendly') === true (re-add same group member is idempotent)
  </behavior>
  <action>
    1. Create a minimal stub file `src/lib/pf2e/conditions.ts` that exports empty/placeholder values so the test file can import without compile errors:
       ```typescript
       export const CONDITION_SLUGS = [] as const
       export type ConditionSlug = (typeof CONDITION_SLUGS)[number]
       export const VALUED_CONDITIONS = [] as const
       export type ValuedCondition = (typeof VALUED_CONDITIONS)[number]
       export const CONDITION_GROUPS: Record<string, ConditionSlug[]> = {}
       export class ConditionManager {
         add(_slug: ConditionSlug, _value = 1): void {}
         remove(_slug: ConditionSlug): void {}
         has(_slug: ConditionSlug): boolean { return false }
         get(_slug: ConditionSlug): number | undefined { return undefined }
       }
       ```
    2. Add barrel exports to `src/lib/pf2e/index.ts` -- append these lines:
       ```typescript
       export {
         CONDITION_SLUGS,
         VALUED_CONDITIONS,
         CONDITION_GROUPS,
         ConditionManager,
       } from './conditions'
       export type { ConditionSlug, ValuedCondition } from './conditions'
       ```
    3. Create `src/lib/pf2e/__tests__/conditions.test.ts` with all tests from the behavior block above. Use section header comments matching existing pattern: `// --- COND-01: Constants` etc. Import everything from `@/lib/pf2e`.
    4. Run tests -- they MUST fail (RED phase). Expect: COND-01 length/content tests fail, COND-02 add/get tests fail, COND-03 rule interaction tests fail.
    5. Commit: `test(06-01): add failing tests for conditions module`
  </action>
  <verify>
    <automated>cd D:/pathbuddy && pnpm test -- src/lib/pf2e/__tests__/conditions.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/pf2e/__tests__/conditions.test.ts exists and contains `describe('COND-01`
    - src/lib/pf2e/__tests__/conditions.test.ts contains `describe('COND-02`
    - src/lib/pf2e/__tests__/conditions.test.ts contains `describe('COND-03`
    - src/lib/pf2e/__tests__/conditions.test.ts contains `CONDITION_SLUGS.length` assertion
    - src/lib/pf2e/__tests__/conditions.test.ts contains `expect(cm.get('wounded')).toBe(1)` (dying removal test)
    - src/lib/pf2e/__tests__/conditions.test.ts contains `expect(cm.has('friendly')).toBe(false)` (group exclusivity test)
    - src/lib/pf2e/conditions.ts exists with `export const CONDITION_SLUGS` stub
    - src/lib/pf2e/conditions.ts exists with `export class ConditionManager`
    - src/lib/pf2e/index.ts contains `from './conditions'`
    - Test run shows FAILURES (RED phase) -- at least 10 failing tests
  </acceptance_criteria>
  <done>Test file committed with all COND-01/02/03 behaviors specified. All tests fail against stub implementation (RED). Barrel exports wired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement conditions module to pass all tests</name>
  <files>src/lib/pf2e/conditions.ts</files>
  <read_first>
    - src/lib/pf2e/conditions.ts (current stub from Task 1)
    - src/lib/pf2e/__tests__/conditions.test.ts (tests to satisfy)
    - src/lib/pf2e/damage.ts (as const pattern with section headers and source URL comments)
    - src/lib/pf2e/iwr.ts (class + internal helper pattern, readonly interfaces)
    - .planning/phases/06-conditions-module/06-RESEARCH.md (exact slug list, code examples for add/remove)
  </read_first>
  <behavior>
    All tests from Task 1 must pass (GREEN phase). Specifically:
    - CONDITION_SLUGS has exactly 44 entries in alphabetical order
    - VALUED_CONDITIONS has exactly 11 entries
    - CONDITION_GROUPS has detection (4) and attitudes (5) keys
    - ConditionManager.add() sets value, enforces group exclusivity
    - ConditionManager.remove() deletes condition, increments wounded when removing dying
    - ConditionManager.has()/get() read from internal Map
  </behavior>
  <action>
    Replace the stub `src/lib/pf2e/conditions.ts` with the full implementation:

    1. **Section: Constants (COND-01)** -- Use section header `// --- Condition Slugs ---...` with source URL comment `// Source: Foundry VTT PF2e values.ts + AON https://2e.aonprd.com/Conditions.aspx`

    2. **CONDITION_SLUGS** -- exactly these 44 slugs in alphabetical order as an `as const` array:
       `'blinded', 'broken', 'clumsy', 'concealed', 'confused', 'controlled', 'cursebound', 'dazzled', 'deafened', 'doomed', 'drained', 'dying', 'encumbered', 'enfeebled', 'fascinated', 'fatigued', 'fleeing', 'friendly', 'frightened', 'grabbed', 'helpful', 'hidden', 'hostile', 'immobilized', 'indifferent', 'invisible', 'malevolence', 'observed', 'off-guard', 'paralyzed', 'persistent-damage', 'petrified', 'prone', 'quickened', 'restrained', 'sickened', 'slowed', 'stunned', 'stupefied', 'unconscious', 'undetected', 'unfriendly', 'unnoticed', 'wounded'`

    3. **ConditionSlug type** -- `export type ConditionSlug = (typeof CONDITION_SLUGS)[number]`

    4. **VALUED_CONDITIONS** -- exactly these 11 slugs as `as const` array:
       `'clumsy', 'doomed', 'drained', 'dying', 'enfeebled', 'frightened', 'sickened', 'slowed', 'stunned', 'stupefied', 'wounded'`

    5. **ValuedCondition type** -- `export type ValuedCondition = (typeof VALUED_CONDITIONS)[number]`

    6. **CONDITION_GROUPS** -- `Record<string, ConditionSlug[]>` with exactly two keys:
       - `detection: ['observed', 'hidden', 'undetected', 'unnoticed']`
       - `attitudes: ['hostile', 'unfriendly', 'indifferent', 'friendly', 'helpful']`

    7. **Section: ConditionManager (COND-02, COND-03)** -- Use section header `// --- ConditionManager ---...`

    8. **ConditionManager class** with private `readonly conditions = new Map<ConditionSlug, number>()`:

       **add(slug: ConditionSlug, value = 1): void** --
       - First, enforce group exclusivity: iterate `Object.entries(CONDITION_GROUPS)`, if `members.includes(slug)`, delete all other members (`member !== slug`) from `this.conditions`, then break
       - Then, `this.conditions.set(slug, value)` -- this handles both new adds and value overwrites

       **remove(slug: ConditionSlug): void** --
       - If `!this.conditions.has(slug)`, return immediately (no-op guard -- critical for dying rule)
       - `this.conditions.delete(slug)`
       - If `slug === 'dying'`, increment wounded: `const current = this.conditions.get('wounded') ?? 0; this.conditions.set('wounded', current + 1)`

       **has(slug: ConditionSlug): boolean** -- `return this.conditions.has(slug)`

       **get(slug: ConditionSlug): number | undefined** -- `return this.conditions.get(slug)`

    9. Run tests -- ALL must pass (GREEN phase).
    10. Commit: `feat(06-01): implement conditions module with ConditionManager`
  </action>
  <verify>
    <automated>cd D:/pathbuddy && pnpm test -- src/lib/pf2e/__tests__/conditions.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/pf2e/conditions.ts contains `export const CONDITION_SLUGS = [` with 44 entries
    - src/lib/pf2e/conditions.ts contains `export const VALUED_CONDITIONS = [` with 11 entries
    - src/lib/pf2e/conditions.ts contains `export const CONDITION_GROUPS: Record<string, ConditionSlug[]>`
    - src/lib/pf2e/conditions.ts contains `export class ConditionManager`
    - src/lib/pf2e/conditions.ts contains `private readonly conditions = new Map<ConditionSlug, number>()`
    - src/lib/pf2e/conditions.ts contains `if (slug === 'dying')` in the remove method
    - src/lib/pf2e/conditions.ts contains `CONDITION_GROUPS` usage in the add method for group exclusivity
    - src/lib/pf2e/conditions.ts contains `// Source:` comment with AON URL
    - All conditions.test.ts tests pass (0 failures)
    - `pnpm test` full suite passes (no regressions in other modules)
  </acceptance_criteria>
  <done>All COND-01/02/03 tests pass. CONDITION_SLUGS has exactly 44 slugs, VALUED_CONDITIONS has 11, CONDITION_GROUPS has detection+attitudes, ConditionManager enforces dying/wounded and group exclusivity rules. Full test suite green.</done>
</task>

</tasks>

<verification>
1. `pnpm test -- src/lib/pf2e/__tests__/conditions.test.ts` -- all conditions tests pass
2. `pnpm test` -- full test suite passes (no regressions)
3. `grep -c "export const CONDITION_SLUGS" src/lib/pf2e/conditions.ts` returns 1
4. `grep -c "export class ConditionManager" src/lib/pf2e/conditions.ts` returns 1
5. `grep "from './conditions'" src/lib/pf2e/index.ts` shows barrel exports present
</verification>

<success_criteria>
- CONDITION_SLUGS exports exactly 44 PF2e Remaster condition slugs (no flat-footed, includes off-guard/cursebound/malevolence)
- VALUED_CONDITIONS exports exactly 11 valued conditions
- CONDITION_GROUPS exports detection (4 slugs) and attitudes (5 slugs) groups
- ConditionManager.add/remove/has/get all function correctly with Map backing
- Dying removal auto-increments wounded (including when wounded already present)
- Group exclusivity removes old member when new member added
- All tests pass, full suite green, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/06-conditions-module/06-01-SUMMARY.md`
</output>
